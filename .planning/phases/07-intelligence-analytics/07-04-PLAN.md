---
phase: 07-intelligence-analytics
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - app/compare/page.tsx
  - app/components/ComparePageClient.tsx
  - app/components/ModelSelector.tsx
  - app/components/ComparisonCard.tsx
  - app/components/BellIcon.tsx
  - app/components/AlertSetForm.tsx
  - app/components/AlertBanner.tsx
  - app/components/ModelDetailClient.tsx
  - tests/components/compare-page.test.tsx
  - tests/components/model-selector.test.tsx
  - tests/components/alert-banner.test.tsx
autonomous: true
requirements:
  - INTL-04
  - INTL-05
must_haves:
  truths:
    - "User can select 2-5 models for side-by-side comparison"
    - "Comparison shows pricing, context window, practical costs, free tier, confidence"
    - "Selected models sync to URL query parameter for sharing"
    - "User can set a price alert from the model detail page via bell icon"
    - "Alert banner shows on page load when any alert threshold is triggered"
  artifacts:
    - path: "app/compare/page.tsx"
      provides: "Server component for model comparison with URL param sync"
    - path: "app/components/ComparePageClient.tsx"
      provides: "Client wrapper with model selectors and comparison grid"
    - path: "app/components/ModelSelector.tsx"
      provides: "Custom searchable dropdown for model selection"
    - path: "app/components/ComparisonCard.tsx"
      provides: "Single model comparison card with all dimensions"
    - path: "app/components/BellIcon.tsx"
      provides: "Interactive bell icon for setting alerts on model detail page"
    - path: "app/components/AlertSetForm.tsx"
      provides: "Inline form for setting alert threshold"
    - path: "app/components/AlertBanner.tsx"
      provides: "On-page toast/banner for triggered alerts"
    - path: "app/components/ModelDetailClient.tsx"
      provides: "Modified to include BellIcon in hero section"
  key_links:
    - from: "app/compare/page.tsx"
      to: "src/db/schema.ts"
      via: "Drizzle query for selected models"
      pattern: "db\\.select.*from.*extractions"
    - from: "app/components/ComparePageClient.tsx"
      to: "app/components/ModelSelector.tsx"
      via: "import and render ModelSelector"
      pattern: "import.*ModelSelector"
    - from: "app/components/BellIcon.tsx"
      to: "app/lib/alerts.ts"
      via: "import addAlert, removeAlert"
      pattern: "import.*alerts"
    - from: "app/components/ModelDetailClient.tsx"
      to: "app/components/BellIcon.tsx"
      via: "import and render BellIcon"
      pattern: "import.*BellIcon"
    - from: "app/layout.tsx"
      to: "app/components/AlertBanner.tsx"
      via: "import and render AlertBanner"
      pattern: "import.*AlertBanner"
---

<objective>
Build the /compare page for multi-model side-by-side comparison, add bell icon for alert setting on model detail pages, and create the alert banner for on-page notifications.

Purpose: Delivers INTL-04 (multi-model comparison) and completes INTL-05 (price alerts with bell icon + on-page banner) as the final vertical slice.
Output: Working /compare page, bell icon on model detail, alert banner across site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-intelligence-analytics/07-CONTEXT.md
@.planning/phases/07-intelligence-analytics/07-RESEARCH.md
@.planning/phases/07-intelligence-analytics/07-UI-SPEC.md

@app/components/ModelDetailClient.tsx
@app/components/CostCalculator.tsx
@app/components/PricingTable.tsx
@app/lib/pricing-utils.ts
@app/lib/cost-scenarios.ts
@app/lib/alerts.ts
@app/page.tsx
@src/db/schema.ts
@app/layout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /compare page with model selectors and comparison cards</name>
  <files>app/compare/page.tsx, app/components/ComparePageClient.tsx, app/components/ModelSelector.tsx, app/components/ComparisonCard.tsx, tests/components/compare-page.test.tsx, tests/components/model-selector.test.tsx</files>
  <read_first>
    - app/page.tsx (server component pattern with Drizzle query)
    - app/digest/page.tsx (searchParams pattern for URL params)
    - app/components/CostCalculator.tsx (practical cost calculation pattern, COST_SCENARIOS)
    - app/components/PricingTable.tsx (PricingRow type, confidence badges)
    - app/lib/pricing-utils.ts (formatPrice, formatContextWindow, sanitizeDisplayName, getConfidenceColor, calculateScenarioCosts, PracticalCost)
    - app/lib/cost-scenarios.ts (COST_SCENARIOS array)
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-10: /compare route with URL params, D-11: dropdown selectors, D-12: all dimensions, D-13: side-by-side cards)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (/compare page layout, model selector interaction, comparison cards, empty states, responsive, copywriting)
    - .planning/phases/07-intelligence-analytics/07-RESEARCH.md (URL query param sync pattern, searchable dropdown, Pitfall 3: URL encoding)
  </read_first>
  <behavior>
    - ModelSelector: custom searchable dropdown, type to filter, click to select, shows model name + provider
    - ComparePageClient: 2-5 model selectors, "Add model" button (disabled at 5), URL sync
    - ComparisonCard: shows pricing, context window, 4 practical costs, free tier status, confidence
    - /compare page reads ?models= query param, fetches data for selected models
    - Empty states: "Select models to compare" (0 selected), "Add another model" (1 selected)
    - URL sync: selected models encoded as comma-separated names in ?models= param
  </behavior>
  <action>
    Create five files for the /compare feature:

    1. **app/components/ModelSelector.tsx** -- Custom searchable dropdown per D-11:
       - Interface `ModelOption`: `{ modelName: string; sourceId: number; sourceName: string }`
       - Props: `{ models: ModelOption[]; selected: ModelOption | null; onSelect: (model: ModelOption) => void; placeholder?: string }`
       - State: open boolean, query string
       - Input: text input that shows selected model name or search query
       - Dropdown: absolute positioned list, filtered by query (match modelName or sourceName case-insensitive)
       - Each option: model name (font-medium) + source name (text-gray-500 ml-2)
       - Click option: call onSelect, close dropdown, clear query
       - Click outside: close dropdown (use useEffect with document click listener)
       - Keyboard: Escape closes dropdown
       - Styling per UI-SPEC: w-full px-3 py-2 border border-gray-300 rounded-lg text-sm
       - Dropdown: z-10 bg-white border border-gray-200 rounded-lg shadow-lg max-h-60 overflow-auto

    2. **app/components/ComparisonCard.tsx** -- Single model comparison card per D-12, D-13:
       - Props: `{ model: PricingRow; practicalCosts: PracticalCost[]; promotions: Promotion[]; exchangeRate?: number }`
       - Card container: border rounded-lg p-4 (per UI-SPEC)
       - Sections:
         - Header: model name (text-lg font-semibold), source name (text-sm text-gray-500), confidence badge
         - Pricing: input price / output price per 1M tokens (formatPrice)
         - Context window: formatContextWindow
         - Practical costs: list all 4 COST_SCENARIOS with their calculated costs (formatPrice)
         - Free tier status (D-12): if promotions.length > 0, show badge per type:
           - Free tier: `bg-green-100 text-green-800` badge with "Free tier" text + credits amount
           - Promotion: `bg-blue-100 text-blue-800` badge with description
           - Beta: `bg-purple-100 text-purple-800` badge with "Beta" text
           - If no promotions: show "No free tier" in gray text
       - Uses existing utility functions from pricing-utils.ts

    3. **app/components/ComparePageClient.tsx** -- Client wrapper per D-10, D-11, D-13:
       - Props: `{ allModels: ModelOption[]; initialSelected: ModelOption[]; practicalCostsMap: Record<string, PracticalCost[]>; promotionsMap: Record<string, Promotion[]>; exchangeRate: number }`
       - State: selectedModels array (initialized from initialSelected)
       - URL sync via useRouter and useSearchParams:
         - On model change: update URL with `?models=name1,name2,name3` (encodeURIComponent per Pitfall 3)
         - On mount: read from searchParams (handled by server, passed as initialSelected)
       - Model selectors row: render 2-5 ModelSelector components
       - "Add model" button: adds empty selector (disabled when 5 selected)
       - "Remove" button on each selector (minimum 2)
       - Comparison cards: horizontal scroll container if >3 models (overflow-x-auto)
       - Grid: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 for <=3, horizontal scroll for >3
       - Empty states per UI-SPEC:
         - 0 selected: "Select models to compare" + body text
         - 1 selected: "Add another model" + body text
       - Page heading: "Compare Models" + subheading per UI-SPEC

    4. **app/compare/page.tsx** -- Server component per D-10, D-12:
       - `export const revalidate = 60`
       - Accept searchParams: `Promise<{ models?: string }>`
       - Parse models param: split by comma, decodeURIComponent, filter empty
       - Query 1: Fetch all distinct models (for selector dropdown) -- select modelName, sourceId, sourceName from extractions JOIN sources GROUP BY
       - Query 2: If models selected, fetch full pricing data for selected models
       - Query 3: Fetch practical costs for selected models (JOIN extractions with practical_costs)
       - Query 4: Fetch promotions for selected models (for free tier status per D-12):
         `db.select().from(promotions).where(inArray(promotions.modelPattern, modelNames))`
         Returns array of { modelPattern, type, description, credits, startDate, endDate }
       - Try/catch with empty fallbacks
       - Pass all data to ComparePageClient including promotions

    5. **tests/components/compare-page.test.tsx** + **tests/components/model-selector.test.tsx**:
       - ModelSelector: test renders input, test filters on type, test calls onSelect on click, test closes on Escape
       - ComparisonCard: test renders model name, test shows pricing, test shows practical costs
       - ComparePageClient: test shows empty state with 0 models, test shows "Add another" with 1 model
  </action>
  <verify>
    <automated>pnpm test tests/components/compare-page.test.tsx tests/components/model-selector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ModelSelector renders searchable dropdown with model name + provider
    - ModelSelector filters options on type, selects on click
    - ComparePageClient renders 2-5 model selectors with Add/Remove buttons
    - ComparisonCard shows pricing, context window, practical costs, free tier status, confidence
    - ComparisonCard shows green "Free tier" badge when promotions exist for model
    - URL sync: /compare?models=gpt-4o,claude-sonnet-4-5 works on page load
    - URL updates when models are added/removed
    - Empty states match UI-SPEC copywriting
    - Tests pass
  </acceptance_criteria>
  <done>User can visit /compare, select 2-5 models from searchable dropdowns, see side-by-side comparison with all dimensions including free tier status, and share via URL</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bell icon to model detail page and create alert banner</name>
  <files>app/components/BellIcon.tsx, app/components/AlertSetForm.tsx, app/components/AlertBanner.tsx, app/components/ModelDetailClient.tsx, app/layout.tsx, tests/components/alert-banner.test.tsx</files>
  <read_first>
    - app/components/ModelDetailClient.tsx (current hero section structure, where to add bell icon)
    - app/lib/alerts.ts (PriceAlert interface, getAlerts, addAlert, removeAlert -- created in Plan 01)
    - app/layout.tsx (root layout where AlertBanner will be added)
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-15: bell icon on model detail, D-16: on-page check, D-17: below threshold, D-18: alert structure)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (bell icon states, inline form, alert banner position, auto-dismiss, z-index, spacing)
    - .planning/phases/07-intelligence-analytics/07-RESEARCH.md (localStorage SSR mismatch pitfall, alert check flow)
  </read_first>
  <behavior>
    - BellIcon: gray outline when no alert, blue filled when alert exists
    - BellIcon click toggles inline AlertSetForm below
    - AlertSetForm: threshold input + "Set Alert" / "Remove Alert" buttons
    - AlertBanner: fixed bottom-right, shows triggered alerts (only below threshold per D-17), auto-dismiss 10s
    - AlertBanner receives currentPrices prop from layout, compares against thresholds (D-16)
    - ModelDetailClient: BellIcon added next to model name in hero section
    - layout.tsx: AlertBanner added to root layout
  </behavior>
  <action>
    Create four files and modify two existing files:

    1. **app/components/BellIcon.tsx** -- Interactive bell per D-15:
       - Props: `{ modelName: string; sourceId: number; currentPrice: number | null }`
       - State: hasAlert boolean (initialized from localStorage via useEffect), showForm boolean
       - useEffect: check if alert exists for this modelName+sourceId in localStorage
       - Bell icon: Unicode character or inline SVG
         - No alert: text-gray-400 (outline style)
         - Has alert: text-blue-600 (filled style)
       - Click: toggles showForm
       - Touch target: min 44px (p-2.5 to reach size per UI-SPEC)
       - Renders AlertSetForm when showForm is true

    2. **app/components/AlertSetForm.tsx** -- Inline threshold form per D-15, D-17, D-18:
       - Props: `{ modelName: string; sourceId: number; currentPrice: number | null; hasAlert: boolean; onAlertChange: (hasAlert: boolean) => void }`
       - State: thresholdInput string
       - Shows current price as reference: "Current price: {formatPrice(currentPrice)}/1M tokens"
       - Input: number input for threshold price (USD per 1M tokens)
       - If no existing alert: "Set Alert" button (bg-blue-600 text-white)
         - On click: validate input > 0, call addAlert({ modelName, sourceId, thresholdPrice, createdAt: new Date().toISOString() }), call onAlertChange(true)
       - If existing alert: "Remove Alert" button (bg-red-600 text-white)
         - On click: call removeAlert(modelName, sourceId), call onAlertChange(false)
       - Inline confirmation per UI-SPEC for remove: "Remove alert for {modelName}? You will no longer be notified when the price drops below {threshold}."
       - Container: mt-2 p-3 bg-gray-50 rounded-lg border

    3. **app/components/AlertBanner.tsx** -- On-page toast per D-16, D-17:
       - Client component with 'use client'
       - Props: `{ currentPrices?: Record<string, number> }` where key is `${modelName}:${sourceId}` and value is inputPricePer1m
       - State: triggeredAlerts array of `{ modelName: string; thresholdPrice: number; currentPrice: number }`, visible boolean
       - useEffect on mount (D-16: "check on page load"):
         - Read alerts from getAlerts()
         - For each alert, look up currentPrices[`${alert.modelName}:${alert.sourceId}`]
         - If current price exists AND currentPrice < alert.thresholdPrice (D-17: "below threshold only"):
           - Add to triggeredAlerts array
         - If triggeredAlerts.length > 0, set visible = true
       - How currentPrices get passed:
         - app/layout.tsx is a server component that queries latest prices for all alerted models
         - Query: `db.select({ modelName: extractions.modelName, sourceId: extractions.sourceId, inputPricePer1m: extractions.inputPricePer1m }).from(extractions).where(inArray(extractions.modelName, alertedModelNames))`
         - Only runs if alerts exist in cookies (check cookie 'has_alerts' set by AlertSetForm)
         - Passes currentPrices prop to client AlertBanner
       - Display for each triggered alert:
         - "🔔 {modelName}: price is {currentPrice}/1M tokens (your threshold: {thresholdPrice}/1M)"
         - Green background (bg-green-50) since price is below threshold
       - Fixed position: fixed bottom-4 right-4 z-50 per UI-SPEC
       - Dismiss: X button per triggered alert, or "Dismiss all"
       - Auto-dismiss: 10 second setTimeout per UI-SPEC
       - Stack: if multiple, vertical stack with gap-2
       - Fade out animation: opacity transition over 300ms

    4. **Modify app/components/ModelDetailClient.tsx** -- Add BellIcon per D-15:
       - Import BellIcon
       - Add BellIcon next to the model name in the hero section (after h1)
       - Pass: modelName={model.modelName}, sourceId={model.sourceId}, currentPrice={model.inputPricePer1m}
       - The bell icon sits inline with the model name: `<h1>{displayName} <BellIcon ... /></h1>`

    5. **Modify app/layout.tsx** -- Add AlertBanner to root layout:
       - Import AlertBanner, db, extractions, inArray
       - Check for 'has_alerts' cookie (set by AlertSetForm when alert is created)
       - If cookie exists, query current prices for alerted models:
         `db.select({ modelName: extractions.modelName, sourceId: extractions.sourceId, inputPricePer1m: extractions.inputPricePer1m }).from(extractions).where(inArray(extractions.modelName, alertedModelNames))`
       - Build currentPrices Record<string, number> from query results
       - Add `<AlertBanner currentPrices={currentPrices} />` inside body, after the TopNav and pt-14 div
       - Position will be handled by AlertBanner's fixed positioning

    6. **tests/components/alert-banner.test.tsx**:
       - Test renders nothing when no alerts in localStorage
       - Test renders alert count when alerts exist
       - Test dismiss button removes banner
       - Test auto-dismiss after timeout (use vi.useFakeTimers)
  </action>
  <verify>
    <automated>pnpm test tests/components/alert-banner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BellIcon.tsx renders bell icon with gray/blue states
    - BellIcon click toggles AlertSetForm
    - AlertSetForm "Set Alert" stores alert in localStorage via addAlert()
    - AlertSetForm "Remove Alert" removes alert via removeAlert()
    - AlertBanner reads alerts from localStorage on mount
    - AlertBanner compares current prices against thresholds (D-16)
    - AlertBanner only shows triggered alerts where currentPrice < thresholdPrice (D-17)
    - AlertBanner displays per-alert message: "🔔 {modelName}: price is {price}/1M tokens (your threshold: {threshold}/1M)"
    - AlertBanner positioned fixed bottom-4 right-4 z-50
    - AlertBanner auto-dismisses after 10 seconds
    - ModelDetailClient includes BellIcon next to model name
    - layout.tsx includes AlertBanner component with server-fetched current prices
    - tests/components/alert-banner.test.tsx passes
  </acceptance_criteria>
  <done>User can set price alerts via bell icon on model detail page, and sees triggered alert banner across the site when current price drops below threshold. Compare page shows multi-model side-by-side comparison with all dimensions including free tier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->localStorage | Alert data and comparison state in browser |
| server->client | Model pricing data from database |
| URL->server | Model names in ?models= query parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | URL model names | mitigate | Validate model names against database before rendering; use encodeURIComponent/decodeURIComponent per Pitfall 3 |
| T-07-06 | Tampering | localStorage alerts | accept | Per-device convenience data, no server-side security impact |
| T-07-07 | Information Disclosure | comparison URL | accept | Model names are public data, no PII in URLs |
</threat_model>

<verification>
- /compare page loads with model selectors
- Selecting models updates URL and shows comparison cards
- Bell icon appears on model detail pages
- Setting alert stores in localStorage
- Alert banner appears when alerts exist
- All tests pass
</verification>

<success_criteria>
- /compare page allows selecting 2-5 models with searchable dropdowns
- Comparison cards show all dimensions (pricing, context, practical costs, confidence)
- URL sync works: /compare?models=x,y loads with pre-selected models
- Bell icon on model detail page toggles alert set/remove form
- Alert banner shows on site when alerts are active
- All new component tests pass
</success_criteria>

<output>
Create `.planning/phases/07-intelligence-analytics/07-04-SUMMARY.md` when done
</output>
