---
phase: 10-consumer-pricing-subscription-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/providers/consumer/chatgpt/config.ts
  - src/providers/consumer/chatgpt/adapter.ts
  - src/providers/consumer/gemini/config.ts
  - src/providers/consumer/gemini/adapter.ts
  - src/providers/consumer/claude/config.ts
  - src/providers/consumer/claude/adapter.ts
  - src/providers/consumer/perplexity/config.ts
  - src/providers/consumer/perplexity/adapter.ts
  - src/providers/consumer/copilot/config.ts
  - src/providers/consumer/copilot/adapter.ts
  - src/providers/consumer/poe/config.ts
  - src/providers/consumer/poe/adapter.ts
  - src/providers/consumer/grok/config.ts
  - src/providers/consumer/grok/adapter.ts
  - src/providers/consumer/you/config.ts
  - src/providers/consumer/you/adapter.ts
  - src/providers/consumer/phind/config.ts
  - src/providers/consumer/phind/adapter.ts
  - src/providers/consumer/cursor/config.ts
  - src/providers/consumer/cursor/adapter.ts
  - src/providers/consumer/registry.ts
  - src/pipeline/workers/extract.ts
  - src/pipeline/orchestrator.ts
  - tests/pipeline/subscription-pipeline.test.ts
autonomous: true
requirements:
  - DCOL-08

must_haves:
  truths:
    - "At least 10 consumer adapters exist and are registered in the consumer registry"
    - "Each Tier 1 consumer adapter (ChatGPT, Gemini, Claude, Perplexity, Copilot) crawls its provider's subscription page"
    - "Each Tier 2 consumer adapter (Poe, Grok, You.com, Phind, Cursor) crawls its provider's subscription page"
    - "Each adapter has expectedPlanNames for extraction cross-checking (review finding #1)"
    - "Each adapter has adapter-level timeout config (review finding #6)"
    - "Extract worker upserts subscription plan data into subscription_plans table"
    - "Pipeline orchestrator handles consumer adapter failures gracefully -- one adapter failure does not fail the whole daily run (review finding #6)"
    - "Pipeline orchestrator enqueues collect jobs for all registered consumer adapters"
  artifacts:
    - path: "src/providers/consumer/chatgpt/adapter.ts"
      provides: "ChatGPT consumer adapter with expectedPlanNames and provider-specific prompt"
      exports: ["ChatGPTConsumerAdapter"]
    - path: "src/providers/consumer/gemini/adapter.ts"
      provides: "Gemini consumer adapter"
      exports: ["GeminiConsumerAdapter"]
    - path: "src/providers/consumer/claude/adapter.ts"
      provides: "Claude consumer adapter"
      exports: ["ClaudeConsumerAdapter"]
    - path: "src/providers/consumer/perplexity/adapter.ts"
      provides: "Perplexity consumer adapter"
      exports: ["PerplexityConsumerAdapter"]
    - path: "src/providers/consumer/copilot/adapter.ts"
      provides: "Copilot consumer adapter"
      exports: ["CopilotConsumerAdapter"]
    - path: "src/providers/consumer/poe/adapter.ts"
      provides: "Poe consumer adapter"
      exports: ["PoeConsumerAdapter"]
    - path: "src/providers/consumer/grok/adapter.ts"
      provides: "Grok consumer adapter"
      exports: ["GrokConsumerAdapter"]
    - path: "src/providers/consumer/you/adapter.ts"
      provides: "You.com consumer adapter"
      exports: ["YouConsumerAdapter"]
    - path: "src/providers/consumer/phind/adapter.ts"
      provides: "Phind consumer adapter"
      exports: ["PhindConsumerAdapter"]
    - path: "src/providers/consumer/cursor/adapter.ts"
      provides: "Cursor consumer adapter"
      exports: ["CursorConsumerAdapter"]
    - path: "src/pipeline/workers/extract.ts"
      provides: "subscription plan upsert logic"
      contains: "subscriptionPlans"
    - path: "src/pipeline/orchestrator.ts"
      provides: "consumer adapter job enqueueing with failure isolation"
      contains: "consumer"
    - path: "tests/pipeline/subscription-pipeline.test.ts"
      provides: "Pipeline behavior tests for subscription upsert, failure isolation, free trial projection"
  key_links:
    - from: "src/providers/consumer/chatgpt/adapter.ts"
      to: "src/providers/consumer/base.ts"
      via: "ChatGPTConsumerAdapter extends ConsumerAdapter"
      pattern: "extends ConsumerAdapter"
    - from: "src/providers/consumer/registry.ts"
      to: "src/providers/consumer/chatgpt/adapter.ts"
      via: "imports and registers ChatGPTConsumerAdapter"
      pattern: "registerConsumerAdapter.*ChatGPT"
    - from: "src/pipeline/workers/extract.ts"
      to: "src/db/schema.ts"
      via: "upserts into subscriptionPlans table"
      pattern: "insert.*subscriptionPlans"
    - from: "src/pipeline/orchestrator.ts"
      to: "src/providers/consumer/registry.ts"
      via: "getAllConsumerAdapters for job enqueueing"
      pattern: "getAllConsumerAdapters"
---

<objective>
Implement all 10 consumer provider adapters and integrate them into the data collection pipeline.

Purpose: Per D-01, the system must crawl subscription pricing pages from 10 consumer AI providers (5 Tier 1, 5 Tier 2). Each adapter extracts structured subscription plan data with provider-specific extraction prompts and expected plan name cross-checking (review finding #1). The extract worker persists subscription plans to the database. The orchestrator enqueues consumer adapter jobs with failure isolation (review finding #6). Pipeline behavior tests validate upsert, failure handling, and free trial projection (review finding #9).

Addresses review findings:
- Finding #1 (HIGH): Each adapter has expectedPlanNames, provider-specific prompt tuning, source constraints
- Finding #6 (MEDIUM): Adapter-level timeout config, failure isolation, partial-success reporting
- Finding #9 (MEDIUM): Pipeline behavior tests for upsert, failure isolation, free trial projection

Output: 10 consumer adapter classes (config + adapter each), extract worker subscription plan upsert, orchestrator consumer adapter integration with failure isolation, pipeline behavior tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-consumer-pricing-subscription-intelligence/10-CONTEXT.md
@.planning/phases/10-consumer-pricing-subscription-intelligence/10-RESEARCH.md
@src/providers/base.ts
@src/providers/openai/adapter.ts
@src/providers/openai/config.ts
@src/providers/registry.ts
@src/providers/consumer/base.ts
@src/providers/consumer/registry.ts
@src/providers/consumer/schemas.ts
@src/pipeline/workers/extract.ts
@src/pipeline/orchestrator.ts
@src/lib/ai-client.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Create Tier 1 consumer adapters with extraction reliability safeguards</name>
  <files>
    src/providers/consumer/chatgpt/config.ts,
    src/providers/consumer/chatgpt/adapter.ts,
    src/providers/consumer/gemini/config.ts,
    src/providers/consumer/gemini/adapter.ts,
    src/providers/consumer/claude/config.ts,
    src/providers/consumer/claude/adapter.ts,
    src/providers/consumer/perplexity/config.ts,
    src/providers/consumer/perplexity/adapter.ts,
    src/providers/consumer/copilot/config.ts,
    src/providers/consumer/copilot/adapter.ts,
    src/providers/consumer/registry.ts
  </files>
  <read_first>
    - src/providers/openai/adapter.ts (reference adapter pattern: extract() with generateObject, pricingSchema, truncate HTML)
    - src/providers/openai/config.ts (reference config pattern: ProviderConfig fields)
    - src/providers/consumer/base.ts (ConsumerAdapter base class from Plan 01)
    - src/providers/consumer/registry.ts (consumer registry from Plan 01)
    - src/providers/consumer/schemas.ts (consumerSubscriptionSchema from Plan 01)
    - src/providers/base.ts (ProviderConfig, ProviderExtraction, ConsumerSubscriptionPlan interfaces)
    - src/providers/types.ts (SourceTier, ProviderSource types)
    - src/lib/ai-client.ts (getAIModel() for AI extraction)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-CONTEXT.md (D-01 provider list and URLs)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-REVIEWS.md (finding #1 extraction reliability)
  </read_first>
  <action>
Create 5 Tier 1 consumer adapters following the pattern from `src/providers/openai/adapter.ts`, enhanced with review finding #1 (extraction reliability). Each adapter has two files: `config.ts` and `adapter.ts`.

**Config file pattern** -- each config exports a `ProviderConfig` object enhanced with consumer-specific fields:

Each config must include:
- Standard ProviderConfig fields: `name`, `baseUrl`, `pricingUrl`, `tier`, `currency`, `crawlFrequencyHours`, `apiKeyOptional`, `modelIdFormat`, `sources`
- **NEW per review #1:** `expectedPlanNames: string[]` -- known plan names for this provider, used for extraction cross-checking. If extraction returns a plan name not in this list (fuzzy match), set confidence to 'low_confidence'.
- **NEW per review #6:** `adapterTimeoutMs: number` -- per-adapter timeout in milliseconds. Default 30000 (30s) for Tier 1. If extraction exceeds this, abort and return empty results with extractionNotes.

**Tier 1 provider configs (per D-01):**
| Provider | name | pricingUrl | expectedPlanNames |
|----------|------|-----------|-------------------|
| ChatGPT | chatgpt-consumer | https://chatgpt.com/pricing | ["ChatGPT Free", "ChatGPT Plus", "ChatGPT Pro"] |
| Gemini | gemini-consumer | https://one.google.com/about/plans | ["Google One AI Premium", "Gemini Advanced"] |
| Claude | claude-consumer | https://www.anthropic.com/claude | ["Claude Free", "Claude Pro", "Claude Max", "Claude Team"] |
| Perplexity | perplexity-consumer | https://www.perplexity.ai/pro | ["Perplexity Free", "Perplexity Pro"] |
| Copilot | copilot-consumer | https://copilot.microsoft.com | ["Microsoft Copilot Free", "Copilot Pro", "Copilot for Microsoft 365"] |

**Adapter file pattern** -- each adapter:
- Imports `ConsumerAdapter` from `../base` (NOT `ProviderAdapter` from `../../base`)
- Imports `ProviderExtraction` from `../../base`
- Imports `consumerSubscriptionSchema` from `../schemas`
- Imports `getAIModel` from `../../../lib/ai-client`
- Imports its config from `./config`
- Extends `ConsumerAdapter`
- Sets `config` to its config object
- Implements `extract(html: string): Promise<ProviderExtraction>`:
  - Truncates HTML to 100K chars (same as OpenAI adapter)
  - Wraps the `generateObject()` call in a `Promise.race` with the adapter timeout from `this.config.adapterTimeoutMs`
  - Calls `generateObject()` with `consumerSubscriptionSchema` and **provider-specific prompt** (review #1)
  - After extraction, cross-checks plan names against `this.config.expectedPlanNames`. Plans not matching any expected name get `confidence: 'low_confidence'` and an extractionNotes entry
  - Maps rawPriceText from the extraction output (new field from Plan 01)
  - Maps billingPeriod from the extraction output (new field from Plan 01)
  - Returns `{ models: [], promotions: [], subscriptionPlans: object.plans.map(...) }`
  - On timeout or error: returns `{ models: [], promotions: [], subscriptionPlans: [] }` with console.error logging (review #6: graceful failure)

**Provider-specific prompt template** -- each adapter gets a tailored prompt that includes:
- The provider name and known plan names as extraction hints
- Instructions to capture rawPriceText alongside normalized prices
- Instructions to determine billingPeriod (monthly/annual/one_time/unknown)
- Instructions to set confidence based on extraction clarity
- The standard JSON structure with all fields from consumerSubscriptionSchema
- Explicit instruction: "If a price says 'contact sales', 'included with X', or is ambiguous, set the numeric price field to null and capture the text in rawPriceText"

Example for ChatGPT adapter:
```
Extract all consumer subscription plans from this ChatGPT pricing page.
Known plans to look for: ChatGPT Free, ChatGPT Plus, ChatGPT Pro.

For each plan, extract:
- planName: exact name as shown on page
- monthlyPrice: numeric USD monthly price, or null if not available
- annualPrice: numeric USD annual total, or null if not available
- annualMonthlyPrice: effective monthly when billed annually, or null
- rawPriceText: the exact price text as shown (e.g., "$20/mo")
- billingPeriod: "monthly" if billed monthly, "annual" if billed annually, "one_time" if one-time, "unknown" if unclear
- freeTrialDays: integer, 0 if no trial
- freeTrialConditions: text conditions or null
- keyFeatures: array of feature strings

Return { "plans": [...] } with all fields.
If a price is ambiguous or says "contact sales", set numeric fields to null and capture text in rawPriceText.

HTML content:
${truncatedHtml}
```

**After creating Tier 1 adapters**, update `src/providers/consumer/registry.ts` to import and register Tier 1 adapters at the bottom of the file (same pattern as `src/providers/registry.ts` lines 199-219):
```
// Tier 1 consumer providers
registerConsumerAdapter(new ChatGPTConsumerAdapter());
registerConsumerAdapter(new GeminiConsumerAdapter());
registerConsumerAdapter(new ClaudeConsumerAdapter());
registerConsumerAdapter(new PerplexityConsumerAdapter());
registerConsumerAdapter(new CopilotConsumerAdapter());
```

Task 1b will add Tier 2 registrations.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `find src/providers/consumer -maxdepth 2 -name 'adapter.ts' | wc -l` returns 5
    - `find src/providers/consumer -maxdepth 2 -name 'config.ts' | wc -l` returns 5
    - `grep -r 'extends ConsumerAdapter' src/providers/consumer/chatgpt src/providers/consumer/gemini src/providers/consumer/claude src/providers/consumer/perplexity src/providers/consumer/copilot | wc -l` returns 5
    - `grep -r 'expectedPlanNames' src/providers/consumer/chatgpt/config.ts` returns at least 1 (review #1)
    - `grep -r 'adapterTimeoutMs' src/providers/consumer/chatgpt/config.ts` returns at least 1 (review #6)
    - `grep -r 'rawPriceText' src/providers/consumer/chatgpt/adapter.ts` returns at least 1 (review #2)
    - `grep -r 'Promise.race' src/providers/consumer/chatgpt/adapter.ts` returns at least 1 (review #6 timeout)
    - `grep -c 'registerConsumerAdapter' src/providers/consumer/registry.ts` returns at least 5
    - TypeScript compiles without errors: `npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>5 Tier 1 consumer adapters created with config + adapter files; each has expectedPlanNames, adapterTimeoutMs, provider-specific prompt, rawPriceText/billingPeriod mapping; all registered in consumer registry.</done>
</task>

<task type="auto">
  <name>Task 1b: Create Tier 2 consumer adapters (Poe, Grok, You.com, Phind, Cursor)</name>
  <files>
    src/providers/consumer/poe/config.ts,
    src/providers/consumer/poe/adapter.ts,
    src/providers/consumer/grok/config.ts,
    src/providers/consumer/grok/adapter.ts,
    src/providers/consumer/you/config.ts,
    src/providers/consumer/you/adapter.ts,
    src/providers/consumer/phind/config.ts,
    src/providers/consumer/phind/adapter.ts,
    src/providers/consumer/cursor/config.ts,
    src/providers/consumer/cursor/adapter.ts,
    src/providers/consumer/registry.ts
  </files>
  <read_first>
    - src/providers/consumer/chatgpt/adapter.ts (Tier 1 adapter pattern from Task 1a to follow exactly)
    - src/providers/consumer/chatgpt/config.ts (Tier 1 config pattern from Task 1a with expectedPlanNames and adapterTimeoutMs)
    - src/providers/consumer/registry.ts (current registrations from Task 1a)
  </read_first>
  <action>
Create 5 Tier 2 consumer adapters following the exact same pattern as the Tier 1 adapters from Task 1a. Each adapter has two files: `config.ts` and `adapter.ts`.

**Tier 2 provider configs (per D-01):**
| Provider | name | pricingUrl | tier | expectedPlanNames |
|----------|------|-----------|------|-------------------|
| Poe | poe-consumer | https://poe.com/subscribe | tier2 | ["Poe Free", "Poe Subscription"] |
| Grok | grok-consumer | https://x.com/i/premium | tier2 | ["X Premium", "X Premium+", "Grok"] |
| You.com | you-consumer | https://you.com/pro | tier2 | ["You.com Free", "YouPro"] |
| Phind | phind-consumer | https://www.phind.com/pro | tier2 | ["Phind Free", "Phind Pro"] |
| Cursor | cursor-consumer | https://www.cursor.com/pricing | tier2 | ["Cursor Free", "Cursor Pro", "Cursor Business"] |

Follow the exact same adapter pattern as `src/providers/consumer/chatgpt/adapter.ts` from Task 1a. The only differences are:
- Config `name` matches the table above
- Config `tier` is `'tier2'` instead of `'tier1'`
- Config `pricingUrl` matches the table above
- Config `expectedPlanNames` matches the table above
- Config `adapterTimeoutMs` is 45000 (45s) for Tier 2 -- slightly longer because Tier 2 pages may be less optimized
- Adapter class name matches the provider (e.g., `PoeConsumerAdapter`)
- Extraction prompt is adapted for each provider's subscription page structure

**After creating Tier 2 adapters**, append to `src/providers/consumer/registry.ts`:
```
// Tier 2 consumer providers
registerConsumerAdapter(new PoeConsumerAdapter());
registerConsumerAdapter(new GrokConsumerAdapter());
registerConsumerAdapter(new YouConsumerAdapter());
registerConsumerAdapter(new PhindConsumerAdapter());
registerConsumerAdapter(new CursorConsumerAdapter());
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `find src/providers/consumer -maxdepth 2 -name 'adapter.ts' | wc -l` returns 10
    - `find src/providers/consumer -maxdepth 2 -name 'config.ts' | wc -l` returns 10
    - `grep -r 'extends ConsumerAdapter' src/providers/consumer/ | wc -l` returns 10
    - `grep -r 'expectedPlanNames' src/providers/consumer/ | wc -l` returns 10 (review #1)
    - `grep -r 'adapterTimeoutMs' src/providers/consumer/ | wc -l` returns 10 (review #6)
    - `grep -c 'registerConsumerAdapter' src/providers/consumer/registry.ts` returns at least 10
    - TypeScript compiles without errors: `npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>All 10 consumer adapters created (5 Tier 1 + 5 Tier 2); all have expectedPlanNames and adapterTimeoutMs; all registered in consumer registry.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate consumer adapters into pipeline with failure isolation and behavior tests</name>
  <files>src/pipeline/workers/extract.ts, src/pipeline/orchestrator.ts, tests/pipeline/subscription-pipeline.test.ts</files>
  <read_first>
    - src/pipeline/workers/extract.ts (current extract worker with promotion upsert pattern at lines 188-213)
    - src/pipeline/orchestrator.ts (current orchestrator with provider job enqueueing at lines 80-111)
    - src/providers/consumer/registry.ts (getAllConsumerAdapters, isConsumerTier1Provider, isConsumerTier2Provider)
    - src/db/schema.ts (subscriptionPlans table definition with all new fields from Plan 01)
    - src/providers/base.ts (ProviderExtraction with subscriptionPlans field)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-REVIEWS.md (findings #6, #9)
  </read_first>
  <action>
1. In `src/pipeline/workers/extract.ts`, add subscription plan upsert logic after the promotions upsert block (after line 213). Follow the exact same pattern as the promotions upsert (lines 188-213):

   - Import `subscriptionPlans` from `../../db/schema` (add to existing import on line 7)
   - Import `confidenceEnum` values or use string literals for confidence field
   - After the promotions block, check `normalized.subscriptionPlans && normalized.subscriptionPlans.length > 0`
   - For each plan, call `db.insert(subscriptionPlans).values({...}).onConflictDoUpdate({...})`
   - The insert values map ALL fields including the new review-added fields: `sourceId`, `providerName` (from job.data.providerName), `planName`, `monthlyPrice`, `annualPrice`, `annualMonthlyPrice`, `rawPriceText`, `billingPeriod`, `freeTrialDays`, `freeTrialConditions`, `keyFeatures`, `currency` (default 'USD'), `confidence`, `extractionNotes`, `sourceUrl`, `crawledAt: new Date()`, `updatedAt: new Date()`
   - The `onConflictDoUpdate` target is `[subscriptionPlans.sourceId, subscriptionPlans.planName]`
   - The `set` clause updates ALL mutable fields: `monthlyPrice`, `annualPrice`, `annualMonthlyPrice`, `rawPriceText`, `billingPeriod`, `freeTrialDays`, `freeTrialConditions`, `keyFeatures`, `confidence`, `extractionNotes`, `sourceUrl`, `crawledAt`, `updatedAt`
   - Wrap the subscription plan upsert loop in try/catch per plan (not per batch). On individual plan upsert failure: log error with plan name, continue to next plan. This ensures one malformed plan does not prevent other plans from being saved.

2. In `src/pipeline/orchestrator.ts`, add consumer adapter job enqueueing after the existing API adapter loop (after line 104). Addresses review finding #6 (failure isolation):

   - Import `getAllConsumerAdapters`, `isConsumerTier1Provider`, `isConsumerTier2Provider` from `../providers/consumer/registry`
   - After the API adapter loop, get consumer adapters: `const consumerAdapters = getAllConsumerAdapters()`
   - Loop through consumer adapters and enqueue collect jobs with failure isolation:
     ```
     for (const adapter of consumerAdapters) {
       try {
         const isT1 = isConsumerTier1Provider(adapter.config.name);
         const isT2 = isConsumerTier2Provider(adapter.config.name);
         const priority = isT1 ? 5 : isT2 ? 6 : 10;
         await collectQueue.add('collect', {
           providerName: adapter.config.name,
           pipelineRunId: runId,
           isTier1: false,
           isTier2: false,
           isTier3: false,
         }, {
           jobId: `collect-${adapter.config.name}-${Date.now()}`,
           priority,
         });
       } catch (error) {
         // Review #6: Failure isolation -- one adapter enqueue failure does not fail the whole run
         console.error(`Failed to enqueue consumer adapter ${adapter.config.name}:`, error);
         stats.consumerFailures = (stats.consumerFailures || 0) + 1;
       }
     }
     ```
   - Update the console.log (line 106-109) to include consumer adapter count: add `+${consumerAdapters.length} consumer` to the log message
   - Update `initialStats` to include: `consumerProvidersCount` (total), `consumerFailures` (enqueue failures), `consumerSuccesses` (enqueue successes)
   - Add `'consumerProvidersCount'`, `'consumerFailures'`, `'consumerSuccesses'` to the `VALID_STATS_KEYS` set (line 127)
   - After the loop, update `consumerSuccesses` = consumerAdapters.length - consumerFailures

3. **Pipeline integration -- lazy init pattern** (same approach as original plan, no change):

   In `src/providers/consumer/registry.ts`, add a `mirrorToMainRegistry()` function that uses a lazy import to avoid circular dependency:
   ```
   export async function mirrorToMainRegistry(): Promise<void> {
     const { registerAdapter } = await import('../registry');
     for (const adapter of getAllConsumerAdapters()) {
       registerAdapter(adapter);
     }
   }
   ```

   In `src/pipeline/orchestrator.ts`, call `mirrorToMainRegistry()` once at the start of the `runPipeline()` function (before the adapter loops), guarded by a module-level flag:
   ```
   import { mirrorToMainRegistry } from '../providers/consumer/registry';
   let consumerMirrored = false;
   if (!consumerMirrored) {
     await mirrorToMainRegistry();
     consumerMirrored = true;
   }
   ```

4. Create `tests/pipeline/subscription-pipeline.test.ts` with pipeline behavior tests (review finding #9):

   ```
   describe('Subscription Pipeline Behavior', () => {
     describe('subscription plan upsert', () => {
       it('should insert a new subscription plan') // insert plan, verify row exists
       it('should update existing plan on conflict') // insert same (sourceId, planName), verify updated
       it('should handle null prices gracefully') // insert plan with null monthlyPrice, verify stored as null
       it('should store rawPriceText alongside numeric price') // insert plan with rawPriceText, verify stored
       it('should store confidence level') // insert plan with confidence, verify stored
     })

     describe('free_trial projection', () => {
       it('should have free_trial as valid promotionTypeEnum value') // verify enum contains free_trial
       it('subscription plans with freeTrialDays > 0 are eligible for /promotions display') // conceptual test
     })

     describe('adapter failure isolation', () => {
       it('should not throw when consumer adapter enqueue fails') // mock collectQueue.add to throw, verify no throw from orchestrator
       it('should track consumer failures in stats') // verify stats.consumerFailures incremented on failure
     })
   })
   ```
   Note: Some tests are conceptual (they verify schema/enum state rather than running the full pipeline). The upsert tests can use a test database or mock. The failure isolation tests mock the collect queue.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/pipeline/workers/extract.ts | grep -c 'subscriptionPlans'` returns at least 1
    - `grep -v '^#' src/pipeline/workers/extract.ts | grep -c 'onConflictDoUpdate'` returns at least 3 (extractions, promotions, subscriptionPlans)
    - `grep -v '^#' src/pipeline/workers/extract.ts | grep -c 'rawPriceText'` returns at least 1 (review #2)
    - `grep -v '^#' src/pipeline/workers/extract.ts | grep -c 'confidence'` returns at least 1 (review #5)
    - `grep -v '^#' src/pipeline/orchestrator.ts | grep -c 'consumer'` returns at least 1
    - `grep -v '^#' src/pipeline/orchestrator.ts | grep -c 'getAllConsumerAdapters'` returns at least 1
    - `grep -v '^#' src/pipeline/orchestrator.ts | grep -c 'consumerFailures'` returns at least 1 (review #6)
    - `grep -v '^#' src/pipeline/orchestrator.ts | grep -c 'mirrorToMainRegistry'` returns at least 1
    - `grep -v '^#' src/providers/consumer/registry.ts | grep -c 'mirrorToMainRegistry'` returns at least 1
    - `test -f tests/pipeline/subscription-pipeline.test.ts && echo OK` (review #9)
    - TypeScript compiles without errors: `npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>Extract worker upserts subscription plans with all review-added fields (rawPriceText, billingPeriod, confidence, extractionNotes); orchestrator enqueues consumer adapter jobs with failure isolation and partial-success stats; consumer adapters registered in main registry via lazy mirror; pipeline behavior tests cover upsert, failure isolation, and free trial projection per review finding #9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer pricing page HTML -> AI extraction | Untrusted HTML fed to LLM for structured extraction |
| AI extraction output -> DB insert | Zod schema validates before database write; confidence field tracks extraction quality |
| Pipeline stats update | Atomic JSONB updates prevent race conditions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | AI extraction output | mitigate | consumerSubscriptionSchema validates all fields; invalid data rejected by Zod before DB insert; expectedPlanNames cross-checking flags unexpected plans |
| T-10-04 | Denial of Service | Crawlee rate limiting | mitigate | 24-hour crawl frequency for consumer pages; Crawlee built-in rate limiting; adapter-level timeout per review #6 |
| T-10-05 | Information Disclosure | subscription plan data | accept | Public pricing data, no PII; source URLs stored for transparency |
| T-10-08 | Denial of Service | Pipeline failure cascade | mitigate | Failure isolation per review #6: one adapter failure does not fail the whole daily run; try/catch per adapter enqueue |
</threat_model>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Consumer adapter count: `find src/providers/consumer -name 'adapter.ts' | wc -l` returns 10
- expectedPlanNames in all adapters: `grep -r 'expectedPlanNames' src/providers/consumer/ | wc -l` returns 10
- Pipeline integration: `grep -c 'subscriptionPlans' src/pipeline/workers/extract.ts` returns >= 1
- Orchestrator integration: `grep -c 'getAllConsumerAdapters' src/pipeline/orchestrator.ts` returns >= 1
- Failure isolation: `grep -c 'consumerFailures' src/pipeline/orchestrator.ts` returns >= 1
- Pipeline tests exist: `test -f tests/pipeline/subscription-pipeline.test.ts && echo OK`
</verification>

<success_criteria>
- 10 consumer adapters created (5 Tier 1 + 5 Tier 2) per D-01
- Each adapter has expectedPlanNames for extraction cross-checking (review #1)
- Each adapter has adapterTimeoutMs for timeout control (review #6)
- Each adapter uses provider-specific extraction prompt with rawPriceText and billingPeriod instructions
- Extract worker upserts subscription plans with all fields including confidence and extractionNotes
- Orchestrator enqueues consumer adapter collect jobs with failure isolation (review #6)
- Pipeline behavior tests exist for upsert, failure isolation, and free trial projection (review #9)
- Consumer adapters discoverable via main adapter registry via lazy mirrorToMainRegistry()
</success_criteria>

<output>
Create `.planning/phases/10-consumer-pricing-subscription-intelligence/10-02-SUMMARY.md` when done
</output>
