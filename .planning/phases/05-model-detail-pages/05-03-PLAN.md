---
phase: 05-model-detail-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/components/ModelDetailClient.tsx
  - app/components/PricingGrid.tsx
  - app/components/PriceHistoryChart.tsx
  - app/components/PromotionsList.tsx
  - app/components/ProviderLinks.tsx
autonomous: true
requirements:
  - MDTL-01
  - MDTL-02
  - MDTL-03
  - MDTL-04
  - MDTL-05

must_haves:
  truths:
    - "Model detail page shows current pricing in 3 cards (input, output, context window) with currency toggle"
    - "Price history chart renders dual lines (input + output) using Recharts"
    - "Specifications section displays context window, model family, and release date"
    - "Promotions section shows active vs expired status with visual cues"
    - "Provider links section shows docs, API, and playground links"
    - "Hero section shows model name, provider badge, confidence, and Back to pricing link"
  artifacts:
    - path: "app/components/ModelDetailClient.tsx"
      provides: "Client wrapper with currency state and section layout"
      contains: "useState"
    - path: "app/components/PricingGrid.tsx"
      provides: "3-card pricing display with currency toggle"
      contains: "formatCurrencyPrice"
    - path: "app/components/PriceHistoryChart.tsx"
      provides: "Recharts dual-line chart for price history"
      contains: "LineChart"
    - path: "app/components/PromotionsList.tsx"
      provides: "Active/expired promotions display"
      contains: "promotions"
    - path: "app/components/ProviderLinks.tsx"
      provides: "Provider docs/API/playground links"
      contains: "getProviderLinks"
  key_links:
    - from: "app/components/ModelDetailClient.tsx"
      to: "app/components/PricingGrid.tsx"
      via: "Props: pricing data, currency, exchangeRate"
      pattern: "PricingGrid"
    - from: "app/components/ModelDetailClient.tsx"
      to: "app/components/PriceHistoryChart.tsx"
      via: "Props: history data array"
      pattern: "PriceHistoryChart"
    - from: "app/components/PriceHistoryChart.tsx"
      to: "recharts"
      via: "LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer"
      pattern: "from.*recharts"
    - from: "app/components/ProviderLinks.tsx"
      to: "app/lib/provider-links.ts"
      via: "getProviderLinks function"
      pattern: "getProviderLinks"
---

<objective>
Build all client-side components for the model detail page.

Purpose: Create the client component layer that renders model pricing, price history chart, specifications, promotions, and provider links. These components receive data from the server page (Plan 02) via props and handle all interactive behavior (currency toggle, chart rendering, responsive layout). Follows the established pattern: server fetches, client renders.

Output:
- app/components/ModelDetailClient.tsx (main client wrapper)
- app/components/PricingGrid.tsx (pricing cards)
- app/components/PriceHistoryChart.tsx (Recharts chart)
- app/components/PromotionsList.tsx (promotions display)
- app/components/ProviderLinks.tsx (provider links)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-model-detail-pages/05-RESEARCH.md
@.planning/phases/05-model-detail-pages/05-CONTEXT.md
@.planning/phases/05-model-detail-pages/05-01-SUMMARY.md
@app/components/HomePageClient.tsx
@app/components/PricingTable.tsx
@app/lib/pricing-utils.ts
@app/lib/provider-links.ts
@app/lib/provider-metadata.ts
@app/globals.css
</context>

<interfaces>
<!-- Reusable functions from existing codebase -->

From app/lib/pricing-utils.ts:
```typescript
export function formatPrice(price: number | null | undefined): string;
export function formatCurrencyPrice(price: number | null | undefined, currency: 'usd' | 'vnd', rate?: number): string;
export function formatContextWindow(tokens: number | null | undefined): string;
export function sanitizeDisplayName(name: string, maxLength?: number): string;
export function getConfidenceColor(confidence: string): string;
export function getModelFamily(modelName: string): string;
export function convertToVND(price: number | null | undefined, rate?: number): number | null;
export function formatVND(priceInVND: number | null | undefined): string;
```

From app/lib/provider-links.ts (created by Plan 01):
```typescript
export function getProviderLinks(providerName: string): { docs: string; api: string; playground: string | null } | null;
```

From app/lib/provider-metadata.ts:
```typescript
export function getProviderLogo(providerName: string): string | null;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PriceHistoryChart, PricingGrid, PromotionsList, ProviderLinks components</name>
  <files>app/components/PriceHistoryChart.tsx, app/components/PricingGrid.tsx, app/components/PromotionsList.tsx, app/components/ProviderLinks.tsx</files>
  <behavior>
    - PriceHistoryChart renders a Recharts LineChart with dual lines (input + output)
    - PriceHistoryChart shows empty state when fewer than 2 data points
    - PriceHistoryChart uses ResponsiveContainer for responsive sizing
    - PricingGrid renders 3 cards: Input $/1M, Output $/1M, Context Window
    - PricingGrid formats prices using formatCurrencyPrice with currency toggle
    - PromotionsList shows active promotions with green styling and expired with gray
    - PromotionsList shows empty state when no promotions exist
    - ProviderLinks renders clickable links for docs, API, playground
    - ProviderLinks opens links in new tabs with rel="noopener noreferrer"
  </behavior>
  <action>
    Create 4 client components. All must have 'use client' directive at the top.

    === PriceHistoryChart.tsx ===
    Per D-07, D-14: Dual-line Recharts chart with input and output prices over time.

    Props: { data: HistoryPoint[] } where HistoryPoint = { collectedAt: Date; inputPricePer1m: number | null; outputPricePer1m: number | null }

    Implementation:
    - 'use client' directive (Recharts requires browser APIs)
    - Import: LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer from 'recharts'
    - Import: format from 'date-fns'
    - Transform data: filter out null prices, map to { date: format(collectedAt, 'MMM d'), input, output }
    - If chartData.length < 2, render empty state: "Price history will appear after multiple data collections."
    - Otherwise render ResponsiveContainer (width="100%", height={300}) containing LineChart
    - XAxis dataKey="date", YAxis, CartesianGrid (strokeDasharray="3 3"), Tooltip, Legend
    - Two Line components:
      - Input: dataKey="input", name="Input $/1M", stroke="#3b82f6" (blue), strokeWidth=2, dot={{ r: 4 }}
      - Output: dataKey="output", name="Output $/1M", stroke="#ef4444" (red), strokeWidth=2, dot={{ r: 4 }}
    - IMPORTANT: Do NOT place ResponsiveContainer inside a flex container without explicit dimensions

    === PricingGrid.tsx ===
    Per D-13: 3 cards with currency toggle.

    Props: { inputPrice: number | null; outputPrice: number | null; contextWindow: number | null; currency: 'usd' | 'vnd'; exchangeRate: number }

    Implementation:
    - 3 cards in a grid (grid grid-cols-1 md:grid-cols-3 gap-4)
    - Each card: bg-white border rounded-lg p-6 text-center (or use existing Tailwind theme classes)
    - Card 1: "Input $/1M" — formatCurrencyPrice(inputPrice, currency, exchangeRate)
    - Card 2: "Output $/1M" — formatCurrencyPrice(outputPrice, currency, exchangeRate)
    - Card 3: "Context Window" — formatContextWindow(contextWindow)
    - Each card has a label (text-sm text-gray-500) and value (text-2xl font-bold)
    - Use formatCurrencyPrice and formatContextWindow from pricing-utils

    === PromotionsList.tsx ===
    Per D-09: Display active vs expired promotions with visual cues.

    Props: { promotions: Array<{ id: number; modelPattern: string; type: string; description: string; credits: string | null; startDate: Date | null; endDate: Date | null; sourceUrl: string | null }> }

    Implementation:
    - If promotions.length === 0, show empty state: "No active promotions or free tier offers."
    - For each promotion, determine active vs expired:
      - Active: endDate is null OR endDate > now
      - Expired: endDate <= now
    - Active promotions: green accent (border-l-4 border-green-500, bg-green-50)
    - Expired promotions: gray accent (border-l-4 border-gray-300, bg-gray-50, opacity-75)
    - Each promotion shows: type badge, description, credits (if any), date range, source link
    - Type badge: free_tier = green, promotion = blue, beta = purple
    - Date format: "MMM d, yyyy" using date-fns format

    === ProviderLinks.tsx ===
    Per MDTL-05: Links to provider docs, API, playground.

    Props: { providerName: string; sourceUrl: string | null }

    Implementation:
    - Call getProviderLinks(providerName) to get links
    - Render a list of links: Docs, API, Playground (if available)
    - Also include "Pricing Page" link using sourceUrl prop
    - Each link: <a href={url} target="_blank" rel="noopener noreferrer" className="text-blue-600 hover:text-blue-800 underline">
    - If no provider links found, show just the source URL (if available)
    - Use Material Symbols or simple text labels for each link type
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -i "error\|Error" | head -10</automated>
  </verify>
  <done>All 4 components compile without TypeScript errors. PriceHistoryChart uses Recharts with dual lines. PricingGrid shows 3 formatted cards. PromotionsList distinguishes active/expired. ProviderLinks renders clickable links. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create ModelDetailClient wrapper component</name>
  <files>app/components/ModelDetailClient.tsx</files>
  <action>
    Create app/components/ModelDetailClient.tsx as the main client wrapper.

    Per D-11: Full detail page with sections: Hero -> Pricing grid -> Price history chart -> Specifications -> Promotions -> Provider links -> Digest mentions.
    Per D-12: Hero section with large model name, provider badge, confidence indicator, "Back to pricing" link.
    Per D-13: Pricing grid with 3 cards and currency toggle.

    Props (exported interface for Plan 02 to import):
    ```
    export interface ModelDetailData {
      id: number;
      modelName: string;
      inputPricePer1m: number | null;
      outputPricePer1m: number | null;
      contextWindow: number | null;
      confidence: 'verified' | 'likely' | 'low_confidence';
      collectedAt: Date;
      sourceName: string | null;
      sourceUrl: string | null;
      sourceId: number;
    }

    export interface HistoryPoint {
      collectedAt: Date;
      inputPricePer1m: number | null;
      outputPricePer1m: number | null;
    }

    export interface PromotionData {
      id: number;
      modelPattern: string;
      type: 'free_tier' | 'promotion' | 'beta';
      description: string;
      credits: string | null;
      startDate: Date | null;
      endDate: Date | null;
      sourceUrl: string | null;
    }
    ```

    Component: `export function ModelDetailClient({ model, history, promotions, exchangeRate }: { model: ModelDetailData; history: HistoryPoint[]; promotions: PromotionData[]; exchangeRate: number })`

    State: `const [currency, setCurrency] = useState<'usd' | 'vnd'>('usd');`

    Layout (top to bottom):

    1. **Hero Section** (D-12):
       - "Back to pricing" link: <Link href="/"> with left arrow icon
       - Large model name: sanitizeDisplayName(model.modelName) in text-3xl font-bold
       - Provider badge: model.sourceName with ProviderLogo (import from provider-metadata)
       - Confidence badge: using getConfidenceColor(model.confidence)
       - Collected date: format(model.collectedAt, 'MMM d, yyyy h:mm a')

    2. **Pricing Grid** (D-13):
       - Currency toggle (USD/VND buttons, same pattern as HomePageClient)
       - <PricingGrid inputPrice={model.inputPricePer1m} outputPrice={model.outputPricePer1m} contextWindow={model.contextWindow} currency={currency} exchangeRate={exchangeRate} />

    3. **Price History Chart** (D-14):
       - Section heading: "Price History"
       - <PriceHistoryChart data={history} />

    4. **Specifications** (MDTL-03):
       - Context Window: formatContextWindow(model.contextWindow)
       - Model Family: getModelFamily(model.modelName)
       - Release Date: format(model.collectedAt, 'MMM d, yyyy') (first collection = approximate release)
       - Display as a definition list or key-value grid

    5. **Promotions** (MDTL-04):
       - Section heading: "Promotions & Free Tier"
       - <PromotionsList promotions={promotions} />

    6. **Provider Links** (MDTL-05):
       - Section heading: "Provider Resources"
       - <ProviderLinks providerName={model.sourceName ?? ''} sourceUrl={model.sourceUrl} />

    7. **Digest Mentions** (placeholder until Phase 6):
       - Section heading: "Daily Digest Mentions"
       - Placeholder text: "Daily digest mentions will appear here once the content engine is active."

    Styling:
    - Use Tailwind classes consistent with existing components
    - max-w-4xl mx-auto px-4 for content width
    - Section headings: text-xl font-semibold mb-4
    - Sections separated by border-b py-8

    Imports:
    - Link from 'next/link'
    - useState from 'react'
    - format from 'date-fns'
    - sanitizeDisplayName, formatContextWindow, getModelFamily, getConfidenceColor, formatCurrencyPrice from pricing-utils
    - getProviderLogo from provider-metadata
    - PricingGrid, PriceHistoryChart, PromotionsList, ProviderLinks (local components)
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -i "error\|Error" | head -10</automated>
  </verify>
  <done>ModelDetailClient renders all 7 sections per D-11. Hero has model name, provider badge, confidence, back link. Currency toggle works. All child components imported and rendered. Build succeeds with zero TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server props -> Client components | Data from DB is trusted; no user input reaches components |
| External links | Provider links are from a static map, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | External link URLs | accept | URLs point to public provider docs, no sensitive data |
| T-05-07 | Tampering | XSS via model names | mitigate | sanitizeDisplayName strips bidi overrides and control chars (existing utility) |
| T-05-SC | Tampering | recharts install | accept | recharts@3.8.1 verified in RESEARCH.md Package Legitimacy Audit (11 years old, 3M+ weekly downloads) |
</threat_model>

<verification>
1. Install recharts: `pnpm add recharts`
2. `pnpm build` — all components compile without errors
3. `pnpm dev` and visit `/model/[valid-slug]` — all 7 sections render
4. Currency toggle switches between USD and VND
5. Price history chart shows dual lines (if data exists)
6. Promotions section shows active/expired styling
7. Provider links open in new tabs
</verification>

<success_criteria>
- All 5 client components compile and render without errors
- PriceHistoryChart uses Recharts with dual lines and tooltips
- PricingGrid shows 3 formatted cards with currency support
- PromotionsList distinguishes active vs expired promotions
- ProviderLinks renders clickable external links
- ModelDetailClient assembles all sections per D-11 layout
- Currency toggle synchronizes with PricingGrid
</success_criteria>

<output>
Create `.planning/phases/05-model-detail-pages/05-03-SUMMARY.md` when done
</output>
