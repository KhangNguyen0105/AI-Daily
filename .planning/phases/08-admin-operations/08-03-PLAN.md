---
phase: 08-admin-operations
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - app/admin/articles/page.tsx
  - app/components/admin/ConfirmDialog.tsx
  - app/components/admin/Toast.tsx
  - app/api/admin/articles/[id]/sources/route.ts
autonomous: true
requirements:
  - ADMN-01
user_setup: []
must_haves:
  truths:
    - "Admin can view a list of all published articles with date, title, status, and edit link"
    - "ConfirmDialog renders modal with title, message, cancel/confirm buttons, Escape to dismiss"
    - "Toast renders success/error notifications in bottom-right, auto-dismisses after 5s"
    - "Source evidence API returns extractions related to an article's date"
  artifacts:
    - path: "app/admin/articles/page.tsx"
      provides: "Articles list page with table showing date, title, status, edit link"
    - path: "app/components/admin/ConfirmDialog.tsx"
      provides: "Reusable confirmation modal for destructive actions"
    - path: "app/components/admin/Toast.tsx"
      provides: "Toast notification component (success/error, auto-dismiss)"
    - path: "app/api/admin/articles/[id]/sources/route.ts"
      provides: "GET extractions and sources related to an article (by date)"
      exports: ["GET"]
  key_links:
    - from: "app/admin/articles/page.tsx"
      to: "src/db/schema"
      via: "Drizzle query for articles list"
      pattern: "db.select.*from.*articles"
    - from: "app/api/admin/articles/[id]/sources/route.ts"
      to: "src/db/schema"
      via: "Drizzle query for extractions by date"
      pattern: "db.select.*from.*extractions"
---

<objective>
Build the articles list page showing all published articles with date, title, status, and edit links. Also create the reusable ConfirmDialog and Toast components that will be used across all admin pages, and the source evidence API route for linking articles to their extraction data. Per D-01, D-03, D-07.

Purpose: The articles list is the entry point for content management. ConfirmDialog and Toast are shared components used by articles, pipeline, and sources pages. The source evidence API enables the "Sources" tab in the article edit page.

Output: Working articles list at /admin/articles, reusable ConfirmDialog and Toast components, source evidence API route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-admin-operations/08-CONTEXT.md
@.planning/phases/08-admin-operations/08-UI-SPEC.md
@.planning/phases/08-admin-operations/08-PATTERNS.md
@src/db/schema.ts
@src/db/index.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/db/schema.ts:
- articles: id, date, title, summary, content, publishedAt, createdAt, updatedAt
- extractions: id, modelName, inputPricePer1m, outputPricePer1m, confidence, sourceName, sourceUrl, collectedAt
- sources: id, name, url, providerType, isActive, createdAt, updatedAt

From 08-UI-SPEC.md (copywriting):
- Empty state articles: "No articles yet" / "Articles will appear here once the daily content engine publishes its first digest."
- Error generic: "{Action} failed. {What went wrong}. {What to try}."

From PATTERNS.md:
- Pattern 11: Toast Hook - useToast returns { toasts, addToast, removeToast }
- Pattern 17: Table Header Styling - text-xs font-semibold uppercase tracking-wide text-gray-500
- Pattern 18: Empty State - centered text with primary/secondary lines
- Pattern 19: Badge Styling - green for Published, gray for Draft
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConfirmDialog and Toast reusable components</name>
  <files>
    app/components/admin/ConfirmDialog.tsx,
    app/components/admin/Toast.tsx
  </files>
  <read_first>
    app/globals.css,
    .planning/phases/08-admin-operations/08-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: ConfirmDialog renders modal overlay with title, message, cancel/confirm buttons
    - Test: ConfirmDialog calls onConfirm when confirm button clicked
    - Test: ConfirmDialog calls onCancel when Escape key pressed
    - Test: ConfirmDialog calls onCancel when backdrop clicked
    - Test: ConfirmDialog danger variant uses red confirm button
    - Test: useToast hook returns toasts array, addToast function, removeToast function
    - Test: addToast("success", "message") adds green-bordered toast
    - Test: addToast("error", "message") adds red-bordered toast
    - Test: Toast auto-dismisses after 5 seconds
    - Test: ToastContainer renders max 3 toasts
  </behavior>
  <action>
  1. Create `app/components/admin/Toast.tsx` (per Pattern 11 from PATTERNS.md):
     - `'use client'` directive
     - Export `ToastContainer` component and `useToast` hook
     - `useToast` returns `{ toasts, addToast, removeToast }`. State: array of `{ id: string; type: "success" | "error"; message: string }`.
     - `addToast(type, message)` adds a toast with a unique ID. Auto-remove after 5 seconds via setTimeout.
     - `ToastContainer` renders toasts in fixed bottom-right position (`fixed bottom-6 right-6 z-50 flex flex-col gap-2`):
       - Each toast: `flex items-center gap-3 px-4 py-3 bg-white rounded-lg shadow-lg border-l-4`. Left border: `border-green-500` for success, `border-red-500` for error.
       - Message text: `text-sm text-gray-700`
       - Dismiss button: X icon
     - Max 3 visible at once (slice array to 3)

  2. Create `app/components/admin/ConfirmDialog.tsx`:
     - `'use client'` directive
     - Props: `isOpen: boolean; title: string; message: string; confirmLabel: string; onConfirm: () => void; onCancel: () => void; variant?: "danger" | "default"`
     - Render when isOpen:
       - Overlay: `fixed inset-0 bg-black/50 z-50 flex items-center justify-center`. Click overlay calls onCancel.
       - Card: `bg-white rounded-lg p-6 max-w-md w-full mx-4 shadow-xl`
       - Title: `text-lg font-semibold text-gray-900 mb-2`
       - Message: `text-sm text-gray-600 mb-6`
       - Buttons: Cancel (`px-4 py-2 text-sm border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50`), Confirm (`px-4 py-2 text-sm rounded-md text-white`). Danger: `bg-red-600 hover:bg-red-700`. Default: `bg-blue-600 hover:bg-blue-700`.
     - Handle Escape key via useEffect keydown listener
     - Focus trap: auto-focus confirm button on open via useRef + useEffect
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ConfirmDialog renders modal with title, message, cancel/confirm buttons, Escape to dismiss
    - ConfirmDialog danger variant uses red confirm button
    - Toast renders success/error notifications in bottom-right, auto-dismisses after 5s
    - useToast hook manages toast state with addToast and removeToast
    - Max 3 toasts visible at once
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    - ConfirmDialog renders modal with title, message, cancel/confirm buttons, Escape to dismiss
    - ConfirmDialog danger variant uses red confirm button
    - Toast renders success/error notifications in bottom-right, auto-dismisses after 5s
    - useToast hook manages toast state with addToast and removeToast
    - Max 3 toasts visible at once
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create articles list page and source evidence API</name>
  <files>
    app/admin/articles/page.tsx,
    app/api/admin/articles/[id]/sources/route.ts
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/index.ts,
    src/auth.ts,
    .planning/phases/08-admin-operations/08-PATTERNS.md
  </read_first>
  <behavior>
    - Test: /admin/articles shows list of all articles ordered by date desc
    - Test: Each article row shows date, title (truncated to 60 chars), status badge, edit link
    - Test: Published articles show green "Published" badge
    - Test: Draft articles show gray "Draft" badge
    - Test: Edit link points to /admin/articles/[id]/edit
    - Test: Empty state shows "No articles yet" message
    - Test: Source evidence API returns extractions for a given article's date
    - Test: Source evidence API includes source name, URL, prices, confidence
  </behavior>
  <action>
  1. Create `app/admin/articles/page.tsx` (per Pattern 7 from PATTERNS.md):
     - Server component (async function)
     - Import db, articles, desc from drizzle-orm, format from date-fns, Link from next/link
     - Export `const dynamic = 'force-dynamic'`
     - Query: db.select({ id, date, title, publishedAt }).from(articles).orderBy(desc(articles.date))
     - Wrap in try/catch with fallback to empty array
     - Render:
       - Page heading: "Articles" (text-2xl font-bold text-gray-900)
       - Table with columns: Date, Title, Status, Edit
       - Date: format(new Date(article.date), 'MMM d, yyyy')
       - Title: truncated to 60 chars with "..." if longer
       - Status: "Published" badge (bg-green-100 text-green-800) if publishedAt not null, "Draft" badge (bg-gray-100 text-gray-600) otherwise
       - Edit: Link to /admin/articles/${article.id}/edit (text-sm text-blue-600 hover:text-blue-800)
       - Empty state: "No articles yet" / "Articles will appear here once the daily content engine publishes its first digest."
     - Use plain HTML table (not TanStack — simple list, no sorting/filtering needed)

  2. Create `app/api/admin/articles/[id]/sources/route.ts`:
     - Import db, articles, extractions, sources, eq, and, gte, lt, desc from drizzle-orm
     - Import auth from @/src/auth, NextResponse from next/server
     - GET handler:
       - Auth check: const session = await auth(); if (!session) return 401
       - Extract id from params
       - Fetch article to get its date field (YYYY-MM-DD string)
       - Parse date to get start-of-day and end-of-day timestamps
       - Query extractions collected on that date with source info:
         db.select({ id, modelName, inputPricePer1m, outputPricePer1m, confidence, sourceName, sourceUrl, collectedAt }).from(extractions).leftJoin(sources, ...).where(and(gte(collectedAt, startOfDay), lt(collectedAt, endOfDay))).orderBy(desc(collectedAt))
       - Return { extractions: [...] }
     - Export GET
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - /admin/articles shows list of all articles with date, title, status, edit link
    - Published articles show green "Published" badge, drafts show gray "Draft" badge
    - Edit link navigates to /admin/articles/[id]/edit
    - Empty state shows "No articles yet" message
    - Source evidence API returns extractions for a given article's date
    - API includes source name, URL, prices, confidence in response
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    - /admin/articles shows list of all articles with date, title, status, edit link
    - Published articles show green "Published" badge, drafts show gray "Draft" badge
    - Edit link navigates to /admin/articles/[id]/edit
    - Empty state shows "No articles yet" message
    - Source evidence API returns extractions for a given article's date
    - API includes source name, URL, prices, confidence in response
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->api | Source evidence API called from article edit page |
| api->db | API route reads extractions and sources tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | Articles list | accept | Article content is already published publicly. List is admin-only, protected by middleware. |
| T-08-08 | Information Disclosure | Source evidence API | accept | Extraction data is admin-only. Protected by middleware auth check. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes without errors
2. Navigate to /admin/articles — shows list of articles (or empty state)
3. Each article row shows date, title, status badge, edit link
4. ConfirmDialog and Toast components render correctly in isolation
5. Source evidence API returns extractions for a valid article date
</verification>

<success_criteria>
- Articles list page shows all articles with date, title, status, edit link
- Published/Draft status badges display correctly
- ConfirmDialog is reusable with title, message, confirm/cancel callbacks
- Toast notifications display success/error with auto-dismiss
- Source evidence API returns extractions by article date
- TypeScript compiles without errors
</success_criteria>

<artifacts>
## Artifacts This Phase Produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| `ConfirmDialog` | app/components/admin/ConfirmDialog.tsx | component | Reusable confirmation modal |
| `useToast` | app/components/admin/Toast.tsx | hook | Toast state management hook |
| `ToastContainer` | app/components/admin/Toast.tsx | component | Toast renderer (bottom-right, max 3) |
| `AdminArticlesPage` | app/admin/articles/page.tsx | page | Articles list with status badges |
| `GET /api/admin/articles/[id]/sources` | app/api/admin/articles/[id]/sources/route.ts | route | Source evidence by article date |
</artifacts>

<output>
Create `.planning/phases/08-admin-operations/08-03-SUMMARY.md` when done
</output>
