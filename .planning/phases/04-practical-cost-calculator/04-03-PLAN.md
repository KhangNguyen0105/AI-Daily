---
phase: 04-practical-cost-calculator
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/page.tsx
  - app/components/PricingTable.tsx
autonomous: true
requirements:
  - COST-05
  - COST-06

must_haves:
  truths:
    - "User sees the CostCalculator section below the PricingTable on the landing page"
    - "Currency toggle in PricingTable synchronizes with CostCalculator currency display"
    - "Both components show the same currency (USD or VND) at all times"
    - "The CostCalculator receives the same pricing data and exchange rate as PricingTable"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing page with PricingTable and CostCalculator sections, shared currency state"
      contains: "CostCalculator"
    - path: "app/components/PricingTable.tsx"
      provides: "PricingTable accepting currency and onCurrencyChange props from parent"
      contains: "onCurrencyChange"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/CostCalculator.tsx"
      via: "imports and renders CostCalculator with data, currency, exchangeRate props"
      pattern: "CostCalculator.*data.*currency"
    - from: "app/page.tsx"
      to: "app/components/PricingTable.tsx"
      via: "passes currency and onCurrencyChange props to PricingTable"
      pattern: "PricingTable.*currency.*onCurrencyChange"
    - from: "app/components/PricingTable.tsx"
      to: "app/page.tsx"
      via: "calls onCurrencyChange when toggle is clicked instead of managing own state"
      pattern: "onCurrencyChange"
---

<objective>
Integrate the CostCalculator into the landing page below the PricingTable and synchronize the USD/VND currency toggle between both components by lifting currency state to the page level.

Purpose: Per COST-05, users need side-by-side comparison on the same page. Per the research pitfall #3, currency toggle state must be shared between PricingTable and CostCalculator to avoid confusion. This plan wires everything together.

Output:
- app/page.tsx (updated with CostCalculator section and shared currency state)
- app/components/PricingTable.tsx (refactored to accept currency as prop)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/page.tsx
@app/components/PricingTable.tsx
@app/components/CostCalculator.tsx
@app/lib/pricing-utils.ts
</context>

<interfaces>
<!-- Key types the executor needs -->

From app/components/PricingTable.tsx (current props):
```typescript
export function PricingTable({ data, exchangeRate }: { data: PricingRow[]; exchangeRate?: number })
```

From app/components/CostCalculator.tsx (created in Plan 2):
```typescript
export function CostCalculator({ data, currency, exchangeRate }: { data: PricingRow[]; currency: 'usd' | 'vnd'; exchangeRate?: number })
```

From app/page.tsx (current server component structure):
```typescript
export const revalidate = 60;
export default async function HomePage() {
  // fetches pricingData from DB
  // fetches exchangeRate from getLatestExchangeRate()
  // renders <PricingTable data={pricingData} exchangeRate={exchangeRate} />
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor PricingTable to accept currency as a prop</name>
  <files>app/components/PricingTable.tsx</files>
  <read_first>
    - app/components/PricingTable.tsx (current component with internal currency state at line 158)
    - app/page.tsx (parent server component that renders PricingTable)
  </read_first>
  <action>
    Refactor app/components/PricingTable.tsx to accept currency from the parent instead of managing it internally:

    1. Update the component props interface from `{ data: PricingRow[]; exchangeRate?: number }` to `{ data: PricingRow[]; exchangeRate?: number; currency?: 'usd' | 'vnd'; onCurrencyChange?: (currency: 'usd' | 'vnd') => void }`.

    2. Remove the internal `const [currency, setCurrency] = useState<'usd' | 'vnd'>('usd')` state declaration (currently at line 158).

    3. Derive currency from props with a fallback: `const effectiveCurrency = currency ?? 'usd'`. Use `effectiveCurrency` everywhere the component currently uses `currency` (column headers, formatCurrencyPrice calls, column definitions useMemo dependency).

    4. Update the toggle button onClick handlers:
       - USD button: `onClick={() => onCurrencyChange?.('usd')}` (was `onClick={() => setCurrency('usd')}`)
       - VND button: `onClick={() => onCurrencyChange?.('vnd')}` (was `onClick={() => setCurrency('vnd')}`)

    5. Update the toggle button active state checks to use `effectiveCurrency` instead of `currency`:
       - USD button active: `effectiveCurrency === 'usd'`
       - VND button active: `effectiveCurrency === 'vnd'`

    6. Update the column definitions useMemo to use `effectiveCurrency` instead of `currency` for header text and cell renderers. Add `effectiveCurrency` to the useMemo dependency array (replacing `currency`).

    7. The component must remain backward-compatible: if `currency` and `onCurrencyChange` props are not provided, it falls back to 'usd' and the toggle buttons become no-ops (the optional chaining `?.` handles this).
  </action>
  <verify>
    <automated>pnpm vitest run tests/pricing-utils.test.ts && pnpm build 2>&1 | Select-String -Pattern "error|Error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - PricingTable accepts optional currency and onCurrencyChange props
    - When currency prop is 'vnd', all price columns display VND format
    - When currency prop is 'usd', all price columns display USD format
    - When onCurrencyChange is provided, toggle buttons call it with 'usd' or 'vnd'
    - When props are not provided, component defaults to 'usd' (backward compatible)
    - No internal useState for currency remains in PricingTable
    - Column headers reflect the effectiveCurrency value
    - All existing tests still pass
    - Build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>PricingTable accepts currency and onCurrencyChange as optional props. Internal currency state removed. Toggle buttons call onCurrencyChange. Backward-compatible when props omitted. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add CostCalculator section to landing page with shared currency state</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current server component with PricingTable rendering)
    - app/components/CostCalculator.tsx (created in Plan 2 -- props: data, currency, exchangeRate)
    - app/components/PricingTable.tsx (refactored in Task 1 -- props: data, exchangeRate, currency, onCurrencyChange)
  </read_first>
  <action>
    Update app/page.tsx to add the CostCalculator section and shared currency state:

    1. Since page.tsx is a server component and currency state is client-side, create a new client wrapper component in the same file (or a separate file). The cleanest approach: create a small client component `CostCalculatorSection` in app/components/CostCalculatorSection.tsx (or inline in page.tsx using a client boundary).

    Actually, the simpler approach: page.tsx is a server component that cannot use useState. Instead, create a thin client wrapper component that manages the shared currency state and renders both PricingTable and CostCalculator. This keeps page.tsx as a server component.

    Create `app/components/HomePageClient.tsx` as a 'use client' component:
    - Props: `{ data: PricingRow[]; exchangeRate: number; lastUpdated: Date | null }`
    - State: `const [currency, setCurrency] = useState<'usd' | 'vnd'>('usd')`
    - Renders: PricingTable with currency and onCurrencyChange props, then CostCalculator with currency prop
    - Import PricingTable, CostCalculator, PricingRow

    Update app/page.tsx:
    - Import HomePageClient from '@/app/components/HomePageClient'
    - Keep the server-side data fetching (pricingData, exchangeRate, lastUpdated)
    - Replace the PricingTable rendering with: `<HomePageClient data={pricingData} exchangeRate={exchangeRate} lastUpdated={lastUpdated} />`
    - Remove the direct PricingTable import (it's now used inside HomePageClient)
    - Keep the branding section (h1, last-updated) in the server component for SSR

    The HomePageClient component structure:
    ```
    <div className="max-w-6xl mx-auto px-4 pb-16">
      <h2>Latest Pricing Data</h2>
      <PricingTable data={data} exchangeRate={exchangeRate} currency={currency} onCurrencyChange={setCurrency} />
    </div>
    <div className="max-w-6xl mx-auto px-4 pb-16">
      <h2>What Does It Actually Cost?</h2>
      <p>See real-world costs for common AI use cases, ranked cheapest to most expensive.</p>
      <CostCalculator data={data} currency={currency} exchangeRate={exchangeRate} />
    </div>
    ```
  </action>
  <verify>
    <automated>pnpm build 2>&1 | Select-String -Pattern "error|Error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx imports and renders HomePageClient (or equivalent client wrapper)
    - Server-side data fetching (pricingData, exchangeRate) remains in page.tsx
    - PricingTable and CostCalculator share the same currency state
    - CostCalculator section appears below PricingTable on the landing page
    - Section header reads "What Does It Actually Cost?" or similar
    - Descriptive text explains the scenarios
    - Toggling currency in PricingTable updates CostCalculator display
    - Build succeeds with no TypeScript or Next.js errors
    - Page renders at localhost:3000 with both sections visible
  </acceptance_criteria>
  <done>CostCalculator section renders below PricingTable on the landing page. Currency state shared between both components via client wrapper. Server-side data fetching preserved. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> Client | page.tsx (server) passes data to HomePageClient (client). Data is read-only pricing info. |
| Currency state -> Display | Shared client-side state controls display format only. No mutation of pricing data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Currency state synchronization | accept | Client-side display toggle only. Prices stored in DB as USD. No security impact from currency display mismatch. |
| T-04-SC | Tampering | npm installs | accept | No new packages -- reuses existing React, Next.js patterns. |
</threat_model>

<verification>
1. `pnpm build` -- Next.js build succeeds with no errors
2. `pnpm vitest run` -- all existing tests pass
3. Visit localhost:3000 -- PricingTable section visible at top
4. Scroll down -- "What Does It Actually Cost?" section visible below PricingTable
5. CostCalculator shows scenario tabs and ranked model list
6. Click "VND" toggle in PricingTable -- CostCalculator also shows VND prices
7. Click "USD" toggle in CostCalculator area (via PricingTable) -- both revert to USD
8. Scenario tabs are clickable and update the ranked list
</verification>

<success_criteria>
- Landing page shows PricingTable followed by CostCalculator section
- Currency toggle in PricingTable synchronizes with CostCalculator display
- Both components show the same currency at all times
- CostCalculator receives pricing data and exchangeRate from server
- Scenario tabs work (click to switch scenarios)
- Ranked model list shows cheapest first with input/output breakdown
- Page renders correctly at localhost:3000
- Build compiles with zero errors
- All existing tests pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/04-practical-cost-calculator/04-03-SUMMARY.md` when done
</output>
