---
phase: 03-pricing-comparison-table
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - app/components/PricingTable.tsx
  - app/page.tsx
  - app/globals.css
autonomous: true
requirements:
  - PRIC-05
  - PRIC-06
  - FRNT-03
  - FRNT-04

must_haves:
  truths:
    - "Each pricing row links to its source URL with the provider's pricing page"
    - "Each row shows a last-updated timestamp (collectedAt) formatted as a human-readable date"
    - "The page displays 'Last updated: [date]' using the most recent collectedAt across all data"
    - "The table is usable on mobile browsers (horizontal scroll or responsive layout)"
    - "On mobile, key columns (Model, Input, Output) remain visible while less critical columns can scroll"
  artifacts:
    - path: "app/components/PricingTable.tsx"
      provides: "Table with source links, last-updated timestamps, responsive design"
      contains: "sourceUrl"
    - path: "app/page.tsx"
      provides: "Page with 'Last updated' banner above the table"
      contains: "Last updated"
  key_links:
    - from: "app/components/PricingTable.tsx"
      to: "sourceUrl (from JOIN query)"
      via: "anchor tag in Source column"
      pattern: "href.*sourceUrl"
    - from: "app/page.tsx"
      to: "lastUpdated prop"
      via: "formatted date string in header"
      pattern: "Last updated"
---

<objective>
Add source attribution, last-updated timestamps, and mobile responsiveness to the pricing table.

Purpose: Complete the Phase 3 success criteria by making data sources transparent (each row links to its source), showing data freshness (last-updated timestamps), and ensuring the table works on mobile devices.

Output:
- app/components/PricingTable.tsx (enhanced with source links, timestamps, responsive design)
- app/page.tsx (with "Last updated" banner)
- app/globals.css (responsive utility classes if needed)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pricing-comparison-table/03-RESEARCH.md
@app/components/PricingTable.tsx
@app/page.tsx
@app/lib/pricing-utils.ts
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source attribution column and last-updated timestamps</name>
  <files>app/components/PricingTable.tsx, app/page.tsx</files>
  <action>
    Update app/components/PricingTable.tsx:

    1. **Source column**: Add a "Source" column after the Confidence column. The cell renderer checks if sourceUrl is available:
       - If sourceUrl exists: render an anchor tag linking to sourceUrl with text "View source" or the sourceName. Link opens in new tab (target="_blank" rel="noopener noreferrer). Style as blue underline text (text-blue-600 hover:text-blue-800).
       - If sourceUrl is null: render "---" (em dash).
       - Column is sortable by sourceName.

    2. **Collected date column**: Add a "Collected" column showing the collectedAt date. Format using date-fns format() as "MMM d, yyyy" (e.g., "Jun 10, 2026"). Import format from date-fns. Column is sortable (sort by date). On smaller screens this column can be hidden.

    3. **Confidence badge tooltip**: Enhance the confidence badge to show a title attribute explaining the level:
       - verified: "Verified: Data confirmed by multiple sources or official documentation"
       - likely: "Likely: Data from a single reliable source"
       - low_confidence: "Low confidence: Data may be incomplete or unverified"

    Update app/page.tsx:

    4. **"Last updated" banner**: Add a banner between the "AI Daily" header and the pricing table showing "Last updated: {formatted date}". Use date-fns format() with the lastUpdated timestamp. If lastUpdated is null, show "Last updated: Unknown". Style as a subtle text line (text-sm text-gray-500) centered below the description.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | Select-String -Pattern "error|Error" | Select-Object -First 5</automated>
  </verify>
  <done>Source column shows clickable links to provider pricing pages. Collected column shows formatted dates. Confidence badges have explanatory tooltips. Page shows "Last updated: [date]" banner. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile responsive table layout</name>
  <files>app/components/PricingTable.tsx, app/globals.css</files>
  <action>
    Update app/components/PricingTable.tsx for mobile responsiveness:

    1. **Table wrapper**: Ensure the table is wrapped in `<div className="overflow-x-auto">` for horizontal scrolling on small screens. This is already present from Plan 01 — verify it works correctly.

    2. **Column visibility by breakpoint**: Use Tailwind responsive classes to hide less critical columns on smaller screens:
       - Provider, Model, Input, Output: always visible (core comparison data)
       - Family: hidden on mobile (< md), visible on md+
       - Context Window: hidden on mobile (< md), visible on md+
       - Confidence: always visible (safety indicator)
       - Source: hidden on mobile (< lg), visible on lg+
       - Collected: hidden on tablet and below (< xl), visible on xl+

       Apply via `<th className="hidden md:table-cell">` and `<td className="hidden md:table-cell">` pattern.

    3. **Filter bar responsiveness**: Make the filter bar stack vertically on mobile:
       - Desktop (md+): flex row with gap-4
       - Mobile (< md): flex col with gap-3
       - Search input: full width on all screens
       - Provider select + free tier checkbox: full width on mobile, auto on desktop
       - Use Tailwind `flex flex-col md:flex-row md:items-center gap-3 md:gap-4`

    4. **Table header sticky on scroll**: Make the table header sticky when scrolling horizontally on mobile. Add `sticky top-0 z-10` to the thead or first tr.

    5. **Minimum column widths**: Set min-w on key columns to prevent them from collapsing too much:
       - Model column: min-w-[120px]
       - Input/Output columns: min-w-[80px]
       - This ensures the table remains readable even on very small screens.

    Update app/globals.css if needed for any custom responsive utilities. Check if the existing globals.css has Tailwind 4.x imports (`@import "tailwindcss"`) and add any needed custom utilities.

    Verify the layout by checking that on a 375px wide viewport:
    - Model, Provider, Input, Output, Confidence columns are visible
    - Family, Context, Source, Collected columns are accessible via horizontal scroll
    - Filter bar stacks vertically
    - Table header remains visible during horizontal scroll
  </action>
  <verify>
    <automated>pnpm build 2>&1 | Select-String -Pattern "error|Error" | Select-Object -First 5</automated>
  </verify>
  <done>Table is usable on mobile via horizontal scroll. Core columns always visible, secondary columns hidden on small screens. Filter bar stacks vertically on mobile. Table header is sticky. Build succeeds with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source URL -> External site | Links point to public provider pricing pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Source links | accept | URLs point to public pricing pages. rel="noopener noreferrer" prevents referrer leakage. |
| T-03-SC | Tampering | npm installs | accept | date-fns is pre-verified in CLAUDE.md stack |
</threat_model>

<verification>
1. `pnpm build` succeeds
2. Visit localhost:3000 on desktop — all columns visible, source links work
3. Visit localhost:3000 on mobile (or resize browser to 375px) — table scrolls horizontally, core columns visible
4. Click "View source" link — opens provider pricing page in new tab
5. Check "Last updated" banner — shows formatted date
6. Check "Collected" column — shows formatted dates for each row
7. Hover confidence badge — tooltip explains confidence level
</verification>

<success_criteria>
- Each row has a clickable source link opening in a new tab
- "Last updated: [date]" banner displays at the top of the page
- Confidence badges have explanatory tooltips
- Table is usable on 375px mobile viewport (horizontal scroll, core columns visible)
- Filter bar stacks vertically on mobile
- Collected date column shows formatted dates
- Build compiles with zero errors
</success_criteria>

<output>
Create `.planning/phases/03-pricing-comparison-table/03-03-SUMMARY.md` when done
</output>
