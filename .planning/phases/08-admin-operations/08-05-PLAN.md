---
phase: 08-admin-operations
plan: 05
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - app/admin/pipeline/page.tsx
  - app/components/admin/PipelineRunsTable.tsx
  - app/components/admin/ErrorLogTable.tsx
  - app/api/admin/pipeline/runs/route.ts
  - app/api/admin/pipeline/errors/route.ts
  - app/api/admin/pipeline/stream/route.ts
autonomous: true
requirements:
  - ADMN-08
user_setup: []
must_haves:
  truths:
    - "Pipeline page shows a table of pipeline runs with status, start time, duration, and stats"
    - "Pipeline runs table rows expand to show per-provider breakdown from stats JSONB"
    - "Error log table shows recent pipeline errors sorted by most recent"
    - "Pipeline page auto-refreshes via SSE stream"
    - "Admin can manually refresh pipeline data"
  artifacts:
    - path: "app/admin/pipeline/page.tsx"
      provides: "Pipeline monitoring page with runs table, error log, SSE stream"
    - path: "app/components/admin/PipelineRunsTable.tsx"
      provides: "Expandable table for pipeline runs with status icons and duration"
    - path: "app/components/admin/ErrorLogTable.tsx"
      provides: "Table for recent pipeline errors with expandable details"
    - path: "app/api/admin/pipeline/runs/route.ts"
      provides: "GET pipeline runs list"
      exports: ["GET"]
    - path: "app/api/admin/pipeline/errors/route.ts"
      provides: "GET recent pipeline errors"
      exports: ["GET"]
    - path: "app/api/admin/pipeline/stream/route.ts"
      provides: "SSE stream for real-time pipeline run updates"
      exports: ["GET"]
  key_links:
    - from: "app/admin/pipeline/page.tsx"
      to: "app/api/admin/pipeline/stream/route.ts"
      via: "EventSource for real-time updates"
      pattern: "EventSource.*pipeline/stream"
    - from: "app/admin/pipeline/page.tsx"
      to: "app/api/admin/pipeline/runs/route.ts"
      via: "fetch GET for initial data load"
      pattern: "fetch.*api/admin/pipeline/runs"
    - from: "app/components/admin/PipelineRunsTable.tsx"
      to: "src/db/schema"
      via: "Pipeline stats from pipelineRuns.stats JSONB"
      pattern: "stats.*PipelineStats"
---

<objective>
Build the pipeline monitoring page with a runs table showing status, duration, and expandable per-provider details, an error log table, and SSE-based real-time updates. Also create the supporting API routes. Per D-04, D-05, D-06.

Purpose: The pipeline page lets the admin monitor the health of the automated data collection system. Real-time updates via SSE ensure the admin sees current status without manual refresh. The error log surfaces failures that need attention.

Output: Working pipeline monitoring at /admin/pipeline with real-time updates, expandable run details, and error log.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-admin-operations/08-CONTEXT.md
@.planning/phases/08-admin-operations/08-UI-SPEC.md
@.planning/phases/08-admin-operations/08-PATTERNS.md
@src/db/schema.ts
@src/db/index.ts
@src/pipeline/orchestrator.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/db/schema.ts:
- pipelineRuns: id, status, startedAt, completedAt, stats (jsonb), createdAt, updatedAt

From src/pipeline/orchestrator.ts:
- PipelineStats: totalProviders, attempted, succeeded, failed, extractions, verifiedCount, likelyCount, lowConfidenceCount

From 08-UI-SPEC.md (copywriting):
- Empty state pipeline runs: "No pipeline runs recorded" / "Pipeline runs will appear here after the first daily collection completes."
- Empty state errors: "No errors recorded" / "All recent pipeline runs completed without errors."
- Error load pipeline: "Could not load pipeline data" / "The pipeline monitor could not retrieve run history. Refresh the page or check the pipeline logs."

From PATTERNS.md:
- Pattern 5: SSE Stream (Real-Time Updates) - ReadableStream with 1s interval
- Pattern 8: Expandable Table Row (Accordion) - one row expanded at a time
- Pattern 17: Table Header Styling
- Pattern 18: Empty State
- Pattern 20: SSE Client Consumer - EventSource with onmessage handler
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pipeline API routes and SSE stream</name>
  <files>
    app/api/admin/pipeline/runs/route.ts,
    app/api/admin/pipeline/errors/route.ts,
    app/api/admin/pipeline/stream/route.ts
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/index.ts,
    src/auth.ts,
    src/pipeline/orchestrator.ts,
    .planning/phases/08-admin-operations/08-PATTERNS.md
  </read_first>
  <behavior>
    - Test: GET /api/admin/pipeline/runs returns runs sorted by startedAt desc, limit 50
    - Test: GET /api/admin/pipeline/runs requires auth (401 if not)
    - Test: GET /api/admin/pipeline/errors returns failed runs sorted by startedAt desc, limit 20
    - Test: GET /api/admin/pipeline/stream returns SSE response with Content-Type text/event-stream
    - Test: SSE stream sends pipeline runs data every 1 second
    - Test: SSE stream closes on client disconnect
  </behavior>
  <action>
  1. Create `app/api/admin/pipeline/runs/route.ts`:
     - Import db, pipelineRuns, desc from drizzle-orm
     - Import auth from @/src/auth, NextResponse from next/server
     - GET handler:
       - Auth check: const session = await auth(); if (!session) return 401
       - Query: db.select().from(pipelineRuns).orderBy(desc(pipelineRuns.startedAt)).limit(50)
       - Return { runs: [...] }
       - Wrap in try/catch, return 500 on error

  2. Create `app/api/admin/pipeline/errors/route.ts`:
     - Import db, pipelineRuns, eq, desc from drizzle-orm
     - Import auth from @/src/auth, NextResponse from next/server
     - GET handler:
       - Auth check
       - Query: db.select().from(pipelineRuns).where(eq(pipelineRuns.status, 'failed')).orderBy(desc(pipelineRuns.startedAt)).limit(20)
       - Return { errors: [...] }
       - Wrap in try/catch, return 500 on error

  3. Create `app/api/admin/pipeline/stream/route.ts` (per Pattern 5 from PATTERNS.md):
     - Export `const dynamic = 'force-dynamic'`
     - Import auth from @/src/auth, NextRequest from next/server
     - GET handler:
       - Auth check: return new Response('Unauthorized', { status: 401 }) if not authenticated
       - Create ReadableStream with:
         - Initial keep-alive comment
         - setInterval every 1000ms: query pipelineRuns, enqueue as `data: ${JSON.stringify(runsData)}\n\n`
         - request.signal.addEventListener('abort', cleanup)
       - Return new Response(stream, { headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache, no-transform', 'Connection': 'keep-alive' } })
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Pipeline runs API returns runs list sorted by most recent
    - Errors API returns failed pipeline runs
    - SSE stream returns real-time pipeline updates
    - SSE stream closes on client disconnect
    - All routes require authentication
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    - Pipeline runs API returns runs list sorted by most recent
    - Errors API returns failed pipeline runs
    - SSE stream returns real-time pipeline updates
    - SSE stream closes on client disconnect
    - All routes require authentication
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pipeline monitoring components and page</name>
  <files>
    app/components/admin/PipelineRunsTable.tsx,
    app/components/admin/ErrorLogTable.tsx,
    app/admin/pipeline/page.tsx
  </files>
  <read_first>
    src/pipeline/orchestrator.ts,
    app/components/admin/Toast.tsx,
    .planning/phases/08-admin-operations/08-PATTERNS.md,
    .planning/phases/08-admin-operations/08-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: PipelineRunsTable renders with status, time, duration, stats columns
    - Test: Clicking a row expands it to show per-provider breakdown
    - Test: Only one row expanded at a time (accordion pattern)
    - Test: Status icons: green check for completed, red X for failed, yellow spinner for running
    - Test: Duration computed from startedAt and completedAt using differenceInSeconds
    - Test: ErrorLogTable shows failed runs with time, provider, error
    - Test: Pipeline page uses EventSource for real-time updates
    - Test: Pipeline page has manual refresh button
  </behavior>
  <action>
  1. Create `app/components/admin/PipelineRunsTable.tsx` (per Pattern 8 from PATTERNS.md):
     - `'use client'` directive
     - Import format, differenceInSeconds from date-fns
     - Props: runs: Array<{ id, status, startedAt, completedAt, stats }>
     - State: expandedRowId (number | null) — only one row expanded at a time
     - Columns: Status (icon), Started (formatted date), Duration (computed), Stats (succeeded/total), Expand (chevron)
     - Status icons: checkmark (green) for "completed", X (red) for "failed", spinner (yellow) for "running", ? (gray) for unknown
     - Duration: compute from startedAt and completedAt using differenceInSeconds, format as seconds/minutes/hours
     - Expanded row: show per-provider breakdown from stats JSONB as a simple table
     - Empty state: "No pipeline runs recorded" / "Pipeline runs will appear here after the first daily collection completes."

  2. Create `app/components/admin/ErrorLogTable.tsx`:
     - `'use client'` directive
     - Import format from date-fns
     - Props: errors: Array<{ id, startedAt, stats }>
     - Columns: Time (formatted), Provider (from stats.errorDetails), Error (message)
     - Expandable rows showing error details in pre block with bg-red-50
     - Empty state: "No errors recorded" / "All recent pipeline runs completed without errors."

  3. Create `app/admin/pipeline/page.tsx` (per Pattern 20 from PATTERNS.md):
     - `'use client'` directive
     - Import PipelineRunsTable, ErrorLogTable, useToast, ToastContainer
     - Import useState, useEffect, useCallback from React
     - State: runs, errors, isLoading
     - On mount: fetch initial data + set up EventSource for real-time updates
     - EventSource: new EventSource('/api/admin/pipeline/stream'), onmessage updates runs
     - Manual refresh button: fetches /api/admin/pipeline/runs and /api/admin/pipeline/errors
     - Render: "Pipeline Monitor" heading, PipelineRunsTable, "Recent Errors" heading, ErrorLogTable, ToastContainer
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PipelineRunsTable renders with status, time, duration, stats columns
    - Clicking a row expands it to show per-provider details
    - Only one row expanded at a time
    - ErrorLogTable shows failed pipeline runs with details
    - Pipeline page uses SSE for real-time updates
    - Manual refresh button works
    - Empty states display correctly
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    - PipelineRunsTable renders with status, time, duration, stats columns
    - Clicking a row expands it to show per-provider details
    - Only one row expanded at a time
    - ErrorLogTable shows failed pipeline runs with details
    - Pipeline page uses SSE for real-time updates
    - Manual refresh button works
    - Empty states display correctly
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->pipeline-api | Pipeline data fetched from /api/admin/pipeline/* |
| api->db | API routes read pipelineRuns table |
| sse->client | SSE stream pushes real-time data to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Information Disclosure | Pipeline runs API | accept | Pipeline data is admin-only. Protected by middleware auth check. |
| T-08-14 | Information Disclosure | SSE stream | accept | Real-time data is admin-only. Auth check at stream creation. |
| T-08-15 | Denial of Service | SSE connections | accept | Single admin user, limited connections. Auth check prevents unauthorized access. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes without errors
2. Navigate to /admin/pipeline — shows pipeline runs table and error log
3. Click a pipeline run row — expands to show per-provider details
4. SSE stream updates pipeline data in real-time
5. Manual refresh button fetches latest data
6. Empty states display when no data available
</verification>

<success_criteria>
- Pipeline runs table shows runs with status, time, duration, stats
- Expandable rows show per-provider breakdown
- Error log shows failed runs with details
- SSE stream provides real-time updates
- Manual refresh button works
- Empty states display correctly
</success_criteria>

<artifacts>
## Artifacts This Phase Produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| `PipelineRunsTable` | app/components/admin/PipelineRunsTable.tsx | component | Expandable table for pipeline runs |
| `ErrorLogTable` | app/components/admin/ErrorLogTable.tsx | component | Table for pipeline errors |
| `PipelinePage` | app/admin/pipeline/page.tsx | page | Pipeline monitoring with SSE |
| `GET /api/admin/pipeline/runs` | app/api/admin/pipeline/runs/route.ts | route | Pipeline runs list |
| `GET /api/admin/pipeline/errors` | app/api/admin/pipeline/errors/route.ts | route | Failed pipeline runs |
| `GET /api/admin/pipeline/stream` | app/api/admin/pipeline/stream/route.ts | route | SSE stream for real-time updates |
</artifacts>

<output>
Create `.planning/phases/08-admin-operations/08-05-SUMMARY.md` when done
</output>
