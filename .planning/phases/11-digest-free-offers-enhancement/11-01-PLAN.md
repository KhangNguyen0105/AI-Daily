---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/FreeOfferCard.test.tsx
  - app/components/PromotionCard.test.tsx
  - app/components/FreeOffersSection.test.tsx
  - app/components/PromotionsSection.test.tsx
  - app/api/digest-promotions/route.test.ts
  - tests/components/promotions-page.test.tsx
  - tests/pipeline/subscription-pipeline.test.ts
autonomous: false
requirements:
  - UI-04
  - UI-05
must_haves:
  truths:
    - "Free models display as green cards with FREE badge and provider link"
    - "Promotions display as amber cards with discount badge and provider link"
    - "No raw markdown formatting visible in digest UI"
    - "Cards render correctly in both light and dark themes"
    - "Cards are responsive: 1 col mobile, 2 col tablet, 3 col desktop"
    - "Empty sections return null (hidden from UI)"
    - "Loading state shows 3 skeleton pulse cards"
    - "Error state shows section heading with error message text"
  artifacts:
    - path: "app/components/FreeOfferCard.test.tsx"
      provides: "Unit tests for FreeOfferCard component"
    - path: "app/components/PromotionCard.test.tsx"
      provides: "Unit tests for PromotionCard component"
    - path: "app/components/FreeOffersSection.test.tsx"
      provides: "Unit tests for FreeOffersSection component"
    - path: "app/components/PromotionsSection.test.tsx"
      provides: "Unit tests for PromotionsSection component"
    - path: "app/api/digest-promotions/route.test.ts"
      provides: "Integration test for digest-promotions API endpoint"
  key_links:
    - from: "app/components/FreeOffersSection.tsx"
      to: "app/api/digest-promotions/route.ts"
      via: "fetch call with date param"
      pattern: "fetch.*api/digest-promotions"
    - from: "app/components/DigestArticle.tsx"
      to: "app/components/FreeOffersSection.tsx"
      via: "renders FreeOffersSection with article.date prop"
      pattern: "<FreeOffersSection date="
---

<objective>
Verify Phase 11 implementation against UI-SPEC, add comprehensive unit tests for all new components and the API endpoint, fix dark mode and mobile responsive issues, and clean up stale test references to the deleted /promotions page.

Purpose: Phase 11 implementation is code-complete but untested and unverified against the UI-SPEC contract. This plan adds test coverage, verifies visual correctness, and removes stale references.

Output: 5 new test files, 2 updated test files, visual verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-digest-free-offers-enhancement/11-UI-SPEC.md
@.planning/phases/11-digest-free-offers-enhancement/11-CONTEXT.md
@app/components/FreeOfferCard.tsx
@app/components/PromotionCard.tsx
@app/components/FreeOffersSection.tsx
@app/components/PromotionsSection.tsx
@app/api/digest-promotions/route.ts
@app/components/DigestArticle.tsx
@app/digest/page.tsx
@app/components/TopNav.tsx
@app/layout.tsx
@app/globals.css
@tests/components/promotions-page.test.tsx
@tests/pipeline/subscription-pipeline.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for Phase 11 components and API endpoint</name>
  <files>
    app/components/FreeOfferCard.test.tsx,
    app/components/PromotionCard.test.tsx,
    app/components/FreeOffersSection.test.tsx,
    app/components/PromotionsSection.test.tsx,
    app/api/digest-promotions/route.test.ts
  </files>
  <read_first>
    app/components/FreeOfferCard.tsx,
    app/components/PromotionCard.tsx,
    app/components/FreeOffersSection.tsx,
    app/components/PromotionsSection.tsx,
    app/api/digest-promotions/route.ts,
    tests/components/promotions-list.test.tsx,
    tests/conftest.ts,
    tests/setup.ts
  </read_first>
  <action>
Create 5 test files using Vitest + @testing-library/react (jsdom environment). Follow existing test patterns from `tests/components/promotions-list.test.tsx` for component tests and `tests/promotions.test.ts` for API tests.

**FreeOfferCard.test.tsx** — Test these behaviors:
- Renders FREE badge text
- Renders modelPattern as heading (h3)
- Renders description with line-clamp-3 class
- Renders credits text when credits prop is provided
- Does NOT render credits span when credits is null/undefined
- Provider link has `href` matching providerUrl prop
- Provider link has `target="_blank"` and `rel="noopener noreferrer"`
- Provider link text shows providerName
- External link SVG icon is present (check for `svg` element)
- Card has green background class `bg-green-500/10`
- Card has green border class `border-green-500/30`
- Card has hover class `hover:bg-green-500/15`

**PromotionCard.test.tsx** — Test these behaviors:
- Renders discount badge when `discount` prop is provided (e.g., "20% OFF")
- Extracts discount from description when no `discount` prop (e.g., "50% off models" shows "50% OFF")
- Renders "PROMO" fallback badge when no discount pattern found in description
- PROMO badge uses fallback style: `bg-amber-500/50 text-amber-700 dark:text-amber-300`
- Discount badge uses solid style: `bg-amber-500 text-white`
- Renders modelPattern as heading (h3)
- Renders description with line-clamp-3 class
- Provider link has correct href, target="_blank", rel="noopener noreferrer"
- Card has amber background class `bg-amber-500/10`
- Card has amber border class `border-amber-500/30`
- Card has hover class `hover:bg-amber-500/15`

**FreeOffersSection.test.tsx** — Test these behaviors:
- Mock `global.fetch` to return `{ freeOffers: [...] }` from `/api/digest-promotions?date=2026-06-22`
- Renders section heading "Free Models" with count badge
- Renders FreeOfferCard for each offer in response
- Returns null (renders nothing) when freeOffers array is empty
- Shows error message "Unable to load free offers" when fetch fails
- Shows loading skeleton with 3 pulse cards before data loads (use `waitFor` or `act`)
- Section heading uses green text class: `text-green-600 dark:text-green-400`
- Grid uses responsive classes: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`

**PromotionsSection.test.tsx** — Test these behaviors:
- Mock `global.fetch` to return `{ promotions: [...] }` from `/api/digest-promotions?date=2026-06-22`
- Renders section heading "Promotions & Discounts" with count badge
- Renders PromotionCard for each promotion in response
- Returns null (renders nothing) when promotions array is empty
- Shows error message "Unable to load promotions" when fetch fails
- Shows loading skeleton with 3 pulse cards before data loads
- Section heading uses amber text class: `text-amber-600 dark:text-amber-400`
- Grid uses responsive classes: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`

**app/api/digest-promotions/route.test.ts** — Test these behaviors:
- Returns 400 for invalid date format (not YYYY-MM-DD)
- Returns freeOffers and promotions arrays in response
- Separates free_tier type into freeOffers array
- Separates promotion and beta types into promotions array
- Returns date, total fields in response
- Defaults to today's date when no date param provided

Note: For the API test, mock the database module (`@/src/db/index`) to return controlled data. Do NOT connect to a real database. Use vi.mock() to stub `db.select().from().innerJoin().where()`.
  </action>
  <verify>
    <automated>cd "E:\WorkSpace\AI-base Projects\AI-daily" && npx vitest run app/components/FreeOfferCard.test.tsx app/components/PromotionCard.test.tsx app/components/FreeOffersSection.test.tsx app/components/PromotionsSection.test.tsx app/api/digest-promotions/route.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files pass with 0 failures
    - FreeOfferCard.test.tsx covers: FREE badge, modelPattern, description, credits optional, provider link with target/rel, green styling classes
    - PromotionCard.test.tsx covers: discount extraction, PROMO fallback, amber styling classes, provider link
    - FreeOffersSection.test.tsx covers: fetch mock, heading + count, card rendering, empty returns null, error state, loading skeleton
    - PromotionsSection.test.tsx covers: fetch mock, heading + count, card rendering, empty returns null, error state, loading skeleton
    - route.test.ts covers: 400 on bad date, freeOffers/promotions split, response shape
    - No test imports PromotionsPageClient (deleted component)
  </acceptance_criteria>
  <done>All 5 test files created and passing. Each component has coverage for rendering, props, styling classes, and edge cases (empty, loading, error).</done>
</task>

<task type="auto">
  <name>Task 2: Clean up stale /promotions references in existing tests</name>
  <files>
    tests/components/promotions-page.test.tsx,
    tests/pipeline/subscription-pipeline.test.ts
  </files>
  <read_first>
    tests/components/promotions-page.test.tsx,
    tests/pipeline/subscription-pipeline.test.ts,
    app/components/PromotionsPageClient.tsx
  </read_first>
  <action>
Fix two stale test files that reference the deleted /promotions page and PromotionsPageClient component.

**tests/components/promotions-page.test.tsx** — This file imports `PromotionsPageClient` from `../../app/components/PromotionsPageClient` which no longer exists (deleted in Phase 11). Two options:

Option A (preferred): Delete the file entirely. The PromotionsPageClient component no longer exists, and the /promotions page was intentionally removed per CONTEXT.md decision. The Phase 11 Task 1 tests now cover the replacement components (FreeOfferCard, PromotionCard, FreeOffersSection, PromotionsSection).

Option B (if deletion feels too aggressive): Rewrite the test to import and test `PromotionsList` from `../../app/components/PromotionsList` instead, since PromotionsList still exists and is used on the model detail page. Update mock data to match PromotionsList's `PromotionData` interface (has startDate, endDate, sourceUrl fields). Keep the filter/sort tests if PromotionsList supports them; otherwise simplify to render + badge + link tests.

Choose Option A — delete the file. The PromotionsList component already has its own test file at `tests/components/promotions-list.test.tsx`.

**tests/pipeline/subscription-pipeline.test.ts** — Lines 282-284 have a test with the description "subscription plans with freeTrialDays > 0 are eligible for /promotions display" and a comment referencing "free_trial promotions on the /promotions page". Update:
- Change the test description to: "subscription plans with freeTrialDays > 0 are eligible for promotions display"
- Change the comment to: "as free_trial promotions in the promotions table"
- The test logic itself is fine (just checks planWithTrial.freeTrialDays > 0) — no code change needed, only description/comment text.
  </action>
  <verify>
    <automated>cd "E:\WorkSpace\AI-base Projects\AI-daily" && npx vitest run tests/pipeline/subscription-pipeline.test.ts --reporter=verbose 2>&1 && echo "---" && ls tests/components/promotions-page.test.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/components/promotions-page.test.tsx is deleted (file does not exist)
    - tests/pipeline/subscription-pipeline.test.ts passes all tests
    - subscription-pipeline.test.ts no longer contains the literal string "/promotions page" or "/promotions display" — only "promotions display" or "promotions table"
    - grep for "PromotionsPageClient" across tests/ directory returns 0 results
  </acceptance_criteria>
  <done>Stale /promotions references removed. PromotionsPageClient test deleted. subscription-pipeline test text updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dark mode, mobile responsive, and UI-SPEC compliance</name>
  <files>app/components/FreeOfferCard.tsx, app/components/PromotionCard.tsx, app/components/FreeOffersSection.tsx, app/components/PromotionsSection.tsx, app/components/DigestArticle.tsx, app/digest/page.tsx</files>
  <action>Human visually verifies Phase 11 components against UI-SPEC contract. No code changes unless issues found — this is a visual QA checkpoint. If dark mode or mobile issues are found, describe them in the resume signal so they can be addressed.</action>
  <what-built>Visual verification of Phase 11 components against UI-SPEC contract: dark mode rendering, mobile responsive grid, card styling, provider links, loading skeletons, empty states.</what-built>
  <how-to-verify>
1. Start dev server: `pnpm dev`
2. Visit http://localhost:3000/digest and click into any article date
3. Verify Free Offers section:
   - Green cards with "FREE" badge are visible
   - Cards show model name, description (max 3 lines), provider link with external icon
   - Clicking provider link opens provider page in new tab
   - Cards in responsive grid: 1 col on narrow viewport, 2 on medium, 3 on wide
4. Verify Promotions section:
   - Amber cards with discount badge (e.g., "20% OFF") or "PROMO" fallback
   - Same layout and link behavior as free offers
5. Toggle dark mode (sun/moon button in TopNav):
   - Green card backgrounds remain visible (not invisible against dark bg)
   - Amber card backgrounds remain visible
   - Text remains readable (no dark-on-dark)
   - Badge colors adapt correctly
6. Test mobile viewport (resize to 375px width):
   - Cards stack in single column
   - Text is readable, no horizontal overflow
   - Provider links are tappable
7. Test edge cases:
   - Visit a digest date with no promotions — sections should be hidden (not show empty state)
   - Check archive page /digest — verify Free/Promo badges appear on articles that have them
8. Verify no raw markdown (e.g., **bold**) is visible in the rendered article content
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass. Describe specific issues if any fail (e.g., "dark mode: green cards invisible on dark background").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | /api/digest-promotions accepts date param from URL query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | /api/digest-promotions date param | mitigate | Date format validated with regex `/^\d{4}-\d{2}-\d{2}$/`, returns 400 on invalid |
| T-11-02 | Information Disclosure | Provider URLs in API response | accept | URLs come from trusted sources table, not user input |
| T-11-SC | Tampering | npm installs | mitigate | No new packages added in this phase |
</threat_model>

<verification>
1. All 5 new test files pass (`npx vitest run app/components/FreeOfferCard.test.tsx app/components/PromotionCard.test.tsx app/components/FreeOffersSection.test.tsx app/components/PromotionsSection.test.tsx app/api/digest-promotions/route.test.ts`)
2. Stale test file deleted, remaining test passes (`npx vitest run tests/pipeline/subscription-pipeline.test.ts`)
3. No grep results for "PromotionsPageClient" in tests/
4. No grep results for "/promotions page" in tests/
5. Human verification checkpoint passes (dark mode, mobile, edge cases)
</verification>

<success_criteria>
- 5 new test files with comprehensive coverage for FreeOfferCard, PromotionCard, FreeOffersSection, PromotionsSection, and /api/digest-promotions
- Stale promotions-page.test.tsx deleted
- subscription-pipeline.test.ts updated to remove /promotions page references
- Visual verification confirms dark mode and mobile responsive behavior per UI-SPEC
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/11-digest-free-offers-enhancement/11-01-SUMMARY.md` when done
</output>
