---
phase: 10-consumer-pricing-subscription-intelligence
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/subscriptions/page.tsx
  - app/components/SubscriptionsPageClient.tsx
  - app/components/SubscriptionCard.tsx
  - app/components/TopNav.tsx
  - app/components/PromotionsPageClient.tsx
  - app/promotions/page.tsx
autonomous: true
requirements:
  - DCOL-09

must_haves:
  truths:
    - "User can navigate to /subscriptions and see a card grid of consumer AI subscription plans"
    - "Each subscription card shows plan name, monthly/annual price, free trial badge, and key features"
    - "Each subscription card shows rawPriceText when numeric price is null (review #2)"
    - "Each subscription card shows 'Last checked' timestamp from crawledAt (review suggestion)"
    - "User can filter subscription plans by: All, Free Trial, Monthly, Annual with explicit semantics (review #7)"
    - "User can sort subscription plans by price (low-high, high-low) and free trial duration"
    - "TopNav shows a 'Subscriptions' link between 'Promotions' and 'Compare' with responsive mobile handling (review #10)"
    - "Promotions page shows subscription plans with free_trial_days > 0 as virtual projection (review #4)"
    - "startDate/endDate are only displayed for promotional campaigns, not standard plans (review #8)"
  artifacts:
    - path: "app/subscriptions/page.tsx"
      provides: "Server component fetching subscription plans from DB"
      exports: ["default"]
    - path: "app/components/SubscriptionsPageClient.tsx"
      provides: "Client component with filter pills, sort controls, and card grid"
      exports: ["SubscriptionsPageClient"]
    - path: "app/components/SubscriptionCard.tsx"
      provides: "Individual subscription plan card component"
      exports: ["SubscriptionCard"]
    - path: "app/components/TopNav.tsx"
      provides: "Updated navigation with Subscriptions link and responsive mobile menu"
      contains: "Subscriptions"
    - path: "app/components/PromotionsPageClient.tsx"
      provides: "Updated promotions page with free_trial filter pill"
    - path: "app/promotions/page.tsx"
      provides: "Server component with virtual projection of subscription free trials"
  key_links:
    - from: "app/subscriptions/page.tsx"
      to: "src/db/schema.ts"
      via: "Drizzle query on subscriptionPlans table"
      pattern: "db.select.*subscriptionPlans"
    - from: "app/subscriptions/page.tsx"
      to: "app/components/SubscriptionsPageClient.tsx"
      via: "Server component passes plans data to client component"
      pattern: "SubscriptionsPageClient"
    - from: "app/components/SubscriptionCard.tsx"
      to: "app/lib/promotion-constants.ts"
      via: "Free trial badge uses green badge styling"
      pattern: "badge-green"
    - from: "app/promotions/page.tsx"
      to: "src/db/schema.ts"
      via: "Virtual projection: queries subscription_plans for free trial entries at query time (review #4)"
      pattern: "subscriptionPlans"
---

<objective>
Build the /subscriptions page with card grid UI, add TopNav navigation link with responsive mobile handling, and surface subscription free trials on the promotions page via virtual projection.

Purpose: Per D-03, users need a dedicated page to view consumer AI subscription plans with filtering and sorting. Per D-04, free trials from subscription plans must also appear on the /promotions page via virtual projection (review #4: query-time, not materialized rows). The TopNav link provides discoverability with responsive mobile support (review #10).

Addresses review findings:
- Finding #4 (MEDIUM): /promotions uses virtual projection -- queries subscription_plans at query time, does NOT write synthetic rows to promotions table
- Finding #7 (MEDIUM): Filter logic explicitly defined with exact semantics
- Finding #8 (MEDIUM): startDate/endDate display only for promotional campaigns
- Finding #10 (LOW): TopNav responsive behavior verified; mobile hamburger menu added

Output: /subscriptions page route, SubscriptionsPageClient component, SubscriptionCard component, updated TopNav with mobile menu, updated PromotionsPageClient with virtual projection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-consumer-pricing-subscription-intelligence/10-CONTEXT.md
@.planning/phases/10-consumer-pricing-subscription-intelligence/10-UI-SPEC.md
@.planning/phases/10-consumer-pricing-subscription-intelligence/10-RESEARCH.md
@src/db/schema.ts
@app/promotions/page.tsx
@app/components/PromotionsPageClient.tsx
@app/components/PromotionCard.tsx
@app/components/TopNav.tsx
@app/lib/promotion-constants.ts
@app/lib/url-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /subscriptions page with SubscriptionCard and SubscriptionsPageClient</name>
  <files>app/subscriptions/page.tsx, app/components/SubscriptionsPageClient.tsx, app/components/SubscriptionCard.tsx</files>
  <read_first>
    - app/promotions/page.tsx (server component pattern: db query, map rows, pass to client component)
    - app/components/PromotionsPageClient.tsx (client component pattern: filter pills, card grid, empty state, theme tokens)
    - app/components/PromotionCard.tsx (card component pattern: border/bg classes, badge styles, source link)
    - app/components/PromotionsList.tsx (PromotionData interface pattern)
    - app/lib/promotion-constants.ts (badge style constants pattern)
    - app/lib/url-utils.ts (isSafeUrl utility)
    - src/db/schema.ts (subscriptionPlans table columns for query select, including new fields from Plan 01)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-UI-SPEC.md (exact layout, colors, copy, spacing)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-REVIEWS.md (findings #2, #7, #8)
  </read_first>
  <action>
1. Create `app/subscriptions/page.tsx` (server component, follows `app/promotions/page.tsx` exactly):

   - Import `db` from `@/src/db`
   - Import `subscriptionPlans` from `@/src/db/schema`
   - Import `SubscriptionsPageClient` from `@/app/components/SubscriptionsPageClient`
   - Export `revalidate = 60` (same as promotions page)
   - Define `SubscriptionPlanData` interface including all review-added fields:
     ```
     export interface SubscriptionPlanData {
       id: number;
       providerName: string;
       planName: string;
       monthlyPrice: number | null;    // null = not available or "contact sales"
       annualPrice: number | null;     // null = annual pricing not available
       annualMonthlyPrice: number | null;
       rawPriceText: string | null;    // review #2: original price string
       billingPeriod: string;          // review #2: monthly/annual/one_time/unknown
       freeTrialDays: number | null;   // null = unknown duration, 0 = no trial
       freeTrialConditions: string | null;
       keyFeatures: string[];
       currency: string;
       sourceUrl: string | null;
       confidence: string;             // review #5: verified/likely/low_confidence
       extractionNotes: string | null; // review #5: raw evidence
       crawledAt: Date | null;         // review suggestion: freshness indicator
     }
     ```
   - Default export async function `SubscriptionsPage`:
     - Query: `db.select({ id, providerName, planName, monthlyPrice, annualPrice, annualMonthlyPrice, rawPriceText, billingPeriod, freeTrialDays, freeTrialConditions, keyFeatures, currency, sourceUrl, confidence, extractionNotes, crawledAt }).from(subscriptionPlans)`
     - Map rows to `SubscriptionPlanData[]`, casting `keyFeatures` as `string[]`
     - Wrap in try/catch (same error handling pattern as promotions page)
     - Return `<SubscriptionsPageClient plans={plans} />`

2. Create `app/components/SubscriptionsPageClient.tsx` (client component, follows `PromotionsPageClient` exactly):

   - `'use client'` directive
   - Import `useState` from React
   - Import `SubscriptionCard` from `./SubscriptionCard`
   - Import `SubscriptionPlanData` from `../subscriptions/page.tsx` (or define inline)
   - Props: `{ plans: SubscriptionPlanData[] }`
   - State: `activeFilter` (type: `'all' | 'free_trial' | 'monthly' | 'annual'`), default `'all'`
   - State: `sortBy` (type: `'price_asc' | 'price_desc' | 'trial_desc'`), default `'price_asc'`

   **Filter logic (review #7 -- explicit semantics):**
   - `'all'`: show all plans (no filtering)
   - `'free_trial'`: filter where `plan.freeTrialDays !== null && plan.freeTrialDays > 0` OR `plan.freeTrialConditions !== null`. Rationale: some trials have conditions text but unknown duration (e.g., "Free trial for new users" without specifying days).
   - `'monthly'`: filter where `plan.monthlyPrice !== null`. This includes plans with explicit monthly pricing regardless of billingPeriod.
   - `'annual'`: filter where `plan.annualPrice !== null`. This includes plans with explicit annual pricing.

   **Sort logic:**
   - `'price_asc'`: sort by `monthlyPrice` ascending (nulls last -- push to end)
   - `'price_desc'`: sort by `monthlyPrice` descending (nulls last -- push to end)
   - `'trial_desc'`: sort by `freeTrialDays` descending (nulls last -- push to end)

   **Layout** (per 10-UI-SPEC.md):
   - Container: `div className="h-[calc(100vh-56px)] overflow-y-auto max-w-6xl mx-auto px-4 py-8"`
   - Heading: `h1 className="text-2xl font-semibold text-text-primary mb-2"` with text "Consumer AI Subscriptions"
   - Subheading: `p className="text-sm text-text-secondary mb-8"` with text from UI-SPEC
   - Filter pills row: `div className="flex flex-wrap gap-2 mb-6"` with 4 pills (All, Free Trial, Monthly, Annual)
   - Sort controls: inline select or button group right-aligned in the filter row
   - Active pill: `bg-accent-blue text-white`
   - Inactive pill: `bg-bg-tertiary text-text-primary hover:bg-bg-secondary`
   - Shared pill: `px-3 py-1.5 rounded-full text-sm font-medium transition-colors`

   **Card grid or empty state:**
   - If filtered+sorted plans length === 0: centered empty state per UI-SPEC copy
   - Otherwise: `div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4"` with `SubscriptionCard` for each plan

3. Create `app/components/SubscriptionCard.tsx` (card component, follows `PromotionCard` pattern):

   - `'use client'` directive
   - Import `SubscriptionPlanData` type
   - Import `format` from `date-fns`
   - Props: `{ plan: SubscriptionPlanData }`

   **Card layout** (per 10-UI-SPEC.md):
   - Container: `article className="border rounded-lg p-4 bg-bg-primary border-border-primary hover:shadow-md transition-shadow"` (uses semantic HTML per UI-SPEC accessibility)

   **Provider name + plan name row:**
   - `div className="flex items-center gap-2 mb-3"`
   - Provider name: `span className="text-sm font-medium text-text-secondary"` (capitalize first letter of providerName)
   - Plan name: `span className="text-lg font-semibold text-text-primary"`

   **Price display (review #2 -- handles null/ambiguous pricing):**
   - If `monthlyPrice` is not null: `p className="text-xl font-semibold text-text-primary"` formatted as `$XX/mo` using `Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' })`
   - If `monthlyPrice` is null AND `rawPriceText` is not null: show `p className="text-lg font-medium text-text-secondary"` with the rawPriceText value (e.g., "Contact sales", "Included with Google One"). This handles the ambiguous pricing case from review #2.
   - If `monthlyPrice` is null AND `rawPriceText` is null: show `p className="text-sm text-text-tertiary"` with "Price not available"
   - Annual breakdown: if `annualPrice` and `annualMonthlyPrice` exist, show `p className="text-sm text-text-secondary"` with `$annualPrice/yr ($annualMonthlyPrice/mo)`
   - **Unknown price UI state (review suggestion):** When both numeric prices are null, the card still shows the plan with rawPriceText or "Price not available" -- never hide the card entirely.

   **Free trial badge:**
   - If `plan.freeTrialDays !== null && plan.freeTrialDays > 0`:
   - `span className="inline-block px-2 py-0.5 rounded text-xs font-medium bg-badge-green-bg text-badge-green-text mb-3"`
   - Text: `"Free trial: {N} days"` or `"Free trial: {N} days -- {conditions}"` if conditions exist
   - Add `aria-label="Free trial available for {N} days"` for accessibility
   - If `plan.freeTrialConditions !== null && (plan.freeTrialDays === null || plan.freeTrialDays === 0)`: show conditions-only badge (e.g., "Free trial: New users only")

   **Key features list:**
   - `ul className="text-sm text-text-secondary space-y-1 mb-3"`
   - Show first 4 features as `li` with bullet point
   - If more than 4: show `li className="text-xs text-text-tertiary"` with "+N more"

   **Last checked timestamp (review suggestion -- freshness indicator):**
   - If `plan.crawledAt` is not null: show `p className="text-xs text-text-tertiary mt-2"` with "Last checked: {format(plan.crawledAt, 'MMM d, yyyy')}"

   **Source link:**
   - If `plan.sourceUrl` and `isSafeUrl(plan.sourceUrl)`:
   - `a` tag with `href={plan.sourceUrl}`, `target="_blank"`, `rel="noopener noreferrer"`
   - Class: `text-xs text-accent-blue hover:text-accent-blue/80 underline`
   - Text: "View details"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/subscriptions/page.tsx && echo OK` returns OK
    - `test -f app/components/SubscriptionsPageClient.tsx && echo OK` returns OK
    - `test -f app/components/SubscriptionCard.tsx && echo OK` returns OK
    - `grep -v '^#' app/subscriptions/page.tsx | grep -c 'subscriptionPlans'` returns at least 1
    - `grep -v '^#' app/subscriptions/page.tsx | grep -c 'SubscriptionsPageClient'` returns at least 1
    - `grep -v '^#' app/subscriptions/page.tsx | grep -c 'rawPriceText'` returns at least 1 (review #2)
    - `grep -v '^#' app/subscriptions/page.tsx | grep -c 'confidence'` returns at least 1 (review #5)
    - `grep -v '^#' app/components/SubscriptionsPageClient.tsx | grep -c 'free_trial'` returns at least 1
    - `grep -v '^#' app/components/SubscriptionsPageClient.tsx | grep -c 'Consumer AI Subscriptions'` returns at least 1
    - `grep -v '^#' app/components/SubscriptionCard.tsx | grep -c 'badge-green'` returns at least 1
    - `grep -v '^#' app/components/SubscriptionCard.tsx | grep -c 'rawPriceText'` returns at least 1 (review #2)
    - `grep -v '^#' app/components/SubscriptionCard.tsx | grep -c 'crawledAt'` returns at least 1 (review suggestion)
    - `grep -v '^#' app/components/SubscriptionCard.tsx | grep -c 'View details'` returns at least 1
    - TypeScript compiles without errors: `npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>/subscriptions page fetches subscription plans from DB and renders SubscriptionsPageClient; client component has filter pills (All/Free Trial/Monthly/Annual) with explicit semantics per review #7, sort controls, and card grid; SubscriptionCard shows plan name, price (or rawPriceText for ambiguous pricing per review #2), free trial badge, features list, last-checked timestamp, and source link per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Add TopNav link with responsive mobile menu and virtual projection for /promotions</name>
  <files>app/components/TopNav.tsx, app/components/PromotionsPageClient.tsx, app/promotions/page.tsx</files>
  <read_first>
    - app/components/TopNav.tsx (current navLinks array at lines 19-26, no mobile menu currently)
    - app/components/PromotionsPageClient.tsx (current filter and render logic)
    - app/components/PromotionsList.tsx (PromotionData interface)
    - app/promotions/page.tsx (server component fetching promotions)
    - src/db/schema.ts (subscriptionPlans and promotions table definitions)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-CONTEXT.md (D-03 nav link, D-04 free trial surfacing)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-UI-SPEC.md (nav link placement, copy)
    - .planning/phases/10-consumer-pricing-subscription-intelligence/10-REVIEWS.md (findings #4, #10)
  </read_first>
  <action>
1. In `app/components/TopNav.tsx`, add "Subscriptions" link and responsive mobile menu (review #10).

   **Add Subscriptions link:** Insert `{ label: 'Subscriptions', href: '/subscriptions' }` between the "Promotions" entry (index 3) and "Compare" entry (index 4). Per UI-SPEC, this groups pricing-related pages together.

   The updated `navLinks` array should have 7 entries:
   ```
   const navLinks = [
     { label: 'Pricing', href: '/' },
     { label: 'Daily Digest', href: '/digest' },
     { label: 'Trends', href: '/trends' },
     { label: 'Promotions', href: '/promotions' },
     { label: 'Subscriptions', href: '/subscriptions' },
     { label: 'Compare', href: '/compare' },
     { label: 'Alerts', href: '/alerts' },
   ];
   ```

   **Responsive mobile menu (review #10):** The current TopNav renders all links in a flex row with no mobile handling. With 7 links plus the theme toggle, this will overflow on small screens. Add responsive behavior:

   - Add state: `const [mobileMenuOpen, setMobileMenuOpen] = useState(false)`
   - On screens < md (768px): hide the inline nav links, show a hamburger button instead
   - Hamburger button: `button` with three-line SVG icon, `className="md:hidden p-2 text-text-secondary hover:text-text-primary"`
   - Mobile menu: when `mobileMenuOpen` is true, show a dropdown panel below the nav bar:
     - `div className="md:hidden absolute top-full left-0 right-0 bg-bg-secondary border-b border-border-primary shadow-lg"`
     - Each link as a block-level element with padding
     - Close menu on link click: `onClick={() => setMobileMenuOpen(false)}`
   - On screens >= md: show inline links as before (existing behavior), hide hamburger with `hidden md:flex`

   The existing active detection logic (`pathname.startsWith(link.href)`) will automatically work for `/subscriptions`.

2. In `app/promotions/page.tsx`, add virtual projection of subscription free trials (review #4).

   **Approach (review #4 -- virtual projection, NOT materialized rows):** The server component queries `subscription_plans` at query time and converts free trial plans into `PromotionData`-compatible objects. This avoids writing synthetic rows to the promotions table, preventing data duplication and inconsistency.

   In `app/promotions/page.tsx`:
   - Import `subscriptionPlans` from `@/src/db/schema`
   - Import `gt` from `drizzle-orm`
   - After the existing promotions query, add a query for subscription plans with free trials:
     ```
     const trialRows = await db
       .select({
         id: subscriptionPlans.id,
         providerName: subscriptionPlans.providerName,
         planName: subscriptionPlans.planName,
         freeTrialDays: subscriptionPlans.freeTrialDays,
         freeTrialConditions: subscriptionPlans.freeTrialConditions,
         sourceUrl: subscriptionPlans.sourceUrl,
       })
       .from(subscriptionPlans)
       .where(gt(subscriptionPlans.freeTrialDays, 0));
     ```
   - Map trial rows to `PromotionData` format:
     ```
     const trialPromos: PromotionData[] = trialRows.map(row => ({
       id: row.id + 100000, // Offset to avoid ID collision with promotions table IDs
       modelPattern: `${row.providerName} ${row.planName}`,
       type: 'free_trial' as const, // Per D-04: new enum value added in Plan 01
       description: row.freeTrialConditions
         ? `Free trial: ${row.freeTrialDays} days -- ${row.freeTrialConditions}`
         : `Free trial: ${row.freeTrialDays} days`,
       credits: null,
       startDate: null,
       endDate: null,
       sourceUrl: row.sourceUrl,
     }));
     ```
   - Combine: `promos = [...promos, ...trialPromos]`

   Note: `startDate` and `endDate` are null for subscription trials because they are standard plans, not promotional campaigns (review #8). Only promotions table rows with explicit campaign dates show start/end dates.

3. In `app/components/PromotionsPageClient.tsx`, add a `free_trial` filter pill and badge styling:
   - Add `{ label: 'Free Trial', value: 'free_trial' }` to the filter pills array
   - Add badge styling for `free_trial` type: reuse the green badge (`bg-badge-green-bg text-badge-green-text`) since trials are positive offers, same visual treatment as `free_tier`
   - Update the filter logic to handle `free_trial` as a separate filter value
   - Add `'free_trial'` to the PromotionData type if not already present (the type union should include it since Plan 01 added it to promotionTypeEnum)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' app/components/TopNav.tsx | grep -c 'Subscriptions'` returns at least 1
    - `grep -v '^#' app/components/TopNav.tsx | grep -c '/subscriptions'` returns at least 1
    - `grep -v '^#' app/components/TopNav.tsx | grep -c 'mobileMenuOpen'` returns at least 1 (review #10)
    - `grep -v '^#' app/components/TopNav.tsx | grep -c 'md:hidden'` returns at least 1 (review #10)
    - TopNav navLinks array has 7 entries (was 6)
    - `grep -v '^#' app/promotions/page.tsx | grep -c 'subscriptionPlans'` returns at least 1 (review #4 virtual projection)
    - `grep -v '^#' app/promotions/page.tsx | grep -c 'freeTrialDays'` returns at least 1
    - `grep -v '^#' app/promotions/page.tsx | grep -c 'free_trial'` returns at least 1
    - `grep -v '^#' app/components/PromotionsPageClient.tsx | grep -c 'free_trial'` returns at least 1
    - TypeScript compiles without errors: `npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>TopNav has "Subscriptions" link between Promotions and Compare with responsive mobile hamburger menu (review #10); /promotions page uses virtual projection to query subscription_plans for free trials at query time (review #4, NOT materialized rows); filter pills include separate "Free Trial" option; startDate/endDate are null for subscription trials since they are standard plans not campaigns (review #8).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB query result -> React render | Data is from trusted DB (already validated on insert); React auto-escapes |
| External source_url -> anchor href | isSafeUrl() validates URLs before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of Privilege | SubscriptionCard source link | mitigate | isSafeUrl() validates URLs; target="_blank" with rel="noopener noreferrer" |
| T-10-07 | Information Disclosure | subscription plan display | accept | Public pricing data; no PII; source URLs are intentional for transparency |
| T-10-09 | Tampering | Virtual projection ID collision | mitigate | trialPromos IDs offset by 100000 to prevent collision with promotions table IDs |
</threat_model>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Subscriptions page exists: `test -f app/subscriptions/page.tsx && echo OK`
- TopNav updated: `grep -c 'Subscriptions' app/components/TopNav.tsx` returns >= 1
- TopNav responsive: `grep -c 'mobileMenuOpen' app/components/TopNav.tsx` returns >= 1
- Promotions virtual projection: `grep -c 'subscriptionPlans' app/promotions/page.tsx` returns >= 1
- D-04 compliance: `grep -c 'free_trial' app/promotions/page.tsx` returns >= 1
- rawPriceText in card: `grep -c 'rawPriceText' app/components/SubscriptionCard.tsx` returns >= 1
</verification>

<success_criteria>
- /subscriptions page renders card grid of subscription plans from database
- SubscriptionCard handles null prices with rawPriceText display (review #2)
- SubscriptionCard shows "Last checked" timestamp from crawledAt
- Filter pills work with explicit semantics: All, Free Trial, Monthly, Annual (review #7)
- Sort controls work: price ascending, price descending, trial duration
- TopNav has "Subscriptions" link with responsive mobile hamburger menu (review #10)
- /promotions page uses virtual projection for subscription free trials (review #4)
- startDate/endDate are null for subscription trials (review #8)
- All components use theme tokens (no hardcoded colors)
</success_criteria>

<output>
Create `.planning/phases/10-consumer-pricing-subscription-intelligence/10-03-SUMMARY.md` when done
</output>
