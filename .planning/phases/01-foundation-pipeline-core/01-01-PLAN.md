---
phase: 01-foundation-pipeline-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - docker-compose.yml
  - Dockerfile
  - drizzle.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - src/db/schema.ts
  - src/db/index.ts
  - src/lib/env.ts
  - .env.example
  - .gitignore
  - vitest.config.ts
  - tests/schema.test.ts
  - tests/landing.test.tsx
autonomous: true
requirements:
  - FRNT-01
  - FRNT-02
must_haves:
  truths:
    - "docker compose up starts Next.js, PostgreSQL, and Redis without errors"
    - "Public page renders at localhost:3000 with 'AI Daily' branding"
    - "No login prompts or registration forms on the public page"
    - "Database schema defines all 6 core tables (sources, raw_data, extractions, articles, pipeline_runs, practical_costs)"
    - "Drizzle Kit push applies schema to PostgreSQL successfully"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL, Redis, Next.js app services"
      contains: "postgres:"
    - path: "src/db/schema.ts"
      provides: "All 6 core tables with foreign keys, JSONB, and enum"
      contains: "sources"
    - path: "app/page.tsx"
      provides: "Public landing page with AI Daily branding"
      contains: "AI Daily"
    - path: "app/layout.tsx"
      provides: "Root layout with metadata and Tailwind"
      contains: "AI Daily"
  key_links:
    - from: "docker-compose.yml"
      to: "src/db/index.ts"
      via: "DATABASE_URL environment variable"
      pattern: "DATABASE_URL.*postgresql"
    - from: "src/db/schema.ts"
      to: "drizzle.config.ts"
      via: "schema path reference"
      pattern: "schema.*src/db/schema"
---

<objective>
Project scaffold with Next.js 16, Docker Compose (PostgreSQL, Redis, Next.js), Drizzle ORM schema with all 6 core tables, and a public landing page at localhost:3000 with "AI Daily" branding.

Purpose: Establish the foundation that all subsequent plans build on. Proves the Docker stack works and the database schema is correct.

Output: Working `docker compose up` that serves a public page, with database schema applied via `drizzle-kit push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-pipeline-core/01-CONTEXT.md
@.planning/phases/01-foundation-pipeline-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project with Next.js, Docker Compose, and database schema</name>
  <files>package.json, tsconfig.json, next.config.ts, docker-compose.yml, Dockerfile, drizzle.config.ts, src/db/schema.ts, src/db/index.ts, src/lib/env.ts, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-pipeline-core/01-RESEARCH.md (standard stack, schema example, docker-compose example, drizzle config example, env validation example)
    - .planning/phases/01-foundation-pipeline-core/01-CONTEXT.md (D-05 through D-08: schema decisions)
    - CLAUDE.md (locked tech stack versions: next 16.2.9, drizzle-orm 0.45.2, bullmq 5.78.0, ioredis 5.11.1, crawlee 3.17.0, playwright 1.60.0, zod 4.4.3, tailwindcss 4.3.0)
  </read_first>
  <action>
Scaffold the project from scratch (this is a greenfield repo with only CLAUDE.md and .planning/). Do NOT use `pnpm create next-app` -- it adds unnecessary boilerplate. Create files manually for full control.

**NOTE ON FILE COUNT:** This task has 11 files in files_modified. This is acceptable because each file is a simple scaffold (config, boilerplate, or small schema definition) with low per-file complexity. No individual file exceeds ~100 lines of meaningful content. The high count is a natural consequence of bootstrapping a full-stack project from zero -- these files are all interdependent scaffolding that must exist together before any subsequent task can run.

1. Initialize pnpm project: `pnpm init` in the project root.

2. Create `package.json` with these exact dependencies (per CLAUDE.md locked versions):
   - dependencies: next@16.2.9, react@19.x, react-dom@19.x, drizzle-orm@0.45.2, pg@8.21.0, @types/pg, dotenv, bullmq@5.78.0, ioredis@5.11.1, crawlee@3.17.0, playwright@1.60.0, zod@4.4.3, @ai-sdk/openai, @ai-sdk/anthropic, ai@6.0.199, date-fns@4.4.0, tailwindcss@4.3.0, @tailwindcss/postcss
   - devDependencies: typescript@5.x, @types/react, @types/node, drizzle-kit, tsx, vitest, eslint, prettier
   - scripts: "dev": "next dev --turbopack", "build": "next build", "start": "next start", "db:push": "drizzle-kit push", "db:generate": "drizzle-kit generate", "test": "vitest run"

3. Run `pnpm install`.

4. Create `tsconfig.json` for Next.js 16 with App Router. Set `strict: true`, `paths: { "@/*": ["./*"] }`, `target: "ES2022"`, `module: "ESNext"`, `moduleResolution: "bundler"`.

5. Create `next.config.ts` with minimal Next.js 16 config. No special settings needed for Phase 1.

6. Create `docker-compose.yml` with 3 services (per RESEARCH.md docker-compose example):
   - `postgres`: postgres:16-alpine, port 5432, user=aidaily, password=aidaily_dev, db=aidaily, healthcheck with pg_isready
   - `redis`: redis:7-alpine, port 6379, healthcheck with redis-cli ping
   - `app`: build from Dockerfile, port 3000, depends_on postgres and redis with condition: service_healthy, env vars DATABASE_URL, REDIS_HOST, REDIS_PORT
   - Volume: postgres_data for persistence

7. Create `Dockerfile` (multi-stage for Next.js):
   - Stage 1 (deps): node:20-alpine, copy package.json and pnpm-lock.yaml, `pnpm install --frozen-lockfile`
   - Stage 2 (builder): copy all, `pnpm build`
   - Stage 3 (runner): node:20-alpine, copy .next/static, public, node_modules, .next/standalone, expose 3000, CMD ["node", "server.js"]
   - Note: Use `output: 'standalone'` in next.config.ts for Docker

8. Create `.env.example` with: DATABASE_URL, REDIS_HOST, REDIS_PORT, OPENAI_API_KEY, ANTHROPIC_API_KEY, NODE_ENV

9. Create `src/lib/env.ts` using Zod schema validation (per RESEARCH.md env example). Validate DATABASE_URL (required), REDIS_HOST (default localhost), REDIS_PORT (default 6379), OPENAI_API_KEY (optional), ANTHROPIC_API_KEY (optional), NODE_ENV (default development).

10. Create `src/db/index.ts` -- Drizzle connection setup using `drizzle-orm/node-postgres`. Import schema, create db instance from DATABASE_URL env var.

11. Create `src/db/schema.ts` with ALL 6 core tables (per D-05, D-06, D-07, D-08 and RESEARCH.md schema example):
    - `confidenceEnum`: pgEnum with values 'verified', 'likely', 'low_confidence' (per D-06)
    - `sources`: id (serial PK), name (varchar 255), url (text), provider_type (varchar 100), is_active (integer default 1), created_at, updated_at
    - `raw_data`: id (serial PK), source_id (FK → sources.id), url (text), evidence (jsonb) (per D-07), crawled_at (timestamp), created_at, updated_at
    - `extractions`: id (serial PK), raw_data_id (FK → raw_data.id), source_id (FK → sources.id), model_name (varchar 255), input_price_per_1m (real), output_price_per_1m (real), context_window (integer), confidence (confidenceEnum) (per D-06), raw_evidence (jsonb) (per D-07), collected_at (timestamp) (per D-08), created_at, updated_at
    - `articles`: id (serial PK), title (varchar 500), content (text), published_at (timestamp nullable), created_at, updated_at
    - `pipeline_runs`: id (serial PK), status (varchar 50), started_at (timestamp), completed_at (timestamp nullable), stats (jsonb), created_at, updated_at
    - `practical_costs`: id (serial PK), extraction_id (FK → extractions.id), scenario_name (varchar 255), input_tokens (integer), output_tokens (integer), estimated_cost (real), created_at, updated_at
    - All tables have created_at and updated_at timestamps with defaultNow() (per D-08)

12. Create `drizzle.config.ts` pointing to `./src/db/schema.ts` with dialect 'postgresql' and dbCredentials from DATABASE_URL.

13. Create `app/globals.css` with Tailwind v4 import: `@import "tailwindcss";`

14. Create `.gitignore` including: node_modules, .next, .env, dist, drizzle, *.tsbuildinfo, .turbo
  </action>
  <verify>
    <automated>npx tsx -e "import * as s from './src/db/schema'; const tables = ['sources','rawData','extractions','articles','pipelineRuns','practicalCosts']; tables.forEach(t => { if (!(s as any)[t]) throw new Error('Missing table: ' + t); }); console.log('All 6 tables exported');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists with "next": "16.2.9" in dependencies
    - docker-compose.yml contains "postgres:", "redis:", and "app:" services
    - docker-compose.yml postgres service has healthcheck with "pg_isready"
    - src/db/schema.ts exports tables: sources, rawData, extractions, articles, pipelineRuns, practicalCosts
    - src/db/schema.ts exports confidenceEnum with values 'verified', 'likely', 'low_confidence'
    - src/db/schema.ts rawData table has evidence column of type jsonb
    - src/db/schema.ts extractions table has raw_evidence column of type jsonb and confidence column using confidenceEnum
    - src/db/index.ts imports drizzle from 'drizzle-orm/node-postgres' and exports db
    - src/lib/env.ts uses Zod to validate DATABASE_URL, REDIS_HOST, REDIS_PORT
    - .env.example contains DATABASE_URL=, REDIS_HOST=, REDIS_PORT=
    - Dockerfile has multi-stage build with node:20-alpine
    - drizzle.config.ts references './src/db/schema.ts'
    - pnpm install completes without errors
  </acceptance_criteria>
  <done>Project scaffolds, dependencies install, and TypeScript compiles without errors. Docker Compose file defines all 3 services with healthchecks. Database schema defines all 6 tables with correct foreign keys, JSONB columns, and enum.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create public landing page and database schema test</name>
  <files>app/layout.tsx, app/page.tsx, vitest.config.ts, tests/conftest.ts, tests/schema.test.ts, tests/landing.test.tsx</files>
  <read_first>
    - src/db/schema.ts (current schema definitions -- must match test assertions)
    - app/globals.css (Tailwind imports)
    - .planning/phases/01-foundation-pipeline-core/01-RESEARCH.md (code examples: root layout, landing page)
    - .planning/phases/01-foundation-pipeline-core/01-CONTEXT.md (FRNT-01: read-only, no registration; FRNT-02: Next.js App Router with SSG)
  </read_first>
  <action>
Create the public landing page and test infrastructure.

1. Create `app/layout.tsx` (per RESEARCH.md code example):
   - Import type Metadata from 'next'
   - Import './globals.css'
   - metadata: title = 'AI Daily - AI Model Pricing Intelligence', description = 'Understand what AI models actually cost in real-world usage'
   - RootLayout component wraps children in html lang="en" and body
   - No login prompts, no registration links, no auth-related UI (per FRNT-01)

2. Create `app/page.tsx`:
   - Default export HomePage component (Server Component, no 'use client')
   - Renders: main element with h1 "AI Daily", subtitle "AI Model Pricing Intelligence", tagline about understanding real-world AI costs
   - Style with Tailwind classes: centered, clean typography, dark text on white background
   - NO login/register buttons, NO auth prompts (per FRNT-01)
   - This is an SSG page -- no dynamic data fetching in Phase 1

3. Create `vitest.config.ts`:
   - Import defineConfig from 'vitest/config'
   - Set test.globals to true
   - Set test.environment to 'node' (default)
   - Set test.include to ['tests/**/*.test.ts', 'tests/**/*.test.tsx']
   - If testing JSX/TSX, add react plugin support or use @testing-library/react later

4. Create `tests/schema.test.ts`:
   - Import all table exports from '../src/db/schema'
   - Test: sources table is defined and has columns id, name, url, provider_type, is_active, created_at, updated_at
   - Test: rawData table is defined and has columns id, source_id, url, evidence, crawled_at, created_at, updated_at
   - Test: extractions table is defined and has columns id, raw_data_id, source_id, model_name, input_price_per_1m, output_price_per_1m, context_window, confidence, raw_evidence, collected_at, created_at, updated_at
   - Test: articles table is defined and has columns id, title, content, published_at, created_at, updated_at
   - Test: pipelineRuns table is defined and has columns id, status, started_at, completed_at, stats, created_at, updated_at
   - Test: practicalCosts table is defined and has columns id, extraction_id, scenario_name, input_tokens, output_tokens, estimated_cost, created_at, updated_at
   - Test: confidenceEnum has values 'verified', 'likely', 'low_confidence'
   - Use Drizzle's table inspection or simply verify the exported objects have the expected Symbol.toStringTag or column names

5. Create `tests/conftest.ts` (shared test fixtures per VALIDATION.md Wave 0):
   - Export a mock DB helper: a function that returns a mock drizzle-like object with select/insert/update/delete stubs
   - Export a mock Redis helper: a function that returns a mock ioredis-like object
   - These mocks allow tests that import pipeline/adapter code to run without real PostgreSQL or Redis connections
   - Keep it minimal -- just enough stubs so `import { db } from '../src/db/index'` does not throw in test context

6. Create `tests/landing.test.tsx` (placeholder -- full JSX testing needs @testing-library/react which we add later):
   - For now, create a simple test that imports the page component and verifies it's a function
   - Mark as todo: "Full render test requires @testing-library/react -- add in Phase 2"

Behavior:
- Schema test: imports src/db/schema.ts, verifies all 6 tables exist with expected column names
- Landing test: imports app/page.tsx, verifies the export is a valid React component function
  </action>
  <verify>
    <automated>pnpm vitest run tests/schema.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains metadata with title "AI Daily" and description containing "real-world usage"
    - app/page.tsx renders an h1 element with text "AI Daily"
    - app/page.tsx does NOT contain the strings "login", "register", "sign in", "sign up" (per FRNT-01)
    - vitest.config.ts exists and configures test.include for tests/ directory
    - tests/schema.test.ts passes with 0 failures
    - pnpm vitest run tests/schema.test.ts exits with code 0
  </acceptance_criteria>
  <done>Landing page renders "AI Daily" branding with no auth prompts. Schema test validates all 6 tables exist with correct columns. Vitest is configured and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js | Public page served via SSG; no user input crosses this boundary in Phase 1 |
| Docker internal | PostgreSQL and Redis are on internal Docker network; not exposed externally in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file | mitigate | .env in .gitignore; .env.example has placeholder values only |
| T-01-02 | Tampering | database | mitigate | Drizzle ORM uses parameterized queries by default; no raw SQL |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `docker compose up -d` starts all 3 services (postgres, redis, app) without errors
- `docker compose ps` shows all services as "running" or "healthy"
- `pnpm vitest run tests/schema.test.ts` passes
- `pnpm run build` completes without TypeScript errors
- `curl http://localhost:3000` returns HTML containing "AI Daily"
</verification>

<success_criteria>
- Docker Compose stack runs with PostgreSQL, Redis, and Next.js app
- Database schema has all 6 core tables with correct foreign keys and types
- Public page at localhost:3000 shows "AI Daily" with no login/register prompts
- Vitest configured and schema tests pass
</success_criteria>

<output>
Create `.planning/phases/01-foundation-pipeline-core/01-01-SUMMARY.md` when done
</output>
