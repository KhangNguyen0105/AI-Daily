---
phase: 07-intelligence-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/TopNav.tsx
  - app/lib/alerts.ts
  - app/alerts/page.tsx
  - app/components/AlertsPageClient.tsx
  - tests/lib/alerts.test.ts
autonomous: true
requirements:
  - INTL-05
must_haves:
  truths:
    - "TopNav shows links to Trends, Promotions, Compare, and Alerts pages"
    - "User can view all saved price alerts on the /alerts page"
    - "User can remove individual alerts or clear all alerts"
    - "Alerts persist in localStorage across page refreshes"
  artifacts:
    - path: "app/components/TopNav.tsx"
      provides: "Navigation links to all Phase 7 pages"
      contains: "Trends"
    - path: "app/lib/alerts.ts"
      provides: "localStorage CRUD for price alerts"
      exports: ["getAlerts", "addAlert", "removeAlert", "clearAlerts"]
    - path: "app/alerts/page.tsx"
      provides: "Alerts management page"
    - path: "app/components/AlertsPageClient.tsx"
      provides: "Alert list with remove/clear actions"
  key_links:
    - from: "app/components/TopNav.tsx"
      to: "/trends, /promotions, /compare, /alerts"
      via: "Link components"
      pattern: "href.*(/trends|/promotions|/compare|/alerts)"
    - from: "app/alerts/page.tsx"
      to: "app/lib/alerts.ts"
      via: "import getAlerts"
      pattern: "import.*alerts"
---

<objective>
Add navigation links for all Phase 7 pages to TopNav and build the price alerts management page with localStorage CRUD utilities.

Purpose: Establishes the navigation backbone for all new pages and delivers the alerts management feature (INTL-05) as a self-contained vertical slice.
Output: Updated TopNav with 4 new links, alerts utility module, /alerts page with full CRUD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-intelligence-analytics/07-CONTEXT.md
@.planning/phases/07-intelligence-analytics/07-RESEARCH.md
@.planning/phases/07-intelligence-analytics/07-UI-SPEC.md

@app/components/TopNav.tsx
@app/components/ModelDetailClient.tsx
@app/lib/pricing-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TopNav with Phase 7 navigation links</name>
  <files>app/components/TopNav.tsx</files>
  <read_first>
    - app/components/TopNav.tsx (current nav links and active detection pattern)
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-19: add Trends, Promotions, Compare, Alerts)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (navigation section, spacing, typography)
  </read_first>
  <action>
    Per D-19: Add four new nav links to the `navLinks` array in TopNav.tsx:
    - { label: 'Trends', href: '/trends' }
    - { label: 'Promotions', href: '/promotions' }
    - { label: 'Compare', href: '/compare' }
    - { label: 'Alerts', href: '/alerts' }

    Insert these after the existing 'Daily Digest' link. Keep the existing active link detection logic (pathname.startsWith) which already handles sub-routes. No changes to the nav styling or layout structure needed -- the existing flex container with gap-6 handles additional links.
  </action>
  <verify>
    <automated>grep -c "Trends\|Promotions\|Compare\|Alerts" app/components/TopNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TopNav.tsx navLinks array contains entries for href values: /trends, /promotions, /compare, /alerts
    - All four new links render with the same text-sm styling as existing links
    - Active link detection works: visiting /trends highlights "Trends" link with text-blue-600 + font-bold
  </acceptance_criteria>
  <done>TopNav displays 6 links total: Pricing, Daily Digest, Trends, Promotions, Compare, Alerts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create alerts localStorage utility and /alerts page</name>
  <files>app/lib/alerts.ts, app/alerts/page.tsx, app/components/AlertsPageClient.tsx, tests/lib/alerts.test.ts</files>
  <read_first>
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-14: localStorage, D-17: below threshold only, D-18: alert structure)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (/alerts page layout, empty states, destructive actions, spacing, typography)
    - .planning/phases/07-intelligence-analytics/07-RESEARCH.md (localStorage CRUD pattern, Pitfall 2: SSR mismatch)
    - app/components/PromotionsList.tsx (pattern for empty state display)
    - app/page.tsx (server component pattern with try/catch)
    - app/lib/pricing-utils.ts (formatPrice, sanitizeDisplayName patterns)
  </read_first>
  <behavior>
    - getAlerts() returns empty array when localStorage is empty
    - addAlert() stores alert with modelName, sourceId, thresholdPrice, createdAt
    - addAlert() prevents duplicate alerts (same modelName + sourceId)
    - removeAlert() removes specific alert by modelName + sourceId
    - clearAlerts() removes all alerts
    - getAlerts() returns [] on server (typeof window === 'undefined')
    - AlertsPageClient renders alert list with model name, threshold, remove button
    - AlertsPageClient shows empty state when no alerts
    - AlertsPageClient "Clear All" shows inline confirmation before clearing
  </behavior>
  <action>
    Create three files:

    1. **app/lib/alerts.ts** -- localStorage CRUD utility per D-14, D-17, D-18:
       - Export `PriceAlert` interface: `{ modelName: string; sourceId: number; thresholdPrice: number; createdAt: string }`
       - Constant `ALERTS_KEY = 'ai-daily-price-alerts'`
       - `getAlerts(): PriceAlert[]` -- reads from localStorage, returns [] if window undefined or parse fails
       - `addAlert(alert: PriceAlert): void` -- appends if not duplicate (same modelName + sourceId), writes to localStorage
       - `removeAlert(modelName: string, sourceId: number): void` -- filters out matching alert
       - `clearAlerts(): void` -- removes the localStorage key
       - All functions guard with `typeof window === 'undefined'` check for SSR safety (Pitfall 2)

    2. **tests/lib/alerts.test.ts** -- TDD tests for the alerts utility:
       - Test getAlerts returns [] when empty
       - Test addAlert stores alert, getAlerts returns it
       - Test addAlert prevents duplicate (same modelName + sourceId)
       - Test removeAlert removes specific alert
       - Test clearAlerts removes all
       - Test getAlerts returns [] when window is undefined (SSR)
       - Use vi.stubGlobal('localStorage', ...) for mocking

    3. **app/components/AlertsPageClient.tsx** -- Client component for alert management:
       - Props: `{ alerts: PriceAlert[] }` (initial data from page, but client reads fresh from localStorage on mount via useEffect)
       - State: local alerts array, clearConfirmVisible boolean
       - Renders page heading "Price Alerts" + subheading per UI-SPEC copywriting
       - Alert count display + "Clear All" button (per UI-SPEC)
       - Each alert row: model name (text-sm font-medium), threshold price (formatPrice), "Remove" button
       - Empty state per UI-SPEC: "No alerts set" / "Visit any model page and click the bell icon to set a price alert."
       - "Clear All" shows inline confirmation: "Remove all {count} price alerts? This cannot be undone." with Confirm/Cancel buttons per UI-SPEC destructive actions
       - Remove individual: inline confirmation per UI-SPEC: "Remove alert for {modelName}? You will no longer be notified when the price drops below {threshold}."
       - Uses useEffect to read from localStorage on mount (hydration-safe)
       - Container: max-w-6xl mx-auto px-4 py-8 per UI-SPEC page layout

    4. **app/alerts/page.tsx** -- Server wrapper page:
       - Simple server component that renders AlertsPageClient
       - No database query needed (all localStorage-based per D-14)
       - export const revalidate = 60 (match existing pattern)
  </action>
  <verify>
    <automated>pnpm test tests/lib/alerts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/alerts.ts exports PriceAlert interface with modelName, sourceId, thresholdPrice, createdAt fields
    - app/lib/alerts.ts exports getAlerts, addAlert, removeAlert, clearAlerts functions
    - getAlerts() returns [] when typeof window is 'undefined' (SSR safety)
    - addAlert() prevents duplicate entries with same modelName + sourceId
    - tests/lib/alerts.test.ts passes all test cases
    - app/alerts/page.tsx exists and renders AlertsPageClient
    - AlertsPageClient shows empty state text: "No alerts set"
    - AlertsPageClient "Clear All" shows inline confirmation before executing
  </acceptance_criteria>
  <done>User can visit /alerts, see all saved alerts, remove individual alerts, and clear all alerts. Alerts persist in localStorage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->localStorage | Alert data stored in browser localStorage is user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | localStorage alerts | accept | Alerts are per-device convenience features, not security-critical. No server-side impact. |
| T-07-02 | Information Disclosure | localStorage alerts | accept | No PII stored -- only model names and price thresholds. |
</threat_model>

<verification>
- TopNav renders 6 links (Pricing, Daily Digest, Trends, Promotions, Compare, Alerts)
- /alerts page loads without errors
- Alert CRUD operations work in browser
- pnpm test passes for alerts utility
</verification>

<success_criteria>
- TopNav has navigation links to all 4 new Phase 7 pages
- /alerts page displays saved alerts with remove/clear functionality
- Alert utility passes all unit tests
- No SSR hydration mismatches on /alerts page
</success_criteria>

<output>
Create `.planning/phases/07-intelligence-analytics/07-01-SUMMARY.md` when done
</output>
