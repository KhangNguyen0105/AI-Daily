---
phase: 9-dark-mode-theme-system
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - app/components/DigestArticle.tsx
  - app/components/TrendsPageClient.tsx
  - app/components/PromotionsPageClient.tsx
  - app/components/ComparePageClient.tsx
  - app/components/AlertsPageClient.tsx
  - app/components/AlertBanner.tsx
  - app/components/PricingGrid.tsx
  - app/components/PriceHistoryChart.tsx
  - app/components/PromotionsList.tsx
  - app/components/BellIcon.tsx
  - app/components/CurrencyToggle.tsx
  - app/components/PromotionCard.tsx
  - app/components/ModelSelector.tsx
  - app/components/ComparisonCard.tsx
  - app/components/TrendChart.tsx
  - app/digest/page.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "All public page components render correctly in both light and dark themes"
    - "DigestArticle, TrendsPageClient, PromotionsPageClient, ComparePageClient, AlertsPageClient all use theme tokens"
    - "Smaller components (BellIcon, CurrencyToggle, PromotionCard, etc.) use theme tokens"
    - "Recharts hex colors (PriceHistoryChart, TrendChart) remain unchanged per D-05"
    - "No hardcoded bg-white, text-gray-*, border-gray-* remain in any public component"
  artifacts:
    - path: "app/components/DigestArticle.tsx"
      provides: "Daily digest article with theme-aware colors"
      contains: "text-text-primary"
    - path: "app/components/AlertsPageClient.tsx"
      provides: "Alerts page with theme-aware colors"
      contains: "bg-bg-secondary"
    - path: "app/components/ComparisonCard.tsx"
      provides: "Comparison card with theme-aware colors"
      contains: "border-border-primary"
    - path: "app/digest/page.tsx"
      provides: "Digest archive page with theme-aware colors"
      contains: "text-text-secondary"
  key_links:
    - from: "app/components/DigestArticle.tsx"
      to: "app/globals.css"
      via: "text-text-primary, bg-bg-secondary, border-border-primary token classes"
      pattern: "text-text-primary|bg-bg-secondary"
    - from: "app/components/AlertsPageClient.tsx"
      to: "app/globals.css"
      via: "bg-bg-primary, text-text-primary, border-border-primary token classes"
      pattern: "bg-bg-primary|text-text-primary"
---

# Plan 09-04: Public Components Color Conversion — 15 Files

**Phase:** 9 — Dark Mode & Theme System
**Wave:** 3
**Status:** pending
**Depends on:** 09-01, 09-02, 09-03

## Goal
Convert all hardcoded color classes in the remaining 15 public-facing component files to theme-aware CSS token classes.

## Tasks

### Task 1: Convert High-Occurrence Public Components (DigestArticle, AlertsPageClient, ComparisonCard, TrendsPageClient)

These 4 files have the most occurrences and should be converted first.

**DigestArticle.tsx (9 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `text-gray-500` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |
| `bg-gray-50` | `bg-bg-secondary` |

**AlertsPageClient.tsx (12 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `text-gray-500` | `text-text-secondary` |
| `text-gray-400` | `text-text-tertiary` |
| `border-gray-200` | `border-border-primary` |
| `border-gray-300` | `border-border-secondary` |
| `bg-gray-50` | `bg-bg-secondary` |
| `bg-gray-100` | `bg-bg-tertiary` |

**ComparisonCard.tsx (10 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `text-gray-500` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |
| `border-gray-300` | `border-border-secondary` |

**TrendsPageClient.tsx (8 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `text-gray-500` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |
| `bg-gray-50` | `bg-bg-secondary` |

**Steps for each file:**
1. Open the file.
2. Replace each hardcoded color class using the mapping.
3. Do NOT convert accent colors (blue-600, green-500, red-500, yellow-500) — they stay the same per D-04.
4. Verify with grep after each file.

**Verify:**
<verify>
<automated>grep -c "bg-white\|bg-gray-50\|bg-gray-100\|text-gray-900\|text-gray-600\|text-gray-500\|text-gray-400\|border-gray-200\|border-gray-300" app/components/DigestArticle.tsx app/components/AlertsPageClient.tsx app/components/ComparisonCard.tsx app/components/TrendsPageClient.tsx</automated>
</verify>

### Task 2: Convert Medium-Occurrence Public Components (PromotionsList, PromotionCard, PromotionsPageClient, ComparePageClient, ModelSelector, BellIcon)

These 6 files have 4-7 occurrences each.

**PromotionsList.tsx (7 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `text-gray-500` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |
| `bg-gray-50` | `bg-bg-secondary` |

**PromotionCard.tsx (7 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |
| `bg-gray-50` | `bg-bg-secondary` |

**PromotionsPageClient.tsx (5 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-500` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |

**ComparePageClient.tsx (6 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `text-gray-500` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |
| `border-gray-300` | `border-border-secondary` |

**ModelSelector.tsx (4 occurrences):**

| Old | New |
|-----|-----|
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `border-gray-300` | `border-border-secondary` |
| `bg-gray-50` | `bg-bg-secondary` |

**BellIcon.tsx (5 occurrences):**

| Old | New |
|-----|-----|
| `text-gray-600` | `text-text-secondary` |
| `text-gray-400` | `text-text-tertiary` |
| `bg-gray-100` | `bg-bg-tertiary` |

**Steps for each file:**
1. Open the file.
2. Replace each hardcoded color class using the mapping.
3. Verify with grep after each file.

**Verify:**
<verify>
<automated>grep -c "bg-white\|bg-gray-50\|bg-gray-100\|text-gray-900\|text-gray-600\|text-gray-500\|text-gray-400\|border-gray-200\|border-gray-300" app/components/PromotionsList.tsx app/components/PromotionCard.tsx app/components/PromotionsPageClient.tsx app/components/ComparePageClient.tsx app/components/ModelSelector.tsx app/components/BellIcon.tsx</automated>
</verify>

### Task 3: Convert Low-Occurrence Components and Special Cases (PricingGrid, PriceHistoryChart, TrendChart, CurrencyToggle, digest/page.tsx, AlertBanner.tsx)

These 6 files have 1-6 occurrences each, plus special handling for Recharts hex colors.

**AlertBanner.tsx (1 occurrence):**

| Old | New |
|-----|-----|
| `text-gray-600` | `text-text-secondary` |

**PricingGrid.tsx (2 occurrences):**

| Old | New |
|-----|-----|
| `text-gray-900` | `text-text-primary` |
| `text-gray-500` | `text-text-secondary` |

**CurrencyToggle.tsx (2 occurrences):**

| Old | New |
|-----|-----|
| `text-gray-600` | `text-text-secondary` |
| `border-gray-300` | `border-border-secondary` |

**digest/page.tsx (6 occurrences):**

| Old | New |
|-----|-----|
| `bg-white` | `bg-bg-primary` |
| `text-gray-900` | `text-text-primary` |
| `text-gray-600` | `text-text-secondary` |
| `text-gray-500` | `text-text-secondary` |
| `border-gray-200` | `border-border-primary` |

**PriceHistoryChart.tsx (2 occurrences + hex colors):**
- Convert `text-gray-*` and `border-gray-*` classes to tokens.
- **Do NOT convert Recharts hex colors** (`#3b82f6`, `#ef4444`, etc.) — these stay the same in both themes per D-05.

**TrendChart.tsx (3 occurrences + hex colors):**
- Convert `text-gray-*` and `border-gray-*` classes to tokens.
- **Do NOT convert Recharts hex colors** — same as PriceHistoryChart.

**Steps for each file:**
1. Open the file.
2. Replace hardcoded color classes using the mapping.
3. For PriceHistoryChart and TrendChart: leave hex color values in Recharts `stroke`/`fill` props unchanged.
4. Verify with grep after each file.

**Verify:**
<verify>
<automated>grep -c "bg-white\|bg-gray-50\|bg-gray-100\|text-gray-900\|text-gray-600\|text-gray-500\|text-gray-400\|border-gray-200\|border-gray-300" app/components/PricingGrid.tsx app/components/PriceHistoryChart.tsx app/components/TrendChart.tsx app/components/CurrencyToggle.tsx app/digest/page.tsx app/components/AlertBanner.tsx</automated>
</verify>

## Files Changed

| File | Action | Occurrences |
|------|--------|-------------|
| `app/components/DigestArticle.tsx` | Modify | 9 |
| `app/components/TrendsPageClient.tsx` | Modify | 8 |
| `app/components/AlertsPageClient.tsx` | Modify | 12 |
| `app/components/ComparisonCard.tsx` | Modify | 10 |
| `app/components/PromotionsList.tsx` | Modify | 7 |
| `app/components/PromotionCard.tsx` | Modify | 7 |
| `app/components/PromotionsPageClient.tsx` | Modify | 5 |
| `app/components/ComparePageClient.tsx` | Modify | 6 |
| `app/components/ModelSelector.tsx` | Modify | 4 |
| `app/components/BellIcon.tsx` | Modify | 5 |
| `app/components/PricingGrid.tsx` | Modify | 2 |
| `app/components/PriceHistoryChart.tsx` | Modify | 2 |
| `app/components/TrendChart.tsx` | Modify | 3 |
| `app/components/CurrencyToggle.tsx` | Modify | 2 |
| `app/digest/page.tsx` | Modify | 6 |
| `app/components/AlertBanner.tsx` | Modify | 1 |

## Verification

1. Run grep across all 15 files — each returns 0 for hardcoded color classes
2. `grep -c "#3b82f6\|#ef4444\|#22c55e" app/components/PriceHistoryChart.tsx app/components/TrendChart.tsx` — hex colors still present (unchanged per D-05)
3. App starts with `npm run dev` without errors
4. All public pages (Digest, Trends, Promotions, Compare, Alerts) render correctly in both themes
5. Recharts charts display with correct colors in both themes

## Success Criteria

- [ ] All 16 public component files have 0 remaining hardcoded color classes
- [ ] Recharts hex colors in PriceHistoryChart and TrendChart remain unchanged
- [ ] All accent colors (blue, green, red, yellow) remain unchanged
- [ ] All public pages render correctly in both light and dark themes

## Output

Create `.planning/phases/09-dark-mode-theme-system/09-04-SUMMARY.md` when done
