---
phase: 03-pricing-comparison-table
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/pricing-rows.ts
  - tests/components/pricing-table.test.tsx
  - tests/components/home-page-client.test.tsx
autonomous: true
requirements:
  - PRIC-01
  - PRIC-02
  - PRIC-03
  - PRIC-04
  - PRIC-05
  - PRIC-06
  - PRIC-07
  - FRNT-03
  - FRNT-04
user_setup: []
must_haves:
  truths:
    - "Wave 0 tests fail before implementation and cover PRIC-01 through PRIC-07 plus FRNT-04."
    - "PricingTable component tests cover sorting, search, provider/free-tier/price/context filters, confidence color-only pill badges, source links with visible provider-name text plus href/target/rel/title URL tooltip, 50-row pagination, and USD/VND conversion per D-12 through D-16, D-26, D-27, D-31, D-32, D-33, and D-34."
    - "HomePageClient component tests cover side-by-side 60/40 layout, shared currency state, last/next run metadata, stale warning, and error banner per D-01 through D-10, D-18, D-23, D-24, D-31, and D-32."
  artifacts:
    - path: "tests/fixtures/pricing-rows.ts"
      provides: "Reusable 10-15 row PricingRow fixture set with providers, families, confidence states, null prices, free tiers, duplicates, and stale timestamps"
      exports: ["pricingRowsFixture", "manyPricingRowsFixture", "stalePricingRowsFixture"]
    - path: "tests/components/pricing-table.test.tsx"
      provides: "Vitest and Testing Library coverage for PricingTable user interactions"
    - path: "tests/components/home-page-client.test.tsx"
      provides: "Vitest and Testing Library coverage for HomePageClient layout, freshness, stale/error states, and shared currency"
  key_links:
    - from: "tests/components/pricing-table.test.tsx"
      to: "app/components/PricingTable.tsx"
      via: "render PricingTable with pricingRowsFixture and userEvent interactions"
      pattern: "render\\(<PricingTable"
    - from: "tests/components/home-page-client.test.tsx"
      to: "app/components/HomePageClient.tsx"
      via: "render HomePageClient with metadata props and shared currency"
      pattern: "render\\(<HomePageClient"
---

## Phase Goal

**As a** developer comparing AI models, **I want to** view, sort, filter, search, and currency-toggle trustworthy model pricing data, **so that** I can quickly choose models based on real cost, freshness, and source confidence.

<objective>
Create the Wave 0 validation layer for the pricing comparison table replan.

Purpose: The implementation plans must be driven by failing user-behavior tests before source changes. This plan establishes component fixtures and automated tests for the exact locked decisions in 03-CONTEXT.md and the visual contract in 03-UI-SPEC.md.

Output: Reusable pricing fixtures plus focused PricingTable and HomePageClient component tests using Vitest, jsdom, Testing Library, and userEvent.
</objective>

<execution_context>
@C:/Users/KHANG/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/KHANG/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-pricing-comparison-table/03-CONTEXT.md
@.planning/phases/03-pricing-comparison-table/03-VALIDATION.md
@.planning/phases/03-pricing-comparison-table/03-PATTERNS.md
@.planning/phases/03-pricing-comparison-table/03-UI-SPEC.md
@app/components/PricingTable.tsx
@app/components/HomePageClient.tsx
@app/components/CostCalculator.tsx
@tests/cost-calculator.test.tsx
@tests/components/pricing-grid.test.tsx
@tests/setup.ts
</context>

## Artifacts this phase produces

- `tests/fixtures/pricing-rows.ts`: `pricingRowsFixture`, `manyPricingRowsFixture`, and `stalePricingRowsFixture` using the canonical `PricingRow` shape.
- `tests/components/pricing-table.test.tsx`: interaction tests for the full pricing table surface.
- `tests/components/home-page-client.test.tsx`: layout and metadata tests for the page client wrapper.
- User-facing states specified by tests: loading skeleton, empty data, filter-empty state, load error, stale data warning, last updated metadata, next run metadata, source links, confidence pills, and currency updates.

<source_coverage>
Phase-wide source audit: GOAL is covered by plans 03-02 through 03-05. REQ PRIC-01, PRIC-02, PRIC-03, PRIC-04, PRIC-05, PRIC-06, PRIC-07, FRNT-03, and FRNT-04 are covered across plans 03-01 through 03-05. RESEARCH items are covered: TanStack client-side sorting/filtering/pagination in 03-03, Drizzle latest-row deduplication in 03-02, Tailwind-only/no shadcn implementation in 03-05, existing exchange-rate worker path in 03-04, and validation Wave 0 in 03-01. CONTEXT D-01 through D-10 are covered in 03-05, D-11 through D-20 in 03-03 and 03-05, D-21 through D-28 in 03-02 through 03-04, and D-29 through D-34 in 03-01 and 03-05. Deferred ideas: none.
</source_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create realistic PricingRow fixtures</name>
  <files>tests/fixtures/pricing-rows.ts</files>
  <read_first>
@app/components/PricingTable.tsx
@tests/cost-calculator.test.tsx
@.planning/phases/03-pricing-comparison-table/03-VALIDATION.md
  </read_first>
  <behavior>
    - Fixture rows use the exported `PricingRow` type.
    - Fixture data includes at least 12 rows across OpenAI, Anthropic, Google, Mistral, DeepSeek, Amazon Bedrock, and an unknown provider.
    - Fixture data includes GPT, Claude, Gemini, Mistral, Llama, and Other model families.
    - Fixture data includes all confidence values: `verified`, `likely`, and `low_confidence`.
    - Fixture data includes free-tier rows where both input and output prices are 0.
    - Fixture data includes null input/output price rows for `N/A` and tooltip coverage.
    - Fixture data includes duplicate model+provider rows with different `collectedAt` values for later deduplication tests.
    - `manyPricingRowsFixture` contains more than 50 rows for D-12 pagination coverage.
  </behavior>
  <action>
Create `tests/fixtures/pricing-rows.ts` per D-34. Export `pricingRowsFixture`, `manyPricingRowsFixture`, and `stalePricingRowsFixture`. Keep the fixture values deterministic and realistic: include prices per 1M tokens, context windows such as 128000 and 1000000, valid `http` or `https` source URLs, and `Date` objects. Include duplicate rows for the same `modelName` and `sourceId` so plan 03-02 can prove latest-row deduplication per D-21 and D-22. Do not import production DB modules from this fixture.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/pricing-table.test.tsx --runInBand</automated>
  </verify>
  <acceptance_criteria>Fixture exports compile, cover all row-state cases listed above, and are reusable by both PricingTable and HomePageClient tests.</acceptance_criteria>
  <done>`tests/fixtures/pricing-rows.ts` exists and provides typed deterministic data for all Phase 3 component tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing PricingTable interaction tests</name>
  <files>tests/components/pricing-table.test.tsx</files>
  <read_first>
@tests/fixtures/pricing-rows.ts
@app/components/PricingTable.tsx
@app/lib/pricing-utils.ts
@app/lib/provider-metadata.ts
@.planning/phases/03-pricing-comparison-table/03-UI-SPEC.md
  </read_first>
  <behavior>
    - PRIC-01: table renders Provider, Model, Family, Input, Output, Context Window, Confidence, Source, and Last Updated/Collected columns.
    - PRIC-01/D-16: clicking sortable headers cycles ascending, descending, and unsorted; tests cover provider, input price, output price, and context window sorting.
    - PRIC-02: provider logos or fallback initials render beside provider names, and model family values render.
    - PRIC-03/D-14: global search matches model name, provider name, and derived model family.
    - PRIC-04/D-13: provider, free-tier, input price, output price, and context window filters compose together and Clear filters resets them.
    - PRIC-05/D-27: source links show provider names as visible text, never raw URLs; `href` is the source URL, `target="_blank"`, `rel` includes `noopener noreferrer`, and `title`/hover text exposes the URL.
    - PRIC-06/D-15: confidence appears as color-only pill badges for verified, likely, and low_confidence states, with no on-screen confidence-state text labels and no icons; accessible state may use non-visible `aria-label` or `title`.
    - PRIC-07/D-28: switching USD/VND updates input/output prices in place without resetting filters or pagination.
    - D-12: pagination uses 50 rows per page and exposes Previous, Next, and numeric page controls.
    - D-17, D-19, D-26: empty, loading skeleton, filter-empty, and null-price states render the specified copy or accessible tooltip/title.
  </behavior>
  <action>
Create `tests/components/pricing-table.test.tsx` with `// @vitest-environment jsdom`. Use Testing Library queries by role and visible text. Use `userEvent.setup()` for search typing, checkbox clicks, select changes, sort-header clicks, currency toggle clicks, pagination, and Clear filters. Keep assertions user-facing rather than implementation-specific except for required Tailwind state classes from the UI contract. For D-27, assert each valid source link has provider-name visible text, `href` equal to the fixture `sourceUrl`, `target="_blank"`, `rel` containing `noopener noreferrer`, and a `title` exposing the URL without rendering the raw URL as visible text. For D-15, assert confidence indicators are color-only pills with no on-screen confidence-state strings or icons; when 03-UI-SPEC asks for label text, the locked D-15 context decision wins and only non-visible `aria-label` or `title` may describe the state. These tests are expected to fail before plans 03-03 and 03-05 are implemented.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/pricing-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>The test file exists and exercises PRIC-01 through PRIC-07 behaviors with realistic fixtures. Failures before implementation are acceptable for Wave 0.</acceptance_criteria>
  <done>PricingTable has explicit failing coverage for table rendering, sorting, filtering, search, source attribution, confidence badges, pagination, currency conversion, loading, empty, filter-empty, and null-price states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add failing HomePageClient layout and freshness tests</name>
  <files>tests/components/home-page-client.test.tsx</files>
  <read_first>
@tests/fixtures/pricing-rows.ts
@app/components/HomePageClient.tsx
@app/components/CostCalculator.tsx
@.planning/phases/03-pricing-comparison-table/03-CONTEXT.md
@.planning/phases/03-pricing-comparison-table/03-UI-SPEC.md
  </read_first>
  <behavior>
    - D-01/D-02: HomePageClient renders PricingTable left and CostCalculator right with `xl:w-[60%]` and `xl:w-[40%]`, stacked below xl.
    - D-03: CostCalculator wrapper includes `xl:sticky xl:top-4`.
    - D-07/D-09/D-10: PricingTable and CostCalculator wrappers use bordered white Tailwind containers and `gap-6`.
    - D-23/FRNT-04: Last updated and next run metadata render with the UI-SPEC fallback copy when dates are unavailable.
    - D-24: stale data older than 24 hours renders the amber stale warning copy.
    - D-18: load error renders a warning banner with "Unable to load pricing data" and a retry button.
    - D-31/D-32: shared currency state updates both PricingTable and CostCalculator in one interaction.
  </behavior>
  <action>
Create `tests/components/home-page-client.test.tsx` with `// @vitest-environment jsdom`. Render `HomePageClient` with the fixture data and props for `exchangeRate`, `lastUpdated`, `nextRunAt`, `isStale`, and `loadError`. Use visible copy from 03-UI-SPEC for Last updated, Next run, stale warning, and error state. Validate the layout classes specified in D-01 through D-10 by querying the closest wrapper elements. Verify the shared currency behavior by clicking the VND toggle inside PricingTable and asserting CostCalculator amounts also show VND formatting.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/home-page-client.test.tsx</automated>
  </verify>
  <acceptance_criteria>The test file exists and fails until HomePageClient exposes the required layout, freshness metadata, stale/error states, and shared currency behavior.</acceptance_criteria>
  <done>HomePageClient has explicit failing coverage for side-by-side layout, sticky calculator, freshness metadata, stale warning, load error, and shared currency state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture -> component render | Fixture data simulates DB-collected provider data and may include malicious names or bad URLs for validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | PricingRow fixture data | mitigate | Include bidi/unsafe-name fixture cases and assert production rendering uses `sanitizeDisplayName()` rather than raw display text. |
| T-03-01-02 | Information Disclosure | Source-link tests | mitigate | Assert links use provider-name visible text, source URL `href`, URL `title` tooltip, `target="_blank"`, and `rel` containing `noopener noreferrer`. |
| T-03-SC | Tampering | npm installs | accept | No package-manager install is planned; Vitest, Testing Library, userEvent, jsdom, and TanStack Table already exist in package.json and RESEARCH.md legitimacy audit. |
</threat_model>

<verification>
- `pnpm vitest run tests/components/pricing-table.test.tsx`
- `pnpm vitest run tests/components/home-page-client.test.tsx`
- `pnpm vitest run tests/pricing-utils.test.ts tests/provider-metadata.test.ts`
</verification>

<success_criteria>
Wave 0 is complete when the fixture and both component test files exist, compile under Vitest/jsdom, and encode all required Phase 3 behaviors even if implementation failures remain.
</success_criteria>

<output>
Create `.planning/phases/03-pricing-comparison-table/03-01-SUMMARY.md` when done.
</output>
