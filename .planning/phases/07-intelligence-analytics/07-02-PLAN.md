---
phase: 07-intelligence-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/trends/page.tsx
  - app/components/TrendsPageClient.tsx
  - app/components/TrendChart.tsx
autonomous: true
requirements:
  - INTL-01
  - INTL-02
must_haves:
  truths:
    - "User can view a list of all models with pricing trend data"
    - "User can click a model to see its pricing trend chart"
    - "Trend chart shows input and output price lines over time"
    - "Price drops are marked with green dots, increases with red dots"
    - "New model launches are marked with a star icon"
  artifacts:
    - path: "app/trends/page.tsx"
      provides: "Server component fetching trend data via Drizzle"
    - path: "app/components/TrendsPageClient.tsx"
      provides: "Client wrapper with model list and chart display"
    - path: "app/components/TrendChart.tsx"
      provides: "Recharts dual-line chart with visual markers"
  key_links:
    - from: "app/trends/page.tsx"
      to: "src/db/schema.ts"
      via: "Drizzle query on extractions table"
      pattern: "db\\.select.*from.*extractions"
    - from: "app/components/TrendsPageClient.tsx"
      to: "app/components/TrendChart.tsx"
      via: "import and render TrendChart"
      pattern: "import.*TrendChart"
    - from: "app/components/TrendChart.tsx"
      to: "recharts"
      via: "LineChart, Line components"
      pattern: "import.*recharts"
---

<objective>
Build the /trends page with per-model pricing trend charts showing price changes over time, with visual markers for price drops, increases, and new model launches.

Purpose: Delivers INTL-01 (pricing trend charts) and INTL-02 (highlight price drops and new launches) as a complete vertical slice.
Output: Working /trends page with model list, trend chart, and visual markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-intelligence-analytics/07-CONTEXT.md
@.planning/phases/07-intelligence-analytics/07-RESEARCH.md
@.planning/phases/07-intelligence-analytics/07-UI-SPEC.md

@app/components/PriceHistoryChart.tsx
@app/components/ModelDetailClient.tsx
@app/page.tsx
@app/lib/pricing-utils.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TrendChart component with visual markers</name>
  <files>app/components/TrendChart.tsx, tests/components/trend-chart.test.tsx</files>
  <read_first>
    - app/components/PriceHistoryChart.tsx (existing Recharts pattern to extend -- LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer)
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-02: per-model charts, D-03: all data no time range, D-04: visual markers, D-05: Recharts)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (chart colors, interaction patterns, chart height 300px, responsive)
    - .planning/phases/07-intelligence-analytics/07-RESEARCH.md (TrendChart with markers code example, Pitfall 1: custom dot performance, Pitfall 5: data ordering)
  </read_first>
  <behavior>
    - TrendChart renders a dual-line Recharts LineChart (input price blue, output price red)
    - Empty state when no data: "No pricing data available yet."
    - Single data point state: "Only 1 data point collected. Trend chart will appear after the next collection."
    - Data is sorted chronologically (oldest first) before rendering
    - Price drops marked with green dot (#16a34a, radius 6)
    - Price increases marked with red dot (#dc2626, radius 6)
    - First data point marked with amber star (#f59e0b)
    - Custom dot components defined outside render function (Pitfall 1)
    - Chart height is 300px with ResponsiveContainer
  </behavior>
  <action>
    Create app/components/TrendChart.tsx extending the PriceHistoryChart pattern:

    - Export `TrendPoint` interface: `{ collectedAt: Date; inputPricePer1m: number | null; outputPricePer1m: number | null }`
    - Export `TrendChart` component accepting `{ data: TrendPoint[]; modelName: string }`
    - Build chart data by:
      1. Filter points where at least one price is non-null
      2. Sort chronologically by collectedAt ascending (Pitfall 5: Recharts expects oldest-first)
      3. For each point, compute flags: isInputDrop, isInputIncrease, isOutputDrop, isOutputIncrease, isFirstPoint by comparing to previous point
      4. Format date as 'MMM d' using date-fns format()
    - Use same Recharts imports as PriceHistoryChart: LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer
    - Define custom dot components as named functions outside the TrendChart render (Pitfall 1):
      - `InputDot`: green circle for drops, red for increases, blue default
      - `OutputDot`: same logic for output price
      - Star SVG for isFirstPoint (amber #f59e0b)
    - Use `<Line dot={InputDot} />` and `<Line dot={OutputDot} />` with activeDot for hover
    - Chart height: 300px, ResponsiveContainer width="100%"
    - Tooltip shows date, input price, output price
    - Legend shows "Input $/1M" (blue) and "Output $/1M" (red)
    - Empty and single-point states match existing PriceHistoryChart messages

    Create tests/components/trend-chart.test.tsx:
    - Test renders empty state when data is empty
    - Test renders single-point state when data has 1 point
    - Test renders chart with 2+ data points
    - Test chart data is sorted chronologically regardless of input order
    - Test custom dots render for price changes (check SVG circle fill colors)
  </action>
  <verify>
    <automated>pnpm test tests/components/trend-chart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TrendChart.tsx exports TrendChart component and TrendPoint interface
    - Chart renders dual lines: input (#3b82f6) and output (#ef4444)
    - Empty state displays "No pricing data available yet."
    - Single point state displays "Only 1 data point collected."
    - Data sorted ascending by collectedAt before chart rendering
    - Custom dot components defined as standalone functions (not inline arrows)
    - tests/components/trend-chart.test.tsx passes
  </acceptance_criteria>
  <done>TrendChart component renders dual-line chart with green/red/amber markers for price changes and new launches</done>
</task>

<task type="auto">
  <name>Task 2: Create /trends page with model list and chart display</name>
  <files>app/trends/page.tsx, app/components/TrendsPageClient.tsx</files>
  <read_first>
    - app/page.tsx (server component pattern: ISR revalidate, try/catch Drizzle query, pass data to client)
    - app/digest/page.tsx (searchParams pattern for pagination)
    - app/components/HomePageClient.tsx (client wrapper pattern)
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-01: /trends route, D-02: per-model charts, D-03: all data)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (/trends page layout, model list grid, empty states, spacing)
    - .planning/phases/07-intelligence-analytics/07-RESEARCH.md (server component pattern, project structure)
    - src/db/schema.ts (extractions table columns)
    - app/lib/pricing-utils.ts (sanitizeDisplayName, formatPrice)
  </read_first>
  <action>
    Create two files for the /trends page:

    1. **app/trends/page.tsx** -- Server component per D-01:
       - `export const revalidate = 60` (ISR, matches existing pattern)
       - Query Drizzle for distinct models with their price history:
         - Select: modelName, sourceId, sourceName (from sources JOIN), and collect all history points (collectedAt, inputPricePer1m, outputPricePer1m)
         - Group by modelName + sourceId to get one entry per model
         - For each model, fetch its price history ordered by collectedAt ascending
       - Two-step approach: first get distinct model+source pairs, then for each get history (or single query with all data grouped client-side)
       - Simpler approach: single query fetching all extractions with source names, group by modelName+sourceId in JS
       - Try/catch with empty array fallback (existing pattern)
       - Pass `{ models: TrendModelData[] }` to TrendsPageClient

    2. **app/components/TrendsPageClient.tsx** -- Client wrapper:
       - Props: `{ models: Array<{ modelName: string; sourceId: number; sourceName: string | null; history: TrendPoint[] }> }`
       - State: `selectedModelIndex: number | null` (null shows model list, number shows chart)
       - Page layout per UI-SPEC: max-w-6xl mx-auto px-4 py-8
       - Heading: "Pricing Trends" + subheading per UI-SPEC copywriting
       - When no model selected: render model list as a grid (1-col mobile, 2-col md, 3-col lg)
         - Each card: model name (text-sm font-medium), source name (text-xs text-gray-500), mini indicator of data points count
         - Cards are clickable, set selectedModelIndex
         - Empty state per UI-SPEC: "No trend data yet" / "Pricing trends will appear after the pipeline collects data over multiple days."
       - When model selected: render back button ("Back to all models") + TrendChart with selected model's history
       - Model name display uses sanitizeDisplayName from pricing-utils
  </action>
  <verify>
    <automated>grep -c "TrendsPageClient\|TrendChart" app/trends/page.tsx app/components/TrendsPageClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/trends/page.tsx exports default async function with revalidate = 60
    - Server component queries extractions JOIN sources and groups by modelName+sourceId
    - TrendsPageClient renders model grid when no model selected
    - TrendsPageClient renders TrendChart when model is selected
    - Back button returns to model list view
    - Empty state shows "No trend data yet" message
    - Page uses max-w-6xl mx-auto px-4 py-8 container per UI-SPEC
  </acceptance_criteria>
  <done>User can visit /trends, see a list of models, click one to view its pricing trend chart with visual markers for price changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server->client | Price data from database passed to client component (read-only, no trust issue) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | URL parameters | accept | No URL parameters on /trends page. Model selection is client-side state. |
</threat_model>

<verification>
- /trends page loads without errors
- Model list displays all models with history data
- Clicking a model shows its trend chart
- Chart shows green dots for price drops, red for increases
- Empty state displays when no data available
</verification>

<success_criteria>
- /trends page shows model list with pricing trend data
- Per-model trend chart displays dual lines (input/output) with visual markers
- Price drops marked green, increases marked red, first point marked amber star
- Empty states match UI-SPEC copywriting
- Data sorted chronologically (oldest first) for correct chart rendering
</success_criteria>

<output>
Create `.planning/phases/07-intelligence-analytics/07-02-SUMMARY.md` when done
</output>
