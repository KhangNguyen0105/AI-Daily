---
phase: 01-foundation-pipeline-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/providers/base.ts
  - src/providers/registry.ts
  - src/providers/openai/config.ts
  - src/providers/openai/adapter.ts
  - tests/adapter.test.ts
  - tests/openai-adapter.test.ts
autonomous: true
requirements:
  - DCOL-06
must_haves:
  truths:
    - "Developer can import ProviderAdapter from src/providers/base and it is an abstract class that cannot be instantiated"
    - "Developer can call registerAdapter() and getAdapter('openai') returns a working OpenAIAdapter instance"
    - "Developer can call adapter.crawl() and it returns a CrawlResult with url, html, and crawledAt fields"
    - "Developer can call adapter.extract(html) and it returns an array of ExtractionResult objects"
    - "Developer can call adapter.normalize(extractions) and it returns extractions with confidence set to 'likely'"
    - "Developer can call getAllAdapters() and it returns an array containing the OpenAI adapter"
  artifacts:
    - path: "src/providers/base.ts"
      provides: "Abstract ProviderAdapter class and type interfaces"
      exports: ["ProviderAdapter", "ProviderConfig", "CrawlResult", "ExtractionResult"]
    - path: "src/providers/registry.ts"
      provides: "Adapter registry with register/get/getAll functions"
      exports: ["registerAdapter", "getAdapter", "getAllAdapters"]
    - path: "src/providers/openai/adapter.ts"
      provides: "OpenAI provider adapter"
      exports: ["OpenAIAdapter"]
    - path: "src/providers/openai/config.ts"
      provides: "OpenAI adapter configuration (URL, selectors)"
      contains: "pricingUrl"
  key_links:
    - from: "src/providers/openai/adapter.ts"
      to: "src/providers/base.ts"
      via: "extends ProviderAdapter"
      pattern: "extends ProviderAdapter"
    - from: "src/providers/registry.ts"
      to: "src/providers/openai/adapter.ts"
      via: "explicit import and registerAdapter call"
      pattern: "registerAdapter.*OpenAIAdapter"
    - from: "src/providers/openai/adapter.ts"
      to: "crawlee"
      via: "PlaywrightCrawler import"
      pattern: "PlaywrightCrawler"
---

<objective>
Provider adapter pattern with abstract base class, explicit registry, and a working OpenAI adapter that can crawl the pricing page, extract structured data via AI, and normalize to standard format.

Purpose: Establishes the adapter pattern (DCOL-06) that all future providers extend. Proves the crawl → extract → normalize flow works with a real provider.

Output: OpenAI adapter that Crawlee can use to fetch pricing page HTML, Vercel AI SDK can extract structured pricing from it, and normalization produces standard ExtractionResult objects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-pipeline-core/01-CONTEXT.md
@.planning/phases/01-foundation-pipeline-core/01-RESEARCH.md
@.planning/phases/01-foundation-pipeline-core/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. From Plan 01 schema. -->

From src/db/schema.ts:
```typescript
export const confidenceEnum = pgEnum('confidence', ['verified', 'likely', 'low_confidence']);
export const sources = pgTable('sources', { id, name, url, provider_type, is_active, created_at, updated_at });
export const rawData = pgTable('raw_data', { id, source_id, url, evidence, crawled_at, created_at, updated_at });
export const extractions = pgTable('extractions', { id, raw_data_id, source_id, model_name, input_price_per_1m, output_price_per_1m, context_window, confidence, raw_evidence, collected_at, created_at, updated_at });
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create provider adapter base class, types, and registry</name>
  <files>src/providers/base.ts, src/providers/registry.ts</files>
  <read_first>
    - .planning/phases/01-foundation-pipeline-core/01-RESEARCH.md (Pattern 1: ProviderAdapter base class example, Pattern 2: Explicit registry example)
    - .planning/phases/01-foundation-pipeline-core/01-CONTEXT.md (D-01: abstract base class; D-02: crawl/extract/normalize; D-03: explicit import registry; D-04: base handles infra, adapter handles config)
    - src/db/schema.ts (confidenceEnum values for ExtractionResult type)
  </read_first>
  <action>
Create the provider adapter pattern per D-01, D-02, D-03, D-04.

1. Create `src/providers/base.ts`:
   - Export interface `ProviderConfig` with: name (string), baseUrl (string), pricingUrl (string)
   - Export interface `CrawlResult` with: url (string), html (string), crawledAt (Date)
   - Export interface `ExtractionResult` with: modelName (string), inputPricePer1m (number | null), outputPricePer1m (number | null), contextWindow (number | null), confidence ('verified' | 'likely' | 'low_confidence'), rawEvidence (string)
   - Export abstract class `ProviderAdapter`:
     - abstract config: ProviderConfig
     - abstract extract(html: string): Promise<ExtractionResult[]>
     - abstract normalize(extractions: ExtractionResult[]): ExtractionResult[]
     - Default crawl() method implementation using Crawlee PlaywrightCrawler:
       - Creates PlaywrightCrawler with maxRequestsPerCrawl: 1, headless: true
       - In requestHandler: captures page.content() into a CrawlResult
       - Runs crawler with [this.config.pricingUrl]
       - Returns the CrawlResult or throws on failure
     - Per D-04: base class handles infrastructure (the Crawlee crawler setup)

2. Create `src/providers/registry.ts`:
   - Import ProviderAdapter from './base'
   - Create a Map<string, ProviderAdapter> for storage
   - Export function registerAdapter(adapter: ProviderAdapter): void -- adds to map by config.name
   - Export function getAdapter(name: string): ProviderAdapter | undefined -- retrieves from map
   - Export function getAllAdapters(): ProviderAdapter[] -- returns all values as array
   - Do NOT import or register OpenAI here yet -- that happens in Task 2
   - Per D-03: explicit import registry, no dynamic loading
  </action>
  <verify>
    <automated>pnpm vitest run tests/adapter.test.ts 2>&1</automated>
  </verify>
  <behavior>
    - ProviderAdapter is an abstract class that cannot be instantiated directly
    - ProviderAdapter has abstract methods: extract(html), normalize(extractions)
    - ProviderAdapter has a concrete crawl() method that uses PlaywrightCrawler
    - ProviderConfig interface requires name, baseUrl, pricingUrl
    - CrawlResult interface requires url, html, crawledAt
    - ExtractionResult interface requires modelName, confidence, and optional pricing fields
    - Registry's registerAdapter stores adapter by config.name
    - Registry's getAdapter returns undefined for unknown names
    - Registry's getAllAdapters returns empty array when no adapters registered
  </behavior>
  <acceptance_criteria>
    - src/providers/base.ts exports abstract class ProviderAdapter
    - src/providers/base.ts exports interfaces ProviderConfig, CrawlResult, ExtractionResult
    - ProviderAdapter.extract is abstract and returns Promise<ExtractionResult[]>
    - ProviderAdapter.normalize is abstract and returns ExtractionResult[]
    - ProviderAdapter.crawl is a concrete method (not abstract) that creates PlaywrightCrawler
    - ExtractionResult.confidence type is 'verified' | 'likely' | 'low_confidence' (matching confidenceEnum)
    - src/providers/registry.ts exports registerAdapter, getAdapter, getAllAdapters
    - Registry uses explicit Map<string, ProviderAdapter> storage (no dynamic import)
    - tests/adapter.test.ts passes: ProviderAdapter contract verified, registry store/retrieve works
  </acceptance_criteria>
  <done>Abstract base class defines the crawl/extract/normalize contract. Registry stores and retrieves adapters by name. Tests verify the contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement OpenAI provider adapter with crawl, extract, and normalize</name>
  <files>src/providers/openai/config.ts, src/providers/openai/adapter.ts, tests/openai-adapter.test.ts</files>
  <read_first>
    - src/providers/base.ts (ProviderAdapter abstract class, interfaces -- must extend correctly)
    - src/providers/registry.ts (registerAdapter function -- must register OpenAI adapter)
    - .planning/phases/01-foundation-pipeline-core/01-RESEARCH.md (Pattern 1: OpenAI adapter example, code example for Crawlee PlaywrightCrawler)
    - .planning/phases/01-foundation-pipeline-core/01-CONTEXT.md (D-13: OpenAI is first provider; D-14: core pricing data only; D-16: Playwright for all pages)
    - src/db/schema.ts (confidenceEnum values)
  </read_first>
  <action>
Implement the OpenAI provider adapter per D-13, D-14, D-16.

1. Create `src/providers/openai/config.ts`:
   - Export const `openaiConfig` of type ProviderConfig:
     - name: 'openai'
     - baseUrl: 'https://openai.com'
     - pricingUrl: 'https://developers.openai.com/api/docs/pricing'

2. Create `src/providers/openai/adapter.ts`:
   - Import ProviderAdapter, ExtractionResult from '../base'
   - Import openaiConfig from './config'
   - Export class `OpenAIAdapter extends ProviderAdapter`:
     - config = openaiConfig
     - crawl() method: override the base class default. Use PlaywrightCrawler from crawlee with maxRequestsPerCrawl: 1, headless: true. In requestHandler: wait for page to load, call page.content() to get HTML, construct CrawlResult with url, html, crawledAt. Run with [this.config.pricingUrl]. Throw if result is null.
     - extract(html: string) method: Use Vercel AI SDK generateObject() with a Zod schema to parse pricing data from the HTML. The Zod schema should define an array of objects with: modelName (string), inputPricePer1m (number), outputPricePer1m (number), contextWindow (number). Use @ai-sdk/openai provider. The prompt should instruct the AI to extract all model pricing from the HTML, returning structured data. Handle errors gracefully -- return empty array on failure, log error. Per D-14: extract core pricing data only (model name, input price, output price, context window).
     - normalize(extractions: ExtractionResult[]) method: Map each extraction to set confidence to 'likely' (default for AI-extracted data -- Phase 2 adds real scoring). Return the mapped array.

3. Update `src/providers/registry.ts`:
   - Add import: `import { OpenAIAdapter } from './openai/adapter'`
   - Add at bottom: `registerAdapter(new OpenAIAdapter())`
   - Per D-03: explicit import, not dynamic

4. Create `tests/openai-adapter.test.ts`:
   - Test: OpenAIAdapter.config.name === 'openai'
   - Test: OpenAIAdapter.config.pricingUrl contains 'openai.com'
   - Test: getAdapter('openai') returns the registered OpenAIAdapter instance
   - Test: getAllAdapters() includes the OpenAI adapter
   - Do NOT test actual crawl or AI extraction in unit tests (requires network + API keys). Mark those as integration tests to run manually.
  </action>
  <verify>
    <automated>pnpm vitest run tests/openai-adapter.test.ts 2>&1</automated>
  </verify>
  <behavior>
    - OpenAIAdapter.config.name === 'openai'
    - OpenAIAdapter.config.pricingUrl === 'https://developers.openai.com/api/docs/pricing'
    - getAdapter('openai') returns an instance of OpenAIAdapter
    - getAllAdapters() includes the OpenAI adapter
    - OpenAIAdapter.extract returns Promise<ExtractionResult[]>
    - OpenAIAdapter.normalize sets confidence to 'likely' for all extractions
  </behavior>
  <acceptance_criteria>
    - src/providers/openai/config.ts exports openaiConfig with name 'openai' and pricingUrl containing 'openai.com'
    - src/providers/openai/adapter.ts exports class OpenAIAdapter that extends ProviderAdapter
    - OpenAIAdapter has methods crawl, extract, and normalize (verified by tests)
    - OpenAIAdapter.normalize sets confidence to 'likely' for all items
    - src/providers/registry.ts imports OpenAIAdapter and calls registerAdapter(new OpenAIAdapter())
    - getAdapter('openai') returns an OpenAIAdapter instance (verified by test)
    - tests/openai-adapter.test.ts passes with 0 failures
  </acceptance_criteria>
  <done>OpenAI adapter implements crawl/extract/normalize. Registry registers it. Tests verify config and registration. The adapter is ready for the BullMQ pipeline to invoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → External site | Crawlee fetches from openai.com -- untrusted HTML content |
| Worker → AI API | Vercel AI SDK sends HTML to LLM for extraction -- prompt injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | AI extraction output | mitigate | Zod schema validates extraction output shape; invalid data rejected before DB insert |
| T-02-02 | Information Disclosure | API keys | mitigate | OPENAI_API_KEY loaded from .env, never logged or stored in raw_data |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Packages verified in Plan 01; no new installs in this plan |
</threat_model>

<verification>
- `pnpm vitest run tests/adapter.test.ts` passes
- `pnpm vitest run tests/openai-adapter.test.ts` passes
- `getAdapter('openai')` returns an OpenAIAdapter instance
- OpenAIAdapter has all three methods: crawl, extract, normalize
- Registry imports OpenAIAdapter explicitly (no dynamic loading)
    - Source-code checks (manual grep): OpenAIAdapter.crawl imports PlaywrightCrawler from 'crawlee'; OpenAIAdapter.extract imports generateObject from 'ai'
</verification>

<success_criteria>
- Provider adapter base class defines the crawl/extract/normalize contract
- Explicit registry stores and retrieves adapters by name
- OpenAI adapter implements all three methods with real Crawlee and Vercel AI SDK code
- Tests pass for adapter contract and OpenAI registration
</success_criteria>

<output>
Create `.planning/phases/01-foundation-pipeline-core/01-02-SUMMARY.md` when done
</output>
