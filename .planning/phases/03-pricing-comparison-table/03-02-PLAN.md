---
phase: 03-pricing-comparison-table
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/components/PricingTable.tsx
  - app/lib/provider-metadata.ts
  - public/logos/openai.svg
  - public/logos/anthropic.svg
  - public/logos/google.svg
  - public/logos/mistral.svg
  - public/logos/cohere.svg
  - public/logos/groq.svg
  - public/logos/together.svg
  - public/logos/perplexity.svg
  - public/logos/xai.svg
  - public/logos/fireworks.svg
  - public/logos/deepseek.svg
  - public/logos/amazon.svg
autonomous: true
requirements:
  - PRIC-02
  - PRIC-03
  - PRIC-04

must_haves:
  truths:
    - "User can search across model names and providers via a text input"
    - "User can filter by provider using a dropdown"
    - "User can filter by free tier availability (checkbox)"
    - "User can filter by price range (min/max input for input and output price)"
    - "User can filter by context window range (min/max)"
    - "User sees provider logos next to provider names"
    - "User sees model family grouping (Family column with sort)"
  artifacts:
    - path: "app/components/PricingTable.tsx"
      provides: "Table with search, provider filter, price range filter, context window filter, free tier filter"
      contains: "globalFilter"
    - path: "app/lib/provider-metadata.ts"
      provides: "Provider logo URL map and display name normalization"
      exports: ["providerLogos", "getProviderLogo"]
    - path: "public/logos/*.svg"
      provides: "SVG logo files for 12 providers"
  key_links:
    - from: "app/components/PricingTable.tsx"
      to: "@tanstack/react-table globalFilter"
      via: "setGlobalFilter on input change"
      pattern: "globalFilter"
    - from: "app/components/PricingTable.tsx"
      to: "app/lib/provider-metadata.ts"
      via: "import providerLogos for logo rendering"
      pattern: "providerLogos"
---

<objective>
Add filtering, search, provider logos, and model family grouping to the pricing table.

Purpose: Transform the basic sortable table from Plan 01 into a fully searchable and filterable comparison table. Users can search by model/provider name, filter by provider/price range/context window/free tier, and see provider logos alongside model family grouping.

Output:
- app/components/PricingTable.tsx (enhanced with search, filters, logos)
- app/lib/provider-metadata.ts (provider logo metadata map)
- public/logos/*.svg (12 provider SVG logos)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pricing-comparison-table/03-RESEARCH.md
@app/components/PricingTable.tsx
@app/lib/pricing-utils.ts
@app/lib/provider-metadata.ts
@src/providers/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create provider metadata map and SVG logos</name>
  <files>app/lib/provider-metadata.ts, public/logos/openai.svg, public/logos/anthropic.svg, public/logos/google.svg, public/logos/mistral.svg, public/logos/cohere.svg, public/logos/groq.svg, public/logos/together.svg, public/logos/perplexity.svg, public/logos/xai.svg, public/logos/fireworks.svg, public/logos/deepseek.svg, public/logos/amazon.svg</files>
  <action>
    Create app/lib/provider-metadata.ts with:

    1. A `providerLogos` Record mapping normalized provider name (lowercase) to logo path:
       ```
       {
         'openai': '/logos/openai.svg',
         'anthropic': '/logos/anthropic.svg',
         'google': '/logos/google.svg',
         'google gemini': '/logos/google.svg',
         'mistral': '/logos/mistral.svg',
         'cohere': '/logos/cohere.svg',
         'groq': '/logos/groq.svg',
         'together': '/logos/together.svg',
         'together ai': '/logos/together.svg',
         'perplexity': '/logos/perplexity.svg',
         'perplexity ai': '/logos/perplexity.svg',
         'xai': '/logos/xai.svg',
         'fireworks': '/logos/fireworks.svg',
         'fireworks ai': '/logos/fireworks.svg',
         'deepseek': '/logos/deepseek.svg',
         'amazon': '/logos/amazon.svg',
         'aws': '/logos/amazon.svg',
         'amazon bedrock': '/logos/amazon.svg',
       }
       ```

    2. A `getProviderLogo(providerName: string): string | null` function that normalizes the provider name (lowercase, trim) and looks up in the map. Returns null if not found.

    3. A `getUniqueProviders(data: PricingRow[]): string[]` function that extracts sorted unique provider names from the data array.

    Create 12 simple SVG logo files in public/logos/. Each SVG should be a 24x24 colored circle with the first letter of the provider name (e.g., "O" for OpenAI, "A" for Anthropic). Use distinct colors per provider:
    - openai: #10A37F (green)
    - anthropic: #D97757 (terracotta)
    - google: #4285F4 (blue)
    - mistral: #FF7000 (orange)
    - cohere: #39594D (dark green)
    - groq: #F55036 (red-orange)
    - together: #6366F1 (indigo)
    - perplexity: #20B2AA (teal)
    - xai: #000000 (black)
    - fireworks: #FF6B35 (orange)
    - deepseek: #4F46E5 (purple)
    - amazon: #FF9900 (amazon orange)

    SVG template for each (replace COLOR and LETTER):
    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
      <circle cx="12" cy="12" r="12" fill="COLOR"/>
      <text x="12" y="16" text-anchor="middle" fill="white" font-size="14" font-family="system-ui, sans-serif" font-weight="600">LETTER</text>
    </svg>
    ```
  </action>
  <verify>
    <automated>Test-Path public/logos/openai.svg,public/logos/anthropic.svg,public/logos/google.svg,public/logos/mistral.svg,public/logos/cohere.svg,public/logos/groq.svg,public/logos/together.svg,public/logos/perplexity.svg,public/logos/xai.svg,public/logos/fireworks.svg,public/logos/deepseek.svg,public/logos/amazon.svg | ForEach-Object { $_ } ; pnpm vitest run tests/pricing-utils.test.ts</automated>
  </verify>
  <done>All 12 SVG logos exist in public/logos/. provider-metadata.ts exports providerLogos map, getProviderLogo function, and getUniqueProviders function. Existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add search, all filters, and provider logos to PricingTable</name>
  <files>app/components/PricingTable.tsx</files>
  <behavior>
    - Search input above the table filters rows by model name OR provider name (case-insensitive)
    - Typing "gpt" in search shows only rows where modelName or sourceName contains "gpt"
    - Provider dropdown filter shows unique providers from the data
    - Selecting "OpenAI" from provider dropdown shows only OpenAI rows
    - Free tier checkbox filters to rows where inputPricePer1m === 0 && outputPricePer1m === 0 (or both null)
    - Checking "Free tier only" shows only free models
    - Price range filter: min/max number inputs for input price and output price
    - Entering min=1 and max=10 for input price shows only models with input price between $1 and $10 per 1M tokens
    - Context window filter: min/max number inputs for context window (in tokens)
    - Entering min=100000 for context window shows only models with 100K+ context
    - Combining search + provider filter + price range + context window + free tier applies all filters simultaneously
    - Provider column shows logo image (24x24) before the provider name text
    - Filter bar is above the table, search input is prominent
    - Clear/reset button resets all filters to defaults
    - Row count indicator shows "Showing X of Y models"
  </behavior>
  <action>
    Update app/components/PricingTable.tsx to add:

    1. **Global filter (search)**: Add a text input above the table. Use @tanstack/react-table's globalFilter feature. Filter function: case-insensitive match on modelName OR sourceName. Use useDeferredValue (React built-in) to debounce the input value — no external dependency needed.

    2. **Provider filter**: Add a <select> dropdown populated with unique provider names from the data (use getUniqueProviders from provider-metadata.ts). Wire as a column filter on the sourceName column. Include "All Providers" as default option.

    3. **Free tier filter**: Add a checkbox labeled "Free tier only". Apply as a pre-filter on the data array with useMemo: keep rows where both inputPricePer1m and outputPricePer1m are 0 or null.

    4. **Price range filter**: Add a collapsible "Filters" section (or inline row) with four number inputs:
       - Input price: min and max (placeholder: "Min" / "Max")
       - Output price: min and max (placeholder: "Min" / "Max")
       Apply as a pre-filter with useMemo: keep rows where inputPricePer1m is within [min, max] and outputPricePer1m is within [min, max]. Null values pass through (not filtered out).

    5. **Context window filter**: Add two number inputs for context window min/max (in tokens). Apply as a pre-filter: keep rows where contextWindow is within [min, max]. Null values pass through.

    6. **Provider logos**: In the Provider column cell renderer, look up the logo via getProviderLogo(sourceName). If found, render an <img> tag (24x24, rounded-full, mr-2 inline) before the text. If not found, render a fallback initial circle.

    7. **Row count**: Below the filter bar, show "Showing X of Y models" where X is filtered row count and Y is total row count.

    8. **Clear filters button**: A button that resets all filter states to defaults (globalFilter to '', provider to '', free tier off, price range cleared, context window cleared).

    Filter bar layout:
    - Top row: search input (flex-1) + provider select + free tier checkbox + clear button
    - Second row (collapsible "Advanced Filters"): price range inputs (input min/max, output min/max) + context window min/max
    - Use Tailwind responsive classes. On mobile, stack vertically.

    Pre-filter pipeline (applied with useMemo before useReactTable):
    1. Free tier filter (if active)
    2. Price range filter (if min/max specified)
    3. Context window filter (if min/max specified)
    The filtered data is then passed to useReactTable, which handles globalFilter (search) and columnFilters (provider) internally.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | Select-String -Pattern "error|Error" | Select-Object -First 5</automated>
  </verify>
  <done>Search filters by model/provider name. Provider dropdown filters by provider. Free tier checkbox filters to free models. Price range and context window filters work. Provider logos display next to names. Row count shows "Showing X of Y". Clear button resets all filters. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Filter logic | Search and filter inputs are used for client-side filtering only, never sent to server |
| Provider name -> Logo path | Logo path derived from provider name via static map (no user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Search/filter inputs | accept | Client-side filter only, no server request, no XSS risk with React escaping |
| T-03-04 | Spoofing | Provider logos | accept | Logo map is static, not user-controllable. SVGs are simple shapes with no script tags. |
| T-03-SC | Tampering | npm installs | accept | No new packages — uses React useDeferredValue (built-in) instead of lodash.debounce |
</threat_model>

<verification>
1. `pnpm build` succeeds with no errors
2. Visit localhost:3000 with dev server
3. Type "gpt" in search — only GPT models shown
4. Select "OpenAI" from provider dropdown — only OpenAI models shown
5. Check "Free tier only" — only free models shown
6. Enter price range (min=1, max=10 for input) — only models in range shown
7. Enter context window min=100000 — only 100K+ models shown
8. Combine filters — all apply simultaneously
9. Click "Clear" — all filters reset
10. Provider logos display next to provider names
</verification>

<success_criteria>
- Search filters across model name and provider name in real-time
- Provider dropdown shows all unique providers from data
- Free tier checkbox filters to models with zero/null pricing
- Price range filters (input min/max, output min/max) work correctly
- Context window range filter works correctly
- All filters compose (search + provider + price range + context window + free tier)
- Provider logos display as 24x24 images next to provider names
- Row count indicator shows filtered vs total
- Clear button resets all filters
</success_criteria>

<output>
Create `.planning/phases/03-pricing-comparison-table/03-02-SUMMARY.md` when done
</output>
