---
phase: 07-intelligence-analytics
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/promotions/page.tsx
  - app/components/PromotionsPageClient.tsx
  - app/components/PromotionCard.tsx
  - tests/components/promotions-page.test.tsx
autonomous: true
requirements:
  - INTL-03
must_haves:
  truths:
    - "User can view all promotions in a card grid layout"
    - "User can filter promotions by type (free_tier, promotion, beta)"
    - "Active promotions appear first, expired ones are grayed out"
    - "Each card shows promotion name, provider, type badge, and expiration"
  artifacts:
    - path: "app/promotions/page.tsx"
      provides: "Server component fetching promotions from database"
    - path: "app/components/PromotionsPageClient.tsx"
      provides: "Card grid with type filter and active/expired grouping"
    - path: "app/components/PromotionCard.tsx"
      provides: "Individual promotion card component"
  key_links:
    - from: "app/promotions/page.tsx"
      to: "src/db/schema.ts"
      via: "Drizzle query on promotions table"
      pattern: "db\\.select.*from.*promotions"
    - from: "app/components/PromotionsPageClient.tsx"
      to: "app/components/PromotionCard.tsx"
      via: "import and render PromotionCard"
      pattern: "import.*PromotionCard"
---

<objective>
Build the /promotions page with a card grid layout showing all promotions, filterable by type, with active promotions sorted first and expired ones grayed out.

Purpose: Delivers INTL-03 (dedicated promotion/free tier tracker) as a complete vertical slice.
Output: Working /promotions page with type filter, card grid, and active/expired display.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-intelligence-analytics/07-CONTEXT.md
@.planning/phases/07-intelligence-analytics/07-RESEARCH.md
@.planning/phases/07-intelligence-analytics/07-UI-SPEC.md

@app/components/PromotionsList.tsx
@app/page.tsx
@app/lib/pricing-utils.ts
@app/lib/url-utils.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PromotionCard component</name>
  <files>app/components/PromotionCard.tsx, tests/components/promotions-page.test.tsx</files>
  <read_first>
    - app/components/PromotionsList.tsx (existing promotion display pattern: PromotionData interface, typeBadgeStyles, isActive, formatDateRange)
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-07: card grid, D-08: show all gray expired, D-09: data from promotions table)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (card styling, badge colors, responsive grid, spacing, typography)
    - .planning/phases/07-intelligence-analytics/07-RESEARCH.md (PromotionCard code example, Pitfall 4: timezone comparison)
    - app/lib/url-utils.ts (isSafeUrl for source links)
  </read_first>
  <behavior>
    - PromotionCard renders a card with type badge, description, credits, date range
    - Active cards: white background, standard border
    - Expired cards: gray background (bg-gray-50), reduced opacity (opacity-60)
    - Type badge colors: free_tier=green, promotion=blue, beta=purple (matching existing typeBadgeStyles)
    - Days remaining shown for promotions with endDate
    - "Expired" label shown for expired promotions
    - Source link shown if sourceUrl is safe
  </behavior>
  <action>
    Create app/components/PromotionCard.tsx extending the PromotionsList pattern:

    - Reuse the `PromotionData` interface from PromotionsList (import it)
    - Reuse `typeBadgeStyles` from PromotionsList (or duplicate the Record -- it's small)
    - Component accepts `{ promo: PromotionData }` props
    - Compute `isActive`: if endDate is null, active = true; otherwise endDate > now (use Date.now() for timezone consistency per Pitfall 4)
    - Compute `daysRemaining`: days until endDate, "Expired" if negative, "Expires today" if 0, "Expires tomorrow" if 1, "{N} days remaining" otherwise
    - Card layout per UI-SPEC:
      - Container: `border rounded-lg p-4` with conditional styling:
        - Active: `bg-white border-gray-200`
        - Expired: `bg-gray-50 border-gray-200 opacity-60`
      - Top row: type badge (inline-block px-2 py-0.5 rounded text-xs font-medium) + days remaining (text-xs text-gray-500)
      - Description: text-sm text-gray-900
      - Credits (if present): text-xs text-gray-600 mt-1 "Credits: {credits}"
      - Source link (if sourceUrl and isSafeUrl): text-xs text-blue-600 hover:text-blue-800 underline "View details"
    - Badge styles match existing: free_tier=bg-green-100 text-green-800, promotion=bg-blue-100 text-blue-800, beta=bg-purple-100 text-purple-800

    Create tests/components/promotions-page.test.tsx:
    - Test PromotionCard renders active card with white background
    - Test PromotionCard renders expired card with opacity-60
    - Test type badge shows correct text and color class
    - Test days remaining calculation (future date, today, tomorrow, expired)
    - Test credits displayed when present
    - Test source link rendered when sourceUrl is safe
  </action>
  <verify>
    <automated>pnpm test tests/components/promotions-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PromotionCard.tsx exports PromotionCard component
    - Active cards have bg-white, expired have opacity-60
    - Type badge uses correct color classes matching typeBadgeStyles
    - Days remaining shows correct text for various date scenarios
    - tests/components/promotions-page.test.tsx passes
  </acceptance_criteria>
  <done>PromotionCard renders individual promotion with type badge, expiration status, and conditional styling</done>
</task>

<task type="auto">
  <name>Task 2: Create /promotions page with filter and card grid</name>
  <files>app/promotions/page.tsx, app/components/PromotionsPageClient.tsx</files>
  <read_first>
    - app/page.tsx (server component pattern: ISR, try/catch, Drizzle query)
    - app/digest/page.tsx (searchParams pattern)
    - app/components/PromotionsList.tsx (PromotionData interface, data shape from promotions table)
    - .planning/phases/07-intelligence-analytics/07-CONTEXT.md (D-06: /promotions route, D-07: card grid with filter, D-08: show all, D-09: data from promotions table)
    - .planning/phases/07-intelligence-analytics/07-UI-SPEC.md (/promotions page layout, filter pills, card grid responsive, empty states, copywriting)
    - src/db/schema.ts (promotions table columns: id, sourceId, modelPattern, type, description, credits, startDate, endDate, sourceUrl)
  </read_first>
  <action>
    Create two files for the /promotions page:

    1. **app/promotions/page.tsx** -- Server component:
       - `export const revalidate = 60`
       - Query Drizzle: `db.select().from(promotions)` with LEFT JOIN to sources for provider name
       - Select: promotions.id, promotions.modelPattern, promotions.type, promotions.description, promotions.credits, promotions.startDate, promotions.endDate, promotions.sourceUrl, sources.name as sourceName
       - Try/catch with empty array fallback
       - Map results to PromotionData shape (matching PromotionsList interface + sourceName)
       - Pass `{ promotions: PromotionData[] }` to PromotionsPageClient

    2. **app/components/PromotionsPageClient.tsx** -- Client wrapper:
       - Extended PromotionData interface adds `sourceName: string | null` field
       - Props: `{ promotions: Array<PromotionData & { sourceName: string | null }> }`
       - State: `activeFilter: 'all' | 'free_tier' | 'promotion' | 'beta'`
       - Page layout per UI-SPEC: max-w-6xl mx-auto px-4 py-8
       - Heading: "Promotions & Free Tiers" + subheading per UI-SPEC copywriting
       - Filter pills row per UI-SPEC: "All", "Free Tier", "Promotion", "Beta"
         - Active pill: bg-blue-600 text-white
         - Inactive pill: bg-gray-100 text-gray-700 hover:bg-gray-200
         - Styling matches existing button patterns (CostCalculator tabs)
       - Filter logic: client-side filter on already-fetched data (per RESEARCH: client-side filter)
       - Sort: active promotions first, then expired (per D-08)
       - Card grid per UI-SPEC: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4
       - Render PromotionCard for each filtered promotion
       - Empty state per UI-SPEC: "No active promotions" / "Promotions and free tier offers will appear here as providers announce them."
       - Provider name shown on each card (from sourceName)
  </action>
  <verify>
    <automated>grep -c "PromotionsPageClient\|PromotionCard" app/promotions/page.tsx app/components/PromotionsPageClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/promotions/page.tsx exports default async function with revalidate = 60
    - Server component queries promotions table with sources JOIN
    - PromotionsPageClient renders filter pills: All, Free Tier, Promotion, Beta
    - Active filter highlighted with bg-blue-600
    - Card grid uses responsive columns: 1-col mobile, 2-col md, 3-col lg
    - Active promotions sorted before expired ones
    - Empty state shows "No active promotions" message
    - Page uses max-w-6xl mx-auto px-4 py-8 container per UI-SPEC
  </acceptance_criteria>
  <done>User can visit /promotions, see all promotions in a card grid, filter by type, with active ones first and expired grayed out</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server->client | Promotion data from database passed to client (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | sourceUrl links | mitigate | Use existing isSafeUrl validation from url-utils.ts before rendering links |
</threat_model>

<verification>
- /promotions page loads without errors
- Type filter switches between All, Free Tier, Promotion, Beta
- Active promotions appear first, expired grayed out
- Card grid is responsive (1/2/3 columns)
- Empty state shows when no promotions exist
</verification>

<success_criteria>
- /promotions page shows all promotions in a card grid
- Type filter works correctly (All, Free Tier, Promotion, Beta)
- Active promotions sorted before expired
- Expired cards have reduced opacity
- Badge colors match existing PromotionsList pattern
- Empty state matches UI-SPEC copywriting
</success_criteria>

<output>
Create `.planning/phases/07-intelligence-analytics/07-03-SUMMARY.md` when done
</output>
