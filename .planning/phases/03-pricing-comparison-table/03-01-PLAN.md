---
phase: 03-pricing-comparison-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/lib/pricing-utils.ts
  - tests/pricing-utils.test.ts
  - app/components/PricingTable.tsx
autonomous: true
requirements:
  - PRIC-01
  - PRIC-06

must_haves:
  truths:
    - "User sees a table of all models with input/output pricing per 1M tokens and context window"
    - "User can sort by any column (model, provider, input price, output price, context window)"
    - "Each row shows a confidence badge (green/yellow/red)"
    - "Provider name is displayed alongside model name"
  artifacts:
    - path: "app/lib/pricing-utils.ts"
      provides: "formatPrice, formatContextWindow, sanitizeDisplayName, getConfidenceColor, getModelFamily"
      exports: ["formatPrice", "formatContextWindow", "sanitizeDisplayName", "getConfidenceColor", "getModelFamily"]
    - path: "app/components/PricingTable.tsx"
      provides: "Interactive client table with @tanstack/react-table sorting"
      contains: "useReactTable"
    - path: "app/page.tsx"
      provides: "Server Component with JOIN query passing data to PricingTable"
      contains: "leftJoin"
    - path: "tests/pricing-utils.test.ts"
      provides: "Unit tests for all utility functions"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/PricingTable.tsx"
      via: "props: data array and lastUpdated timestamp"
      pattern: "PricingTable"
    - from: "app/components/PricingTable.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable hook"
      pattern: "useReactTable"
    - from: "app/page.tsx"
      to: "src/db/schema.ts"
      via: "Drizzle JOIN query on extractions + sources"
      pattern: "leftJoin"
---

<objective>
Build the data layer and a basic interactive pricing table with sorting.

Purpose: Replace the static HTML table in app/page.tsx with a @tanstack/react-table-powered client component that fetches data via a Drizzle JOIN (extractions + sources) and supports column sorting. This is the foundation that Plans 02 and 03 build on.

Output:
- app/lib/pricing-utils.ts (extracted + new utility functions)
- app/components/PricingTable.tsx (client component with sorting)
- app/page.tsx (server component with JOIN query)
- tests/pricing-utils.test.ts (unit tests for utilities)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pricing-comparison-table/03-RESEARCH.md
@app/page.tsx
@src/db/schema.ts
@src/db/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract utility functions and create pricing-utils module</name>
  <files>app/lib/pricing-utils.ts, tests/pricing-utils.test.ts</files>
  <behavior>
    - formatPrice(null) returns "N/A"
    - formatPrice(0) returns "$0.00"
    - formatPrice(3.5) returns "$3.50"
    - formatPrice(0.0025) returns "$0.0025" (up to 4 decimal places for small values)
    - formatContextWindow(null) returns "N/A"
    - formatContextWindow(128000) returns "128K"
    - formatContextWindow(1000000) returns "1M"
    - formatContextWindow(2000000) returns "2M"
    - formatContextWindow(500) returns "500"
    - sanitizeDisplayName strips bidi override characters (U+202A-U+202E)
    - sanitizeDisplayName truncates at 100 chars with "..."
    - getConfidenceColor("verified") returns class string containing "green"
    - getConfidenceColor("likely") returns class string containing "yellow"
    - getConfidenceColor("low_confidence") returns class string containing "red"
    - getModelFamily("gpt-4o") returns "GPT-4"
    - getModelFamily("gpt-4o-mini") returns "GPT-4"
    - getModelFamily("claude-3-opus") returns "Claude 3"
    - getModelFamily("claude-3.5-sonnet") returns "Claude 3.5"
    - getModelFamily("gemini-2.0-flash") returns "Gemini"
    - getModelFamily("gemini-1.5-pro") returns "Gemini"
    - getModelFamily("mistral-large") returns "Mistral"
    - getModelFamily("llama-3.1-70b") returns "Llama"
    - getModelFamily("unknown-model") returns "Other"
  </behavior>
  <action>
    Create app/lib/pricing-utils.ts by extracting the existing formatPrice, formatContextWindow, sanitizeDisplayName, and getConfidenceColor functions from app/page.tsx. Add the new getModelFamily function that derives model family from model name prefix. Per D-01 (server/client split), these utilities are shared by both server and client components.

    getModelFamily logic (per PRIC-02, model family grouping):
    - Match lowercase model name against known prefixes:
      - "gpt-4" -> "GPT-4"
      - "gpt-3" -> "GPT-3"
      - "claude-3.5" or "claude-3.5" -> "Claude 3.5"
      - "claude-3" -> "Claude 3"
      - "claude-2" -> "Claude 2"
      - "gemini" -> "Gemini"
      - "mistral" -> "Mistral"
      - "llama" -> "Llama"
      - "cohere" or "command" -> "Cohere"
      - "deepseek" -> "DeepSeek"
      - "qwen" -> "Qwen"
      - Default -> "Other"

    Update formatPrice to handle very small values (< 0.01) with up to 4 decimal places instead of always 2.

    Export all five functions: formatPrice, formatContextWindow, sanitizeDisplayName, getConfidenceColor, getModelFamily.

    Create tests/pricing-utils.test.ts with comprehensive tests per the behavior spec above. Use Vitest. Import from app/lib/pricing-utils.ts.

    Update app/page.tsx to import from app/lib/pricing-utils.ts instead of defining functions inline. Keep the same page behavior for now.
  </action>
  <verify>
    <automated>pnpm vitest run tests/pricing-utils.test.ts</automated>
  </verify>
  <done>All utility functions extracted, exported, and tested. formatPrice handles edge cases. getModelFamily correctly maps known model prefixes to family names. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PricingTable client component with @tanstack/react-table sorting</name>
  <files>app/components/PricingTable.tsx, app/page.tsx</files>
  <behavior>
    - PricingTable renders a table with columns: Provider, Model, Family, Input ($/1M), Output ($/1M), Context Window, Confidence
    - Clicking a column header toggles sort direction (asc -> desc -> none)
    - Sort indicator (arrow) appears on sorted columns
    - Confidence column renders colored badge (green/yellow/red)
    - Provider column shows source name from JOIN
    - Family column shows model family derived from model name
    - Table handles empty data gracefully (shows "No pricing data" message)
    - Prices are formatted with $ prefix and appropriate decimals
    - Context window is formatted as K/M tokens
  </behavior>
  <action>
    Create app/components/PricingTable.tsx as a 'use client' component.

    Define the PricingRow type matching the shape passed from the server component:
    ```
    {
      id: number;
      modelName: string;
      inputPricePer1m: number | null;
      outputPricePer1m: number | null;
      contextWindow: number | null;
      confidence: 'verified' | 'likely' | 'low_confidence';
      collectedAt: Date;
      sourceName: string | null;
      sourceUrl: string | null;
    }
    ```

    Use @tanstack/react-table's useReactTable with:
    - getCoreRowModel, getSortedRowModel
    - Sorting state managed via useState
    - Column definitions using createColumnHelper

    Columns (in order):
    1. Provider (sourceName) - text, sortable
    2. Model (modelName) - text, sortable, font-medium
    3. Family (derived via getModelFamily) - text, sortable
    4. Input $/1M (inputPricePer1m) - right-aligned, formatted, sortable
    5. Output $/1M (outputPricePer1m) - right-aligned, formatted, sortable
    6. Context Window (contextWindow) - right-aligned, formatted, sortable
    7. Confidence - center-aligned, badge component

    Use the existing getConfidenceColor function for badge styling (custom Tailwind spans, NOT Tremor Badge - per research Pitfall 5, Tremor 3.x may conflict with Tailwind 4.x).

    Table styling: Tailwind classes matching the existing design language (bg-gray-50 headers, border-b dividers, hover:bg-gray-50 rows). Use overflow-x-auto wrapper for horizontal scroll.

    Component signature: `export function PricingTable({ data, lastUpdated }: { data: PricingRow[]; lastUpdated: Date | null })`

    Update app/page.tsx:
    - Change from static HTML table to importing and rendering PricingTable
    - Update the Drizzle query to JOIN extractions with sources:
      ```
      db.select({
        id: extractions.id,
        modelName: extractions.modelName,
        inputPricePer1m: extractions.inputPricePer1m,
        outputPricePer1m: extractions.outputPricePer1m,
        contextWindow: extractions.contextWindow,
        confidence: extractions.confidence,
        collectedAt: extractions.collectedAt,
        sourceName: sources.name,
        sourceUrl: sources.url,
      })
      .from(extractions)
      .leftJoin(sources, eq(extractions.sourceId, sources.id))
      .orderBy(desc(extractions.collectedAt))
      ```
    - Compute lastUpdated as the most recent collectedAt from the result set
    - Pass data and lastUpdated to PricingTable
    - Keep the "AI Daily" branding header and description
    - Keep ISR revalidate = 60
    - Keep the try-catch for DB unavailability during build

    Import eq, desc from drizzle-orm for the JOIN query.
  </action>
  <verify>
    <automated>pnpm vitest run tests/pricing-utils.test.ts && pnpm build 2>&1 | Select-String -Pattern "error|Error" | Select-Object -First 5</automated>
  </verify>
  <done>PricingTable component renders with @tanstack/react-table sorting. Server component joins extractions+sources. Build succeeds with no TypeScript errors. Column sorting works (click header to toggle). Confidence badges display with correct colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> Server Component | Extractions data from PostgreSQL is trusted (collected by pipeline) |
| Server Component -> Client Component | Data passed as props is pre-sanitized by sanitizeDisplayName |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Model name display | mitigate | sanitizeDisplayName strips bidi overrides (WR-01, already exists) |
| T-03-02 | Information Disclosure | Source URLs | accept | URLs point to public pricing pages, no sensitive data |
| T-03-SC | Tampering | npm installs | accept | @tanstack/react-table and date-fns are pre-verified in CLAUDE.md stack |
</threat_model>

<verification>
1. `pnpm vitest run tests/pricing-utils.test.ts` - all utility function tests pass
2. `pnpm build` - Next.js build succeeds with no TypeScript errors
3. `pnpm dev` and visit localhost:3000 - table renders with data, sorting works on all columns
</verification>

<success_criteria>
- PricingTable component renders all extraction rows with provider name, model name, family, prices, context window, and confidence badge
- Clicking any column header sorts the table (ascending, descending, or cleared)
- Build compiles with zero TypeScript errors
- All utility function tests pass
</success_criteria>

<output>
Create `.planning/phases/03-pricing-comparison-table/03-01-SUMMARY.md` when done
</output>
